# Reglas ProGuard/R8 del proyecto. Vacío por ahora (isMinifyEnabled = false
# en el build type "release" del scaffold) — se rellenará cuando se active
# minify, si hace falta, junto con las reglas del SDK de Dropbox/Room.
