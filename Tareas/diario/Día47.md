# ROM Manager Local — Día 47

## Contexto

Día46 cerró VAL-FIX-3/6, ANBERNIC-UX-9/10 y TABS-FIX-6-DISC en la rama
`feature/tabs-fix-6-revisar-copias` (PR #155) — mergeada a `develop` antes de
empezar esta sesión (confirmado en el `git log`: `686db18` es el merge commit).
Sesión de hoy: recoger la lista "siguiente sesión recomendada" de Día46
(VAL-FIX-4/5/7, MEJ-2..5, TEST-GAP-1, entre otros) y acordar con el usuario el
orden concreto a abordar.

## Objetivos del día

Orden acordado con el usuario (relación valor/esfuerzo, todo con esfuerzo S/M):

1. MEJ-3 — backup automático de la BD antes de `apply`
2. VAL-FIX-4 — excluir DuckStation del auto-sync ADB (`Permission denied` en Android 11+)
3. VAL-FIX-5 — preview de cable sync cuenta saves remotos por ADB en vez de "no accesible"
4. VAL-FIX-7 — cable sync ADB registra `verified=` en `save_sync_log`
5. MEJ-2 — deshacer último apply
6. TEST-GAP-1 — tests directos para los caminos de rollback de `file_renamer.py`

Dejados fuera deliberadamente: SAGE-2/3 (bloqueante de una fase de Sage aún no
llegada), REV43-52 (refactor cosmético de `cli.py`), D37-8 (necesita PC ajeno
sin Python).

## Trabajo realizado

Todo en una sola rama `feature/dia47-quick-wins` → **PR #157, mergeada a
`develop`** (fast-forward `686db18..96667a7`):

1. **MEJ-3** — `backup_database()` nuevo en `_RepositoryBase`
   (`database/repositories/base.py`): snapshot vía `sqlite3.Connection.backup()`
   (seguro con WAL, a diferencia de copiar el archivo a pelo) a
   `<data_dir>/db-backup/`, poda a `keep_n=5`. Llamado desde `_do_apply`
   (`web/handlers/organize.py`) y `cli.py apply`, antes de construir el plan.
   2 tests nuevos en `test_repository.py`.
2. **VAL-FIX-4** — el usuario preguntó si merecía la pena intentar recuperar
   las memcards de DuckStation vía `run-as` (sandbox sin root) en vez de solo
   excluirlas; se descartó porque solo funciona si la build es *debuggable*, y
   el DuckStation de Play Store no lo es. Fix aplicado: `accessible: False` en
   el mapping de DuckStation (`config.py`), mismo patrón ya usado por Dolphin
   — `get_adb_sync_sources()` lo excluye, cero reintentos. `docs/emulator-compat.md`
   actualizado (tabla PS1 + resumen rápido) con el workaround real (cambiar
   Memory Cards → Directory a una carpeta pública en DuckStation). 1 test
   nuevo en `test_config.py`.
3. **VAL-FIX-5** — `_build_cable_sync_preview` (`web/builders/misc.py`)
   reutiliza `AdbTransport.ls_recursive()` (el mismo método que ya usa Sync
   Doctor) cuando `mode == "adb"` y hay `serial`; sin serial muestra "conecta
   el dispositivo primero", con error de transporte muestra el mensaje.
   Frontend (`sync.js`, `loadCableSyncPreview`) manda `serial`
   (`#cable-adb-device`) y `android_path` (`#auto-sync-android-path`) en modo
   ADB. 3 tests nuevos (`test_cable_sync_preview.py`).
4. **VAL-FIX-7** — el registro en `save_sync_log` ya existía desde REV43-33
   (sesión anterior); lo que faltaba era `verified=`. `_sql_log()` gana el
   parámetro; en `_adb_copy_to_pc`/`_adb_copy_to_device`, llegar a la línea
   "ok" implica que `pull()`/`push()` con `verify=True` ya comprobó el MD5 (un
   mismatch lanza `OSError` antes de esa línea). 1 test nuevo
   (`test_cable_sync_adb_verified_log.py`).
5. **MEJ-2** — `get_last_apply_batch()` (`database/repositories/games.py`)
   agrupa por el `created_at` compartido del último apply; `_do_undo_last_apply()`
   (`web/handlers/organize.py`) reproduce la misma rama cue/gdi vs archivo
   suelto que `_do_apply`, pero con `source`/`target` invertidos, y llama
   `apply_rename()` con las rutas invertidas — cada undo queda registrado como
   una fila nueva, así que un segundo undo revierte el undo (redo) en vez de
   no hacer nada. Backup de la BD (MEJ-3) antes de tocar nada. Job en
   background (`undo_apply` en `JOB_NAMES`, endpoints `POST /api/undo-last-apply`
   + `GET /api/undo-last-apply-status`, patrón moderno `job_manager.get_job()`
   sin tocar el `get_status()` compartido — mismo patrón que `download_dats`).
   Botón "Deshacer último apply" en Organizar (`organize.js`, `tab-plan.html`).
   2 tests nuevos (`test_undo_last_apply.py`).
6. **TEST-GAP-1** — `tests/test_file_renamer.py` ya existía con buena
   cobertura de éxito, pero le faltaban exactamente los 2 huecos que nombra la
   tarea: (a) rollback de `rename_rom_with_saves` cuando falla un save a mitad
   — nunca se probaba directamente, solo vía el handler de apply; (b) el caso
   normal de rollback en `move_disc_set_to_subfolder` (ya había un test, pero
   solo del caso "el rollback en sí falla", REV43-24). Añadidos: rollback de
   `rename_rom_with_saves` (ROM+saves vuelven a su estado y contenido
   original), rollback exitoso de `move_disc_set_to_subfolder` (bin1 vuelve a
   su sitio, carpeta destino vacía se limpia), y un set de 2 discos movido
   íntegro (nombres y contenido de ambos `.bin` intactos). 3 tests nuevos.

915 tests en verde (903 al empezar + 12 nuevos), lint (`ruff check`) y formato
(`ruff format --check`) limpios en cada paso. `openapi.json` actualizado con
los 2 endpoints nuevos de MEJ-2.

## Archivos modificados

- Backend: `cli.py`, `config.py`, `database/repositories/base.py`,
  `database/repositories/games.py`, `web/builders/misc.py`,
  `web/handlers/organize.py`, `web/handlers/sync_cable.py`,
  `web/jobs/manager.py`.
- Frontend: `web/static/js/main.js`, `web/static/js/tabs/organize.js`,
  `web/static/js/tabs/sync.js`, `web/static/partials/tab-plan.html`.
- Docs: `docs/emulator-compat.md`, `web/static/openapi.json`.
- Tests: `test_cable_sync_adb_verified_log.py` (nuevo),
  `test_cable_sync_preview.py` (nuevo), `test_undo_last_apply.py` (nuevo),
  `test_config.py`, `test_file_renamer.py`, `test_repository.py`.
- `Tareas/backlog.md` (MEJ-2/3, VAL-FIX-4/5/7, TEST-GAP-1 marcados ✅),
  `Tareas/diario/Día47.md` (este archivo).

## Estado al finalizar

Los 6 objetivos del día completos, en PR #157 mergeada a `develop` (CI verde:
lint + pytest 3.11/3.12), rama remota y local `feature/dia47-quick-wins`
eliminadas tras el merge. Working tree de `develop` limpio.

Verificado con: suite completa (`pytest`, 915 tests), lint y formato. Dos
piezas quedan solo con test unitario, sin verificación manual contra hardware
real (marcado explícitamente en el test plan de la PR):

- El botón "Deshacer último apply" no se probó visualmente en el navegador.
- El preview de cable sync en modo ADB (VAL-FIX-5) no se probó con una
  consola real conectada — el test cubre la lógica con `AdbTransport`
  monkeypatcheado, no el flujo end-to-end.

## Siguiente sesión recomendada

1. **Verificación manual pendiente de esta sesión**: probar "Deshacer último
   apply" en el navegador (Organizar → Renombrar) y el preview de cable sync
   ADB (VAL-FIX-5) con la consola real conectada.
2. VAL-FIX-6-DISC ya cerrado; no queda ningún VAL-FIX abierto salvo los ya
   descartados de alcance (ninguno pendiente en el bloque VAL-FIX).
3. MEJ-4 (sync de cheats `.cht`, ~10 líneas) y MEJ-5 ("¿A qué juego hoy?")
   siguen sin tocar — orden sugerido del backlog: MEJ-1→2→3→4→5, con 1/2/3 ya
   cerrados.
4. SAGE-2/3 (bloqueantes de una fase de Sage aún no llegada), REV43-52 (menor,
   refactor de `cli.py`), D37-8 (requiere PC ajeno) — sin tocar todavía.
5. INBOX-FIX-4 (decisión de diseño sobre auto-apply del wizard) y MATCH-FIX-1
   ya cerrado — revisar si queda pendiente la decisión de usuario sobre las 15
   colecciones de JUNK-REVIEW-1 categoría 2 (ver bloque INBOX-FIX del backlog).
