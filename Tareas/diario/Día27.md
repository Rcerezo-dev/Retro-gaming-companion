# Retro Vault — Día 27 (2026-06-18)

## Contexto

El **Día26 queda completo** (robustez + perf + SRP). Resumen de lo cerrado:

- **OBS-1a/b/c** — ningún `except` traga el error en silencio; regla de lint
  `S110`/`S112` en el gate. (PRs #11–#13)
- **PERF-1** — hashing cancelable (`stop_event`) y paralelo
  (`calculate_hashes_batch`, lotes de 64). (PR #14)
- **TEST-1** — cubierta la capa de sync/daemons (conflict policies, daemons SD,
  garantía "backup falla → sync no se bloquea"). (PR #15)
- **SRP-1a/b/c** — `response_builders.py`, `handlers/esde.py` y
  `database/repository.py` divididos en paquetes por dominio/agregado.
  (PRs #17 / #18 / #19)
- **CLEAN-1** — late imports de ciclo eliminados: `server.py` importa los 11
  handlers + `Router` a nivel de módulo; docstrings de daemons al día.
  (PR #20, en revisión)

El histórico del Día26 se ha archivado en `Tareas/diario/archivo/Día26.md`.

Lo que **queda abierto** del roadmap de arquitectura (ARC) son los clusters del
backlog. El siguiente bloque natural es **terminar la migración al JobManager**:
ya está casi todo migrado (scan, apply, sync, tree_diff, CHD/CSO/ZIP, scraper,
health, RA, inbox, setup); solo falta **`cable_sync`**, que sigue usando los
dicts globales de `web/state.py`. Cerrar `cable_sync` desbloquea el barrido final
ARC-JM-6 (eliminar los 12 dicts globales + el parámetro `srv_mod`).

> Nota para la sesión: el backlog (ARC-JM-3c) apunta a `web/handlers/sync.py`,
> pero tras SRP-1 **el handler de cable vive en `web/handlers/sync_cable.py`**
> (`m = srv_mod` → `_cable_progress` / `_cable_cancel`). Mirar ahí.

## Objetivos del día

Cluster **`refactor/arc-jm-cable`** (ARC-JM-3) — migración indivisible:

- [x] **ARC-JM-3a** — Migrados los daemons de cable (`web/cable_sync_daemon.py`:
  `_auto_sync_loop`, `_run_sd_auto_sync`, `_sd_card_sync_loop`) a `_state._job_manager`.
- [x] **ARC-JM-3b** — *(replanteado)* No hace falta inyectar `on_progress` ni cablear
  nada en `server.py`: el patrón ya establecido en `daemons.py` (inbox/health) es usar
  `_state._job_manager` directamente. Los daemons de cable siguen ese mismo patrón.
- [x] **ARC-JM-3c** — Migrado el handler `cable_sync` en `web/handlers/sync_cable.py`
  (`_cable_progress`/`_cable_cancel`/`_jobs`/`_job_results` → `job_manager`), más el
  hub de estado/cancelación en `handlers/scan.py` (`/api/job-status` y `/api/stop-job`).

Si queda margen, abrir el cluster siguiente (**`refactor/arc-jm-cleanup`**,
ARC-JM-6), ya desbloqueado:

- [ ] ARC-JM-6a — grep `_progress` / `_jobs` / `_job_results` en `server.py` y `state.py`.
- [ ] ARC-JM-6b/c — eliminar los 12 dicts de progreso + `_jobs` / `_job_results`.
- [ ] ARC-JM-6d — eliminar el parámetro `srv_mod` de `make_handler` y de los
  `register()` (incluye el alias legado `_srv_mod` que dejó CLEAN-1).

**Criterio de aceptación:** ningún handler ni daemon escribe en los dicts de
`web/state.py`; todo el progreso pasa por `JobManager`. 430+ tests verdes + ruff.

## Trabajo realizado

**ARC-JM-3 — `cable_sync` migrado por completo al `JobManager`.** Era el último job
que escribía en los dicts globales de `web/state.py`. Tres frentes a la vez (la
migración es indivisible: daemon y handler comparten el slot `cable_sync`):

- **Handler `_do_cable_sync`** (`handlers/sync_cable.py`): el guard `_job_lock` +
  `_jobs["cable_sync"]` + el spawn manual de hilo → `job_manager.start("cable_sync", run)`;
  `_cable_cancel` → `job_manager.cancel_event("cable_sync")`; `_cable_progress.update`
  → `job_manager.update_progress`; `_job_results[...]` → variable `job_result` +
  `job_manager.finish` en el `finally`. El cálculo de velocidad ya no lee el dict de
  vuelta (ahora `_last_speed` local). Eliminado `import threading` (ya no se usa).
- **Daemons** (`cable_sync_daemon.py`): los tres usan `_state._job_manager`
  directamente (mismo patrón que inbox/health en `daemons.py`). El SD loop lanza el
  worker vía `job_manager.start("cable_sync", lambda: _run_sd_auto_sync(...))`.
  Eliminado `import threading`.
- **Hub** (`handlers/scan.py`): `/api/job-status` ya no parchea los campos de cable
  (los devuelve `job_manager.get_status()`); `/api/stop-job` cancela cualquier job
  vía `job_manager.cancel` filtrando por `JOB_NAMES` — fuera la rama legada de cable.
- **Test** (`test_cable_sync_daemon.py`): el helper lee el resultado de
  `job_manager.get_status()["cable_sync_result"]` en vez de `_state._job_results`.

Verificación: **430 tests verdes**, ruff (lint+format) limpio, import del servidor OK.

> **Huérfanos creados (para ARC-JM-6b):** `_cable_progress` y `_cable_cancel` en
> `state.py` ya no los usa nadie. Se eliminan en el barrido de los 12 dicts, no aquí
> (mantener 6b como una sola unidad coherente).

## Archivos modificados

- `src/rom_manager/web/handlers/sync_cable.py` — handler migrado; `-import threading`.
- `src/rom_manager/web/cable_sync_daemon.py` — 3 daemons migrados; `-import threading`.
- `src/rom_manager/web/handlers/scan.py` — job-status/stop-job simplificados; `+JOB_NAMES`.
- `tests/test_cable_sync_daemon.py` — helper lee de `job_manager`.

## Estado al finalizar

ARC-JM-3 completo en rama `refactor/arc-jm-cable`. `cable_sync` ya no toca
`web/state.py`. Queda **ARC-JM-6** (ya desbloqueado): eliminar los 12 dicts de
progreso, `_jobs`/`_job_results`, el helper `_start_job` y el parámetro `srv_mod`
de los 8 `register()`. Ojo: `duplicates.py` aún lee `srv_mod._job_results.get("ra_check")`
— revisar en 6 si es un residuo que hay que repuntar a `job_manager`.

## Siguiente sesión recomendada

`refactor/arc-jm-cleanup` (ARC-JM-6a–6d) — el barrido final de globales, ahora
desbloqueado por esta sesión. Luego el split de `AppConfig`
(`refactor/arc-cfg-device-detector` → `arc-cfg-sync` → `arc-cfg-credentials`).
