# Retro Vault — Día 33

## Contexto

Día32 cerrado: ARCADE-SETUP completado (research FBNeo/MAME, `docs/arcade-setup.md`, DAT downloader wired, menú de selección en UI). UX-FIX-1/2/3 corregidos. DESIGN-15 completado (tokenización inline, ~640 instancias, 44 archivos JS/HTML). CI ruff verde.

Pendientes de marcar: NEW-3 y NEW-7 ya estaban implementados — detectado al iniciar esta sesión.

---

## Objetivos del día

### ✅ Housekeeping (hecho al abrir sesión)

- [x] Marcar NEW-3 y NEW-7 como ✅ en backlog (ya estaban implementados)
- [x] Crear Día33.md y archivar Día29/30/32

---

### Bloque 1 — DB-FIX-1 + DB-FIX-2 · ~15 min · una rama + un commit

Archivo único: `src/rom_manager/database/schema.py`

- [ ] **DB-FIX-1**: añadir a `SCHEMA_STATEMENTS`:
  ```sql
  CREATE INDEX IF NOT EXISTS idx_games_md5 ON games (md5)
  ```
- [ ] **DB-FIX-2**: añadir `metadata_scraped INTEGER DEFAULT 0` al bloque
  `CREATE TABLE games (...)` (ya existe solo en la migration, no en el schema declarativo)
- [ ] Verificar que los tests existentes siguen verdes (`pytest tests/ -x -q`)

---

### Bloque 2 — NEW-5: Completitud de colección por plataforma · ~1 h

Sin cambios de schema. Dos pasos: backend → frontend.

#### 2a · Backend · `web/handlers/games.py` (o nuevo `handlers/collection.py`)

- [ ] Endpoint `GET /api/collection-completeness`:
  1. Query: `SELECT platform, COUNT(*) FROM games WHERE file_type='rom' GROUP BY platform`
  2. Para cada plataforma con DAT disponible: `load_dat_files_by_platform(dat_dir, [platform])` → `len(entries)`
  3. Devuelve `[{platform, owned, total, pct}]` ordenado por `pct DESC`
  4. Plataformas sin DAT cargado → `total: null` (se muestran solo como recuento)
- [ ] Registrar la ruta en `server.py`

#### 2b · Frontend · `tab-overview.html` + JS

- [ ] Widget "Colección" en tab Overview debajo del resumen existente:
  - Tabla o lista de barras: plataforma / N propios / N en catálogo / barra de progreso
  - Si `total === null`: mostrar solo el recuento sin barra
  - Llamar al endpoint al cargar el tab (igual que `loadOverview()`)
- [ ] JS: función `loadCollectionCompleteness()` → fetch + render

---

### Bloque 3 — NEW-8: Backup de `.opt` de core · ~30 min

> ⚠️ El `.opt` vive en `RetroArch/config/<CoreName>/<Game>.opt`, **no** en el
> dir de saves. `save_syncer` filtra por `save_extensions`, así que añadir la
> extensión sola no es suficiente. Opción más simple: añadir un `SyncSource`
> de tipo "config dir" con `sync_all=True` apuntando al dir `config/` de RetroArch.

- [ ] Añadir campo `retroarch_config_dir: str = ""` a `AppConfig` (o `SyncConfig`)
- [ ] En Settings: campo de texto "Carpeta config RetroArch" con botón Examinar
  (reutilizar `browseFolder()` existente)
- [ ] En el pipeline de sync: si `retroarch_config_dir` está configurado, añadir
  automáticamente un `SyncSource(name="RetroArch Config", local_dir=..., sync_all=True)`
  antes de llamar al syncer
- [ ] Test: verificar que un `.opt` en el dir config se incluye en la lista de archivos a sincronizar

---

### Bloque 4 — NEW-6: Validador de BIOS · ~2 h

Feature principal del día. Diseño: la carpeta de BIOS se deriva de `retroarch_path`
(ya en config) → `Path(retroarch_path).parent / "system"`. Sin campo `bios_dir`
separado. Se añade `esde_path` para integración con ES-DE.

#### 4a · Tabla estática · `src/rom_manager/utils/bios_validator.py` (nuevo)

- [ ] Dataclass `BiosEntry(filename, system, md5, sha1, required: bool)`
- [ ] Lista `KNOWN_BIOS: list[BiosEntry]` con al menos:
  - PS1: `scph1001.bin`, `scph5501.bin`, `scph7001.bin`
  - PS2: `ps2-0200a-20040614.bin` (BIOS USA)
  - Saturn: `sega_101.bin`, `mpr-17933.bin`
  - GBA: `gba_bios.bin`
  - NDS: `bios7.bin`, `bios9.bin`, `firmware.bin`
  - Mega CD EU/USA/JP: `bios_CD_E.bin`, `bios_CD_U.bin`, `bios_CD_J.bin`
- [ ] Función `check_bios_dir(bios_dir: Path) -> list[BiosResult]`:
  reutiliza hashing existente (`hash_file` en `scanner/`), compara MD5 vs tabla

#### 4b · Config — `esde_path` (nuevo)

- [ ] Añadir `esde_path: str = ""` a `AppConfig`
- [ ] Campo "Ruta ES-DE" en Settings con botón Examinar + botón "Detectar"
  (busca `emulationstation.exe` en `Program Files (x86)`, igual que `_detect_retroarch_install`)
- [ ] **No** añadir `bios_dir`: se deriva automáticamente de `retroarch_path` →
  `Path(retroarch_path).parent / "system"`

#### 4c · Endpoint · `web/handlers/tools.py` (o `esde/`)

- [ ] `GET /api/bios-check`:
  1. Si `retroarch_path` vacío → `{"error": "Configura la ruta de RetroArch en Ajustes"}`
  2. `bios_dir = Path(config.retroarch_path).parent / "system"`
  3. Llama a `check_bios_dir(bios_dir)`, devuelve
     `{bios_dir, entries: [{filename, system, status: "ok"|"missing"|"bad_hash", required}]}`
- [ ] Registrar en `server.py`

#### 4d · UI · `tab-tools.html`

- [ ] Botón "Verificar BIOS" + ruta detectada mostrada como subtítulo
  ("Buscando en C:\RetroArch-Win64\system\")
- [ ] Tabla de resultados:
  - ✅ verde: presente y hash correcto
  - ⚠️ amarillo: presente pero hash no reconocido (versión alternativa válida)
  - ❌ rojo: ausente (`required=True`)
- [ ] Si `retroarch_path` no configurado: banner de aviso con enlace a Ajustes
- [ ] Respuesta síncrona directa (< 1 s para ~15 archivos; sin job/polling)

---

## Archivos a modificar (estimado)

| Bloque | Archivos |
|--------|----------|
| DB-FIX | `database/schema.py` |
| NEW-5 | `web/handlers/games.py` o nuevo `handlers/collection.py`, `tab-overview.html`, `static/js/tabs/overview.js` |
| NEW-8 | `config.py`, `tab-settings.html`, `sync/save_syncer.py` o handler de sync |
| NEW-6 | `utils/bios_validator.py` (nuevo), `config.py`, `web/handlers/tools.py`, `tab-tools.html`, `tab-settings.html` |

---

## Trabajo realizado

- **Housekeeping**: NEW-3, NEW-6, NEW-7 marcados como ✅ (ya estaban implementados desde sesiones anteriores)
- **DB-FIX-1/2**: `idx_games_md5` + `metadata_scraped` añadidos al `CREATE TABLE games` en `schema.py`
- **esde_path (NEW-6 complemento)**: campo en `AppConfig` + Settings + override en `_handle_esde_status()` para que la ruta manual tenga prioridad sobre la auto-detección
- **NEW-5**: `GET /api/collection-completeness` — cruza `games.catalog_source` con DATs en disco; widget con barras de progreso por plataforma en tab Overview
- **NEW-8**: `SyncConfig.ra_config_dir` + `ra_config_remote` — si ambos configurados, el pipeline de sync inyecta automáticamente un `SyncSource(sync_all=True)` para sincronizar archivos `.opt` de RetroArch; dos campos nuevos en Settings → Sync

---

## Archivos modificados

- `src/rom_manager/database/schema.py` — DB-FIX-1/2
- `src/rom_manager/config.py` — `esde_path` en AppConfig, `ra_config_dir`/`ra_config_remote` en SyncConfig
- `src/rom_manager/web/handlers/esde/system.py` — prioridad `esde_path` en auto-detect
- `src/rom_manager/web/handlers/config.py` — guardar `esde_path`, `ra_config_*`
- `src/rom_manager/web/handlers/games.py` — endpoint `/api/collection-completeness`
- `src/rom_manager/web/handlers/sync_cloud.py` — inyección SyncSource ra_config
- `src/rom_manager/web/builders/misc.py` — exponer `esde_path`, `ra_config_*` en `/api/config`
- `src/rom_manager/web/static/partials/tab-settings.html` — campo esde_path + campos ra_config
- `src/rom_manager/web/static/partials/tab-overview.html` — widget completitud
- `src/rom_manager/web/static/js/tabs/config.js` — leer/guardar campos nuevos
- `src/rom_manager/web/static/js/tabs/overview.js` — `loadCollectionCompleteness()`
- `src/rom_manager/web/static/js/main.js` — exportar `loadCollectionCompleteness`
- `Tareas/backlog.md` — NEW-3/6/7 marcados ✅; NEW-5/8 añadidos y completados

---

## Estado al finalizar

Todos los bloques del día completados. 547 tests verdes. Push a `develop`.

Pendientes del backlog para próximas sesiones: DB-FIX-3/4, SYNC-SETUP (wizard rclone), NEW-1 (patch manager), NEW-2/4 (requieren DB-FIX-4).

## Siguiente sesión recomendada

**DB-FIX-3/4** (limpieza de schema + FK saves→games) como bloque técnico, seguido de **SYNC-SETUP** (wizard cloud sin terminal) como feature principal.
