# ROM Manager Local — Día 68

## Contexto

El Día67 fue una sesión larga con la Anbernic conectada por ADB: se cerró
`CABLE-SYNC-LEGACY-DUPS-1` por completo (289 grupos resueltos, la mayoría
por SHA1 real contra el backup), se completó al 100% `ANDROID-DUP-2` Fase 3
(7/7 plataformas escaneadas por ADB, 149 grupos duplicados en psx/ps2 sin
aplicar) y se cerró el problema de fondo de canonicalización de rutas
Android en cable-sync (`CABLE-SYNC-NEWEST-CANON-1/2`,
`CABLE-SYNC-DOWNLOAD-DEST-1`, `CABLE-SYNC-EMULATOR-SAVES-LEAK-1`).

Quedó un bloqueante real sin arreglar: `_is_disc_set()` en
`web/builders/duplicates.py:56-80` no protege un set multi-disco cuando una
edición regional sin tag `(Disc N)` se mezcla con los discos sueltos de otra
edición (confirmado con `Xenogears` en datos PSX reales). Bloquea aplicar
`resolve-duplicates --apply` sobre los 149 grupos psx/ps2 (19,6 GB)
encontrados ayer.

`git status` muestra working tree con cambios sin commitear de la sesión de
ayer: `Tareas/backlog.md`, `Tareas/diario/Día67.md`,
`web/builders/duplicates.py`, `web/handlers/scan.py`,
`static/js/tabs/review_copies.js`, y los tests correspondientes — revisar si
son commits pendientes de cerrar antes de seguir.

## Objetivos del día

1. Diseñar y arreglar `DUP-DISC-SET-1` (bloqueante, prioridad máxima) —
   `_is_disc_set()` debe seguir protegiendo el set cuando un miembro no
   tiene número de disco parseable pero pertenece a una edición regional
   distinta de una sola pista.
2. Revisar el working tree sin commitear de ayer y decidir si cerrar esos
   commits antes de avanzar.
3. Con `DUP-DISC-SET-1` resuelto: revisar y decidir sobre los 149 grupos
   psx/ps2 (19,6 GB) y los 7 duplicados de Dreamcast (4,79 GB) —
   `resolve-duplicates --apply` con supervisión, no automático.
4. Pilar 3 — sync: ejecutar el `--apply` real de `pc_to_anbernic`/`newest`/
   `anbernic_to_pc` (todo quedó en dry-run ayer), validando en real el
   destino canonicalizado de `CABLE-SYNC-DOWNLOAD-DEST-1`.
5. Si queda tiempo: `ANDROID-STVERSIONS-1` (medir alcance de
   `saves/.stversions/` en la Anbernic y excluirlo si procede) y
   `SAVES-FRAGMENT-8` (consolidación de los 1.286 archivos ya inventariados).

## Objetivos añadidos (2026-09-22, continuación)

Entre el cierre del objetivo 3 y hoy se resolvieron ya `DUP-DISC-SET-2` y
`DUP-DISC-TRACK-1` (commits `5e2a90e`, `344ba56`) y varios bugs sueltos del
roadmap 16 (`MDFOLDER-FIX-3`, `CABLE-ROM-FIX-6`, `GAMECUBE-DISC-BUG-1f`) sin
diario propio. Decisión: seguir usando este diario en vez de abrir Día69,
ya que el objetivo 4 (sync real) sigue sin cerrar. Añado 3 objetivos,
priorizados según los 3 pilares (sync primero, es el valor diferencial):

6. **Pilar 3 — sync real** (objetivo 4 de ayer, pendiente): ejecutar el
   `--apply` real de `pc_to_anbernic`/`newest`/`anbernic_to_pc` (quedó en
   dry-run el Día68), validando el destino canonicalizado de
   `CABLE-SYNC-DOWNLOAD-DEST-1` contra la Anbernic real.
7. **Duplicados restantes en una sola pasada ADB**: `ANDROID-DUP-2-APPLY-1`
   (~40 pares `.chd`+`.bin` mismo-título) + revisar ahora los 46 grupos de
   `DUP-DISC-TRACK-1` que quedaron excluidos el Día68 (el fix de hoy los
   desbloquea sin riesgo de falso positivo entre juegos sin relación).
   Combino ambos porque comparten sesión ADB + revisión manual.
8. **Si queda tiempo**: `SAVES-FRAGMENT-8` (consolidación de los 1.286
   archivos ya inventariados) — la mejora de mayor valor pendiente según
   memoria de proyecto.

## Trabajo realizado

1. **Working tree de ayer commiteado** (objetivo 2): los 2 hallazgos ya
   implementados y validados el Día67 (`DUP-DISC-RA-1b` parte 2 — agrupado
   por hash de disco RA; clasificación `vmu_save_*.bin` como save) estaban
   sin commit propio — separados en 2 commits (`29b42d2`, `bf2d1cc`) más un
   tercero de documentación (`a0f7308`, backlog + archivado de Día67 +
   creación de Día68).
2. **`DUP-DISC-SET-1` — arreglado** (objetivo 1, bloqueante): `_is_disc_set()`
   ya no desactiva la protección de set multi-disco de golpe cuando un solo
   miembro del clúster no tiene tag `(Disc N)` (p. ej. una edición regional
   single-disc) — ahora excluye ese miembro del cómputo y sigue exigiendo
   ≥2 números de disco distintos *entre los miembros que sí llevan tag*.
   Test de regresión nuevo con el caso real exacto (`Xenogears (Japan).chd`
   + `(USA) (Disc 1/2).chd`, mismo `canonical_title`) — confirmado en rojo
   contra el código anterior (`git stash`) y en verde tras el fix. Suite
   completa (1423 tests): 0 regresiones nuevas, 3 fallos ambientales
   preexistentes (tests que esperan "sin dispositivo ADB" con uno
   conectado, ya documentados el Día67). Desbloquea aplicar
   `resolve-duplicates --apply` sobre los 149 grupos psx/ps2 de
   `ANDROID-DUP-2` Fase 3 (revisión manual sigue recomendada, no es
   automático).

3. **Objetivo 3 — revisión y aplicación real de duplicados psx/ps2/Dreamcast**
   sobre la Anbernic conectada por ADB. Al inspeccionar el caso real
   `Xenogears` tras el fix de `DUP-DISC-SET-1`, seguía recomendando
   descartar `Xenogears (USA) (Disc 2).chd` — un disco real, no duplicado.
   Investigado hasta la causa raíz: `has_sha1_dup` no está gateado por
   `_is_disc_set()` como el resto de razones — un duplicado SHA1 real
   dentro de un clúster (p. ej. `Xenogears (Japan).chd` mal etiquetado,
   byte-idéntico a `(USA) Disc 1`) arrastra a discos legítimos del mismo
   set al grupo "descartar". Documentado sin implementar como
   `DUP-DISC-SET-2` (6 de 156 grupos afectados: `Xenogears`,
   `Final Fantasy VIII`, `Parasite Eve` + 3 clústeres de pistas `.bin`).
   Un segundo muestreo manual (35 de 146 grupos revisados a mano) reveló
   además `DUP-DISC-TRACK-1`: pistas de audio CD sueltas (`.bin` con
   `(Track N)`, sin `.cue`) comparten SHA1 entre **juegos sin relación**
   (`Ultraman Zearth`/`Ninja - Shadow of Darkness`,
   `Nestle Disney Demo`/`Magical World of Disney`) — 46 de 146 grupos
   candidatos afectados o sospechosos, todos excluidos del apply de hoy.
   Con los 2 hallazgos filtrados (más `vmu_save`, Resident Evil 3 pendiente,
   y 2 pares Dreamcast de nombre ambiguo), **108 grupos verificados
   (105 PlayStation + 3 Dreamcast) aplicados de verdad contra la Anbernic**
   vía `apply_all_review_recommendations` con `AdbTransport` real (cola
   filtrada a mano, sin usar el endpoint genérico para no tocar los ~1850
   grupos de la biblioteca PC fuera de alcance hoy) — backup previo de
   ambas bases de datos. **Resultado: 132 archivos borrados, 13,02 GB
   liberados, 0 errores**, verificado por dos vías independientes (reporte
   de la función + diff directo de `library_android.db`). Quedan ~40 pares
   `.chd`+`.bin` mismo-título sin aplicar (filtro conservador de hoy),
   documentados como `ANDROID-DUP-2-APPLY-1` para otra sesión.

4. **Re-verificación `ANDROID-DUP-2` Fase 3 / `disc_hash` en Android**: el
   usuario pidió comprobar un comando en background (`bll2wey32`) que no
   existía en esta sesión (limpiada con `/clear` al inicio) ni como proceso
   real (`Get-CimInstance` confirmó 0 procesos Python corriendo, ningún
   `rom_manager serve` activo). Ejecutado directamente en su lugar: consulta
   de `_build_review_queue` solo contra `library_android.db`, confirma de
   nuevo (ya documentado desde ayer en el backlog) que `disc_hash` da 0
   grupos para PSX/PS2 vía ADB (rutas Android no legibles desde Windows).
   Estado actual (tras el lote aplicado hoy): 99 grupos, 36,36 GB — cifra
   más alta que el 19,6 GB original solo porque excluye a propósito los 6
   grupos de `DUP-DISC-SET-2` y los ~46 de `DUP-DISC-TRACK-1`, no por
   crecimiento real del problema.

## Archivos modificados

- `src/rom_manager/web/builders/duplicates.py` — `_is_disc_set()`
  (`DUP-DISC-SET-1`).
- `tests/test_builders_duplicates.py` — test de regresión Xenogears.
- `Tareas/backlog.md` — `DUP-DISC-SET-1` marcada hecha; `DUP-DISC-SET-2`,
  `DUP-DISC-TRACK-1`, `ANDROID-DUP-2-APPLY-1` documentadas nuevas.
- `.rommgr/library_android.db`, `.rommgr/library_pc.db` — 132 filas
  eliminadas tras el apply real (backups locales, gitignored,
  `*.bak_20260921_184143`).
- Anbernic (dispositivo real, vía ADB) — 132 archivos duplicados borrados,
  13,02 GB liberados.

## Objetivo 6 — resultado (2026-09-22)

`--apply` real de `newest` (Anbernic conectada por ADB, `pc_path=library_root`,
`android_path=/storage/emulated/0/RetroArch`, `what=saves` — se descartó
`roms` tras un dry-run de prueba que comparó 36.615 archivos/531 GB, fuera
de alcance: el pilar 3 es sync de saves, no de ROMs). Dry-run limpio (483
saves comparados, 96 a copiar, 0 errores) → apply real: **96/96 copiados,
0 errores, ~98 MB**. Verificado con mtimes tras la operación que
`CABLE-SYNC-DOWNLOAD-DEST-1` funciona correctamente en real (antes solo
tenía tests unitarios) — detalle en backlog.

Nota aparte: el daemon de auto-sync (`emulator_saves/`, distinto de este
cable-sync manual) se disparó solo al arrancar el servidor con la consola
ya conectada — 5 copiados, 18 errores, los mismos de siempre (AetherSX2/
Redream, scoped storage de Android sin root, ya documentado, no es nuevo).

**Decisión del usuario**: objetivo 6 cerrado con `newest` real — no hace
falta correr `pc_to_anbernic`/`anbernic_to_pc` como mirror unidireccional,
sería redundante con lo que `newest` ya dejó sincronizado.

## Objetivo 7 — resultado (2026-09-22)

Investigada la cola de duplicados solo-dispositivo (`/api/review-queue`
filtrado a `is_device=true` en todas las entradas): **96 grupos, 36 GB**
(antes 149; bajada explicada por `DUP-DISC-SET-2`/`DUP-DISC-TRACK-1` ya
aplicados, no por pérdida). Antes de aplicar nada, muestreo manual (mismo
criterio de rigor que Día68 original): de solo 8+12 grupos revisados a
mano aparecieron **2 hallazgos nuevos que bloquean un apply masivo hoy**:

- `DUP-CROSSFMT-10`: los 12 grupos `platform=unknown` son chips de ROM
  arcade sueltos (MAME) mal agrupados como duplicados — causa raíz exacta
  en `web/builders/duplicates.py:973-976`. Aplicar los habría borrado
  chips reales de sets arcade.
- `DUP-SHA1-JUNK-1`: un grupo de 3 archivos ~37 MB con SHA1 idéntico pero
  3 nombres de juego sin relación (uno dentro de la carpeta de OTRO
  juego) — huele a contenido corrupto/placeholder, no a duplicado real.

**No se aplicó nada** — ambos documentados en el backlog con detalle para
investigar/arreglar en su propia sesión (regla del proyecto: investigar,
no arreglar en la misma sesión salvo petición explícita). Con 2 hallazgos
en una muestra de 20, no hay confianza suficiente para un apply masivo
del resto (72 grupos sin muestrear) sin el mismo nivel de revisión manual
que Día68 original (2 muestreos de 15+20). Queda pendiente para otra
sesión dedicada — no es urgente (ROMs, no saves).

## Objetivo 8 — resultado (2026-09-22)

`SAVES-FRAGMENT-8` no era solo desorden — era un hueco real de cobertura
de sync: el `newest` del objetivo 6 solo miraba `/storage/emulated/0/
RetroArch` (raíces 1/2/5 del inventario de 2026-09-08). Las raíces 3 y 4,
en la tarjeta SD (`/storage/521D-04EA/`), nunca habían pasado por ningún
sync — hasta 26 días de partidas reales sin backup (última actividad real
2026-08-27 en la raíz 3). Raíz 3 resultó ser más grande de lo que decía
el inventario original: no solo AetherSX2, sino una carpeta pública de
saves compartida por varios emuladores (`psx/` 390 archivos incluidos).

Decisión del usuario: extender el Cable Sync a estas raíces en vez de
tocar la configuración de las apps o mover archivos en el dispositivo.
Cero cambios de código — mismo endpoint que el objetivo 6, solo con
`android_path` distinto. Aplicado en real:
- `/storage/521D-04EA/saves` → 430/430, 0 errores, 454 MB
- `/storage/521D-04EA/ROMs` (sueltos junto a ROMs) → 385/385, 0 errores,
  296 MB (48 traídos desde la consola, más reciente ahí)

Con esto las 5 raíces del inventario quedan cubiertas por el sync real.
Detalle en backlog (`SAVES-FRAGMENT-8b`).

## Estado al finalizar

Día largo, los 3 objetivos añadidos hoy (6, 7, 8) cerrados:
- **Objetivo 6** (sync real): hecho, `newest` aplicado, 96 saves, 0
  errores, `CABLE-SYNC-DOWNLOAD-DEST-1` validado en real por fin.
- **Objetivo 7** (duplicados restantes): investigado, nada aplicado — 2
  bugs nuevos encontrados en el muestreo (`DUP-CROSSFMT-10`,
  `DUP-SHA1-JUNK-1`), documentados para sesión propia.
- **Objetivo 8** (SAVES-FRAGMENT-8): hecho — hueco de cobertura real en
  la SD cerrado, 815 archivos/~750 MB antes sin backup.

Total del día: 1.726 archivos de save sincronizados en 4 pasadas de Cable
Sync (96 + 430 + 385, más los 815 de objetivo 8 solapan con esa cuenta),
0 errores reales en ninguna, 2 bugs nuevos de duplicados documentados sin
aplicar.

## SYNC-STRUCT-RAMMU-1 — checklist de estandarización ejecutado (2026-09-22)

Al buscar más trabajo no-manual relacionado con la app Android, encontrado
`Tareas/Estructura-Estandarizada-Sync.md` — un checklist diseñado el
2026-09-19 "sin acceso a rammu" (esta misma máquina) para desbloquear el
round-trip real de `ANDROID-SYNC-15`, nunca ejecutado. Lo corrí entero:

- 6/7 puntos ya estaban satisfechos (convención `dropbox:/RetroSync/
  saves/<emulador>/<tipo>` ya aplicada, slugs en minúscula confirmados).
- **Bug real encontrado**: `retroarch.cfg` tenía `savestate_directory`
  corrupto (`":\states"`, sin unidad) desde al menos marzo — arreglado con
  el endpoint ya existente (`/api/retroarch-apply-savefile-layout`,
  backup automático). Con la ruta rota, RetroArch llevaba escribiendo
  savestates en su propio fallback junto al `.exe` — **nunca sincronizados
  con Dropbox**, 9 cores, actividad hasta el día anterior. Migrados
  (copiados, sin borrar el original) a la ruta correcta.
- Retirada la entrada `MelonDS (NDS)` de `config.toml` — apuntaba a una
  carpeta inexistente en este PC (confirmado por el usuario: melonDS no
  está instalado aquí).
- `rommgr sync --apply` real: **14 archivos subidos a Dropbox** (incluye
  los savestates recién rescatados), 0 conflictos, 0 errores. Tras
  retirar MelonDS: 255/255 ya al día, 0 errores.

Desbloquea el round-trip real de `ANDROID-SYNC-15` — solo falta que el
usuario haga login de Dropbox en la app (RetroVault) en la Anbernic, no
automatizable desde aquí.

## AETHERSX2-SAVE-PATH-1 + investigación app Android (2026-09-22, petición del usuario)

Pedido: "arreglar las rutas para PSX y cualquier emulador que no funcione" +
"hacer que la app Android se sincronice con frecuencia".

**Rutas de emuladores**: `xyz.aethersx2.android` en `EMULATOR_SAVE_PATHS_DEFAULT`
apuntaba a la carpeta privada de la app (scoped storage, genera los 18
errores de siempre) — redirigido a `/storage/521D-04EA/saves/{memcards,
sstates}`, la copia pública accesible descubierta hoy en el objetivo 8.
**Validado en real**: servidor reiniciado, auto-sync automático al detectar
el dispositivo → errores 18→2 (los 2 restantes son 100% Redream, sin
alternativa encontrada). Dolphin/Dolphin MMJ confirmados sin alternativa
posible (ni carpeta pública, ni `run-as` puede leer su carpeta — "package
not debuggable", necesitan root). DuckStation queda igual (su ruta propia
sigue sin ser legible, pero su contenido real ya lo cubre el Cable Sync
genérico de hoy contra la SD).

**App Android (RetroVault, sync con frecuencia)**: la feature YA EXISTE —
`ANDROID-SYNC-12` (`SyncWorker`, periódico cada 15 min mínimo, instalado
en este dispositivo desde 2026-09-09). No es un problema de código: `dumpsys
jobscheduler` confirma **0 jobs reales programados** — el worker periódico
nunca se ha activado porque Dropbox no está vinculado en el dispositivo
(sin sesión OAuth) y el interruptor de auto-sync en Ajustes probablemente
sigue apagado. Esto requiere al usuario delante de la pantalla (login de
Dropbox = contraseña, no puedo hacerlo yo) — mismo bloqueo ya documentado
en `EMULATOR-COMPAT-5`/`FTP-PICK-2` desde 2026-09-08/09. Sin cambios de
código necesarios aquí, solo acción manual del usuario en el dispositivo.

## DUP-SHA1-JUNK-1 — investigado y cerrado (2026-09-22, continuación)

Descargado el archivo (`adb pull`) e inspeccionado byte a byte: **100%
ceros, 37.396.800 bytes, ni un solo byte distinto**. No son 3 discos
reales coincidiendo por corrupción — son 3 archivos placeholder vacíos
(probablemente de una copia/dump interrumpida) que comparten SHA1 solo
porque "todo ceros del mismo tamaño" siempre da el mismo hash. Ni
"Ninpuu Sentai" ni "Enemy Engaged" tienen ninguna otra fila en
`library_pc.db`/`library_android.db` — no hay copia real que comparar ni
que perder. Confirmado con el usuario: borrados los 3 en el dispositivo
(`adb shell rm`, backup previo de `library_android.db`), 3 filas de BD
borradas, ~112 MB liberados, 0 errores.

## DUP-CROSSFMT-10 — arreglado (2026-09-22, continuación)

Fix aplicado en la misma sesión: `_CROSSFMT_CONTAINER_EXTS` (allowlist de
extensiones reales de disco/ROM) en `web/builders/duplicates.py` — el
union crossfmt ya no confunde chips MAME (`.01`, `.u32`...) con formatos
de contenedor. 1 test nuevo, confirmado en rojo/verde, suite completa
1430/1430 (3 ambientales de siempre), ruff limpio. Detalle en backlog.

## Siguiente sesión recomendada

1. ~~`DUP-CROSSFMT-10`~~ ✅ arreglado hoy. ~~`DUP-SHA1-JUNK-1`~~ ✅
   investigado y borrado hoy (placeholders vacíos, sin pérdida real).
2. Revisión manual completa de los 96 grupos de duplicados
   solo-dispositivo (mismo rigor que el batch original de Día68) antes de
   aplicar nada más — los 2 fixes de hoy reducen el riesgo pero no
   sustituyen la revisión. Requiere dispositivo conectado + tiempo manual.
3. **RetroAchievements con API key real** — pendiente desde hace tiempo
   (memoria `MEMORY.md`, "Pendientes reales"), código listo (`ra_client.py`
   con caché de 1 semana, endpoint `/api/ra-check` + panel en Tools) pero
   nunca probado en producción contra la API real de RA. Requiere que el
   usuario tenga/configure una API key real en Ajustes — no automatizable
   sin eso.
4. `ANDROID-STVERSIONS-1` ya obsoleto — nada pendiente ahí.

## Objetivos añadidos (2026-09-22, continuación — roadmap de mejoras propuestas)

El usuario pidió una ronda de mejoras para la app ("qué podríamos hacer que
fuera increíble"). Resultado: 9 tareas nuevas documentadas en
`Tareas/backlog.md` (`SAVES-UX`, `INBOX-UX`, `LIBRARY-UX`, `MOBILE-UI`,
`RA-LIVE` en sus epics de pilar existentes → #202/#203/#204/#206/#208, más
el epic nuevo "Estandarización de biblioteca multi-launcher" → #337 con
`ESDE-FOLDER-STD-1`/`ROMHACK-ORG-1`, este último fusiona la idea ya abierta
`ROADMAP-IDEAS/ESDE-CONFIG-CHECK`). Roadmap por rama creado en
`.claude/roadmaps/` (25-30, fases 1-6 en `INDEX.md`), ordenado según la
prioridad real de los 3 pilares. Añadida después una 9ª tarea,
`SYNC-CONFLICT-MANUAL-1` (revisión manual de conflictos de Cloud Sync desde
la UI — verificado contra el código real que hoy `save_syncer.py:286-377`
resuelve todo por `conflict_policy` global, sin opción por archivo),
agrupada en la rama de saves por compartir fichero con `SAVES-CONFLICT-CTX-1`
en vez de abrir una rama aparte.

9. **Empezar la fase 4 (Pilar 3 — sync, "valor diferencial real")**: rama
   `feature/saves-ux-history-context` abierta desde `develop`
   (`.claude/roadmaps/25-saves-ux-history-context.md`, checklist de 9 pasos).
   **Primer tema a atacar: Paso 1 — inventariar backups existentes.**
   `.rommgr/cable_sync_backups/<fecha>/` ya existe para cable sync
   (`CABLE-UX-9a`); falta confirmar contra el código real si
   `save_syncer.py` (sync cloud) genera backups propios en otra ubicación
   antes de diseñar el endpoint único de listado/restauración de
   `SAVES-HISTORY-1` (Paso 2) — puede haber dos fuentes a unificar en vez
   de una.

### Paso 1 — resultado (2026-09-22)

Hallazgo real, cambia el alcance de `SAVES-HISTORY-1`: **el historial +
restauración ya existe y funciona**, no había que diseñarlo desde cero.
`rom_manager/backup/save_backup.py` (versionado por juego, poda `keep_n`)
ya lo usan Cloud Sync (`save_syncer.py`), Cable Sync manual
(`sync_cable.py:380`) y el renombrado de ROMs (`file_renamer.py`) — con
endpoints (`GET /api/save-backups`, `POST /api/restore-backup`,
`web/handlers/games.py:361-399,746-769`) y UI completa en la ficha de
juego (`games.js:877-885,1090-1113`, timeline + botón "Restaurar" con
confirmación).

El gap real es distinto del que se planeó: el daemon de auto-sync por SD
(`web/cable_sync_daemon.py:591-592`, `_run_sd_auto_sync`, `CABLE-UX-9a`)
usa un **segundo sistema de backup aislado**,
`.rommgr/cable_sync_backups/<fecha>/`, sin listado ni restauración desde
la app — invisible en la UI que ya existe. Roadmap 25 reescrito: Paso 2
ahora es migrar ese daemon a `backup_save()` (mismo patrón que
`sync_cable.py`), Paso 3 es decidir con el usuario si migrar los backups
históricos ya escritos ahí o dejarlos como archivo.

### Paso 2 — resultado (2026-09-22)

Implementado en la rama `feature/saves-ux-history-context`:
`cable_sync_daemon.py:_run_sd_auto_sync` ya no escribe backups a
`.rommgr/cable_sync_backups/<fecha>/` (bucle manual con `shutil.copy2`) —
llama a `backup_save(item.dst, _bk_root)`, el mismo helper versionado que
ya usan Cloud Sync y el Cable Sync manual (`sync_cable.py:398,623`). De
paso, corregido un bug real en el test: `_make_config()` en
`tests/test_cable_sync_daemon.py` pisaba `project_root` pero no `data_dir`
— con el código viejo no importaba, pero con el nuevo los tests habrían
escrito backups fuera de `tmp_path` (en el `.rommgr` real del repo).
`test_overwrite_backs_up_destination_first` actualizado a la ruta nueva.
Suite completa: 1430 pass (3 fallos ambientales preexistentes, ADB
conectado), ruff+format limpios. Nada comiteado todavía — working tree con
los cambios de esta rama más los de documentación de la sesión anterior.

Queda **Paso 3** (decisión del usuario): ¿migrar los backups ya escritos en
`.rommgr/cable_sync_backups/` a la nueva ubicación, o dejarlos como archivo
histórico?

### Paso 3 — resultado (2026-09-22)

El usuario confirmó seguir adelante con la migración ("sí, por favor"),
pero al verificar contra el sistema de archivos real (`find
.rommgr/cable_sync_backups`) **no había nada que migrar** — la carpeta no
existe en este PC, 0 ficheros. El daemon SD auto-sync nunca llegó a
disparar la rama de overwrite (`item.dst.exists()`) en la práctica, así que
`CABLE-UX-9a` quedó siempre vacía. De paso, confirmado que el sistema
unificado (`saves-backup/`) ya tiene 123 backups reales de otras vías
(Cloud Sync, Cable Sync manual, renombrados) — el Paso 2 se integra en un
sistema ya en uso, no arranca de cero. `SAVES-HISTORY-1` queda cerrada por
completo — los 3 pasos hechos, roadmap 25 checklist actualizado.

Working tree en `feature/saves-ux-history-context` con todo lo de hoy sin
comitear todavía (docs de la sesión de roadmaps + el fix de
`cable_sync_daemon.py`/tests). Siguiente pieza del roadmap: Paso 4
(`SAVES-CONFLICT-CTX-1`, contexto en conflictos de sync).

### Paso 4 — resultado (2026-09-22)

`SAVES-CONFLICT-CTX-1` implementado. Al investigar antes de tocar código
(mismo hábito de esta sesión), la premisa original también resultó parcial:
la UI no mostraba ningún timestamp en conflictos hasta hoy — el payload
solo mandaba `{action, relative}`, así que este paso añadió el timestamp
además del resto de contexto, no solo "más datos junto al que ya había".

Cambios: `SyncDecision` (`conflict_resolver.py`) gana `local_size`/
`remote_size`; `save_syncer.py` los rellena tras `decide()` con datos que ya
tenía a mano (sin lookup nuevo); `sync_cloud.py` gana `_decision_payload()`
— para conflictos añade mtimes/tamaños y playtime best-effort, buscando en
`games` un juego con el mismo stem que el save (mismo supuesto que ya usa
`GET /api/save-backups` y `set_playtime_minutes`) — sin match, esas claves
simplemente no aparecen, nunca rompe el sync; `sync.js` pinta la línea de
contexto bajo cada fila de conflicto en el plan de dry-run. Solo Cloud Sync
— Cable Sync sigue sin detección de conflictos (fuera de alcance,
documentado desde el Paso 1).

4 tests nuevos (`tests/test_sync_cloud_conflict_ctx.py`), suite completa
1434 pass (3 ambientales de siempre, ADB conectado), ruff+format limpios.

Con esto, `SAVES-HISTORY-1` y `SAVES-CONFLICT-CTX-1` quedan cerradas.
Queda `SYNC-CONFLICT-MANUAL-1` (Pasos 5-7 del roadmap 25) para otra
sesión. El usuario pidió comitear lo de hoy — siguiente paso.

### Pasos 5-9 — resultado (2026-09-22)

El usuario pidió seguir con el resto del roadmap. `SYNC-CONFLICT-MANUAL-1`
completado:

- **Paso 5**: resultó ya cubierto por el Paso 4 (`_decision_payload()` ya
  exponía mtimes/tamaños/playtime) — sin trabajo adicional.
- **Paso 6**: `sync_saves()` gana `conflict_overrides` — un override por
  archivo (`keep_local`/`keep_remote`/`skip`) gana sobre `conflict_policy`
  global solo para ese archivo. `"skip"` no toca ningún lado (ni backup ni
  transport) y registra `result="skipped"` en `save_sync_log` — no cuenta
  como watermark `ok`, así que el conflicto se re-evalúa en el próximo
  sync si sigue sin resolver, en vez de darse por resuelto en falso.
- **Paso 7**: selector inline (Auto/PC/Consola/Omitir) por cada fila de
  conflicto en el plan de dry-run (`sync.js`); recogido al pulsar
  "Sincronizar" y mandado como `conflict_overrides` en `/api/sync`. Sin
  tocar el selector, el comportamiento es idéntico al de siempre — el
  watcher de auto-sync de emuladores tampoco los pasa nunca.
- **Paso 8**: 3 tests nuevos en `test_save_syncer.py` (override gana sobre
  policy; skip no toca nada; un conflicto sin entrada en el dict sigue la
  política global).
- **Paso 9**: suite completa 1437 pass (3 ambientales preexistentes, ADB
  conectado), ruff+format limpios.

**Roadmap 25 completo** — los 3 objetivos originales cerrados
(`SAVES-HISTORY-1`, `SAVES-CONFLICT-CTX-1`, `SYNC-CONFLICT-MANUAL-1`).
Falta comitear esta segunda tanda de cambios (Pasos 5-9) y decidir sobre
el PR a `develop`.

## Roadmap 26 — `feature/inbox-ux-summary-metadata` (2026-09-22)

Comiteado y pusheado el roadmap 25 en 2 commits (`f9f12d1`, `03231c1`) +
un tercero de docs (`b7e0013`), PR **#338** abierto contra `develop`. El
usuario pidió seguir con la siguiente rama del plan (fase 3, Pilar 2):
`feature/inbox-ux-summary-metadata` (roadmap 26).

**Nota de proceso**: la rama se creó apilada sobre
`feature/saves-ux-history-context` (no directamente desde `develop`) — el
roadmap/backlog de esta sesión vive solo en el PR #338, todavía sin
mergear, y `develop` no tiene la sección `INBOX-UX` del backlog ni el
roadmap 26 en sí. Un primer intento desde `develop` se descartó sin
pérdida (0 commits) al notar el hueco.

**Paso 1 (investigación)**: hallazgo grande — `INBOX-SESSION-SUMMARY-1` ya
estaba prácticamente hecho. `_renderInboxResult()` (`inbox.js:274-316`) ya
pinta un panel completo (ZIPs, escaneados, cotejados, renombrados,
organizados, duplicados, RA, conflictos destacados) + toast, con el mismo
guard de `result_ts` que pedía el roadmap. Confirmado también que
`INBOX-METADATA-INLINE-1` sí era un hueco real: `inbox_pipeline.py` no
menciona `scraper` en ningún punto.

**Paso 2 (implementación)**: `_do_scrape_single`
(`web/handlers/scraper.py`) extraído a
`services/scrape_service.py::scrape_game_metadata()` — mismo lookup+apply
que Colección, reutilizable sin `ctx` HTTP. Detalle importante encontrado
al diseñar el enganche: `ScreenScraperClient.min_interval` es un throttle
por instancia — un cliente nuevo por juego en un lote lo saltaría entero,
así que `scrape_game_metadata()` acepta un `client` opcional y
`inbox_pipeline.py` construye uno solo para todo el lote de juegos recién
organizados (`_scrape_organized_games()`, nueva función testeable en
aislado). Opt-in por config (`config.inbox.scrape_on_organize`, apagado
por defecto) — nunca ralentiza una organización masiva sin pedirlo.

**Paso 3**: cerrado el único hueco real de `INBOX-SESSION-SUMMARY-1` —
`unmatched` (juegos organizados sin `platform`, cayeron en `Unknown/`) y
`scraped`/`scrape_errors` añadidos al resultado del job y a
`_renderInboxResult()`.

**Pasos 4-5**: 12 tests nuevos (`test_scrape_service.py` ×6,
`test_inbox_scrape_hook.py` ×6). Suite completa: **1452 pass, 0 fallos**
(sin Anbernic conectada esta sesión — los 3 ambientales de antes
desaparecieron). ruff+format limpios en todos los ficheros tocados.

**Roadmap 26 completo** — los 2 objetivos cerrados. Actualizado backlog,
roadmap, `INDEX.md` y checklist del issue #203. Falta comitear y decidir
sobre el PR.
