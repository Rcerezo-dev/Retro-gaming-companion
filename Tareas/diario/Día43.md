# ROM Manager Local — Día 43

## Contexto

Día42 cerró con AUD-1…6 y DEVSEL-FIX-1…4 validados con la consola (RG556) y
mergeados a `develop`. La release quedaba como siguiente paso, pero hoy se
priorizó primero pasar `/revisar` sobre todo `src/rom_manager/` (26.9k
líneas) antes de publicar — 6 agentes en paralelo por área (sync, database,
web/handlers core, web/server+builders, detection/scanner/catalog/patch/
converters/renamer, retroachievements/scraper/utils/cli). Resultado: 52
hallazgos documentados en `Tareas/backlog.md` §REV43, ninguno corregido
todavía (regla "investigar antes de arreglar" — cada fix va en su propia
rama). Publicar la release con REV43-1…8 abiertos no tiene sentido: tocan
directamente el pilar de sync de saves.

## Objetivos del día

### Cerrar los hallazgos de mayor riesgo primero (REV43, backlog)

Cada item se aborda en su propia rama, subdividido en pasos que se pueden
cerrar y commitear de forma independiente (patrón INBOX-FIX-*/ZIP-ROUTE-FIX-*).

Pérdida de datos / sync (Pilar 3 — prioridad absoluta):

- [x] **REV43-1** — `AdbTransport.push` sin staging ni backup
  (`sync/adb_transport.py:245-279`)
  - [x] 1a. Subir a `<dst>.part` en vez de escribir directo sobre `android_dst`
    (mismo patrón que ya usa `pull()`)
  - [x] 1b. Verificar MD5 del `.part` antes de comprometerlo; si coincide,
    mover/renombrar sobre el destino final (`mv -f` en el dispositivo)
  - [x] 1c. Si el MD5 no coincide: borrar solo el `.part`, **nunca** el
    destino original; devolver error explícito al caller
  - [x] 1d. Tests: `test_push_corruption_never_touches_final_destination` +
    `test_push_corruption_leaves_existing_remote_intact` (nuevo, cubre el
    caso real de pérdida de datos)
- [x] **REV43-2** — Ninguna ruta de sync por cable/ADB llama a
  `backup_save()` (`sync/cable_engine.py:101-144`)
  - [x] 2a-2b. Backup a nivel de caller en `web/handlers/sync_cable.py` (no
    en `cable_engine.py` — el motor es puro por diseño; se sigue el mismo
    patrón ya usado por el SD-auto daemon, CABLE-UX-9a): `backup_save()`
    antes de sobrescribir un save existente, tanto en modo filesystem
    (`_apply_copy`) como en pull ADB (`_adb_copy_to_pc`)
  - [x] 2c. Push ADB (PC→dispositivo) **fuera de alcance a propósito** —
    respaldar el lado remoto exigiría un `adb pull` extra solo para
    respaldar (coste doble de transferencia); REV43-1 ya evita que un push
    corrupto destruya el destino. `# ponytail: sin backup remoto en push,
    añadir si aparece un incidente real de sobrescritura por dirección
    equivocada`
  - [x] 2d. Test nuevo:
    `test_overwrite_backs_up_existing_save_when_enabled` — overwrite por
    cable deja backup en `data_dir/saves-backup/` antes de sobrescribir
- [x] **REV43-3** — `UnboundLocalError` en `sync_saves()` si el primer
  archivo falla (`sync/save_syncer.py:190,242`)
  - [x] 3a. `remote_path` se calcula ahora al principio de cada iteración
    (antes de cualquier `transport.upload/download`), no dentro del `try`
    tras la llamada — ya no puede quedar sin definir
  - [x] 3b. El log de auditoría ya no arrastra la ruta de una iteración
    anterior (se eliminaron las 3 asignaciones duplicadas/tardías)
  - [x] 3c. Test nuevo: `test_upload_failure_on_first_file_does_not_raise_unbound`
- [x] **REV43-4** — Comparación `st_mtime` sin tolerancia de clock-skew en
  modo "newest" (`sync/cable_engine.py:60-85`)
  - [x] 4a. `cable_engine.plan_direction()` acepta `tolerance_seconds`
    (default `DEFAULT_MTIME_TOLERANCE_S = 2`, mismo valor por defecto que
    `conflict_resolver.decide()`)
  - [x] 4b. Diferencia dentro de tolerancia → sin cambios (mismo camino que
    mtimes iguales, ya existía)
  - [x] 4c. Tests nuevos: `test_plan_newest_skips_mtimes_within_tolerance` +
    `test_plan_newest_picks_side_beyond_tolerance`
  - [x] 4d. (hallazgo extra durante la implementación) El modo ADB "newest"
    en `web/handlers/sync_cable.py:623-650` es una **segunda
    implementación independiente** de la misma comparación, sin pasar por
    `cable_engine.plan_direction` — tenía el mismo bug por separado. Se
    aplicó la misma tolerancia ahí también (`cable_engine.DEFAULT_MTIME_TOLERANCE_S`).
    Unificar ambas implementaciones queda pendiente como limpieza futura
    (no bloqueante, ya no hay riesgo de datos)
- [x] **REV43-5** — Migración a dos BDs borra la fila origen aunque el
  upsert al destino falle (`web/handlers/sync_cloud.py:602-657`)
  - [x] 5a-5b. `migrated_paths`/`migrated_save_paths` trackean qué filas
    tuvieron éxito en `repo_android`; el `DELETE` en el repo PC solo cubre
    esas rutas, no todas las filas "Android"
  - [x] 5c. Las filas fallidas quedan en `errors` (igual que antes) y ya
    NO se borran del origen — reintentable en la siguiente migración
  - [x] 5d. Test nuevo: `test_failed_upsert_keeps_source_row`
    (`tests/test_migrate_split_db.py`) — fila con upsert forzado a fallar
    sigue en el repo PC y no aparece en el repo Android
- [x] **REV43-6** — `dry_run` ignorado en modo ADB con `skip_sha1_dups`
  (`web/handlers/sync_cable.py:554`)
  - [x] 6a-6b. Decisión: en dry-run el chequeo SHA1 no se evalúa (exigiría
    pull real para hashear) — se cuenta como "se copiaría" sin transferir
    nada, igual que el resto de rutas de previsualización del archivo
  - [x] 6c. Test nuevo: `test_dry_run_skips_real_pull_even_with_sha1_dedup`
    (`tests/test_sync_cable_adb_dryrun.py`) — `transport.pull` mockeado para
    lanzar `AssertionError` si se llama; el test pasa sin dispararlo
- [x] **REV43-7** — Sync filesystem no valida que `pc_root`/`ab_root`
  existan (`web/handlers/sync_cable.py:638`)
  - [x] 7a-7b. `raise OSError(...)` explícito si `pc_root` o `ab_root` no
    existen (mensaje accionable: "¿la SD está insertada/montada?"),
    capturado por el `except Exception` existente de `run()` → `job_result`
    con `error`
  - [x] 7c. Tests nuevos: `test_missing_anbernic_path_errors_instead_of_silent_noop`
    + `test_missing_pc_path_errors_instead_of_silent_noop`
- [x] **REV43-8** — `_do_sync` (cloud) sin `result_ts` — wizard nunca deja
  de hacer polling (`web/handlers/sync_cloud.py:497`)
  - [x] 8a. `result_ts` añadido a los 3 `job_result` de `_do_sync` (éxito,
    "sin fuentes configuradas", except genérico) con `_utc_now_str()`, ya
    importado en el archivo
  - [x] 8b. Test nuevo: `test_no_sources_configured_result_has_result_ts`
    (smoke manual del wizard queda pendiente — requiere rclone real)

Integridad de base de datos:

- [x] **REV43-9** — `PRAGMA foreign_keys` nunca activado
  (`database/repositories/base.py`)
  - [x] 9a-9b. Chequeo de huérfanos en la BD real (`.rommgr/library_pc.db`):
    4183/7943 filas de `file_operations` y 8276/20895 de `game_metadata` ya
    huérfanas (normal: `PRAGMA foreign_keys=ON` no valida datos existentes,
    solo escrituras futuras — no bloquea activar el enforcement)
  - [x] 9c. `PRAGMA foreign_keys=ON` añadido en `_open_conn()`
    (`database/repositories/base.py`)
  - [x] Hallazgo extra (grep de callers antes de tocar): 4 sitios más hacían
    `DELETE FROM games` directo sin limpiar hijos —
    `services/ra_duplicates_service.py:29`, `web/inbox_pipeline.py:102,911`,
    `web/handlers/sync_cloud.py:640` — habrían roto duplicados RA/inbox/
    migración cloud en cuanto el juego borrado tuviera metadata (~40% de los
    casos). Unificado en `cascade_delete_games_by_source_path()`
    (`database/repositories/games.py`), reutilizado por los 4 en vez de
    parchear cada uno por separado
  - [x] 9d. Test nuevo: `test_foreign_keys_enforced`
- [x] **REV43-10** — `delete_game()` deja filas huérfanas
  (`database/repositories/games.py:309-313`)
  - [x] 10a. `delete_game()` borra también `game_metadata`/`game_tags`/
    `file_operations` del `game_id` (helper compartido
    `_delete_game_children`, misma transacción)
  - [x] Decisión del usuario: `file_operations` se borra también (no
    `SET NULL`) — prioriza limpieza del historial de un archivo ya
    inexistente sobre preservar la auditoría
  - [x] 10b. Test nuevo: `test_delete_game_removes_children`
  - [x] 10c. No aplica — no se activó cascade real vía `ON DELETE CASCADE`
    en el schema, el borrado es explícito en código (ver 9)
- [x] **REV43-11** — `ambiguous column name: id` al combinar filtro `tag` +
  `genre`/`year` (`database/repositories/games.py:410-443`)
  - [x] 11a. La condición `"id IN"` ya no queda excluida del rewrite a
    `g.id IN` cuando hay JOIN con `game_metadata`
  - [x] 11b. Test nuevo: `test_get_games_paginated_tag_and_genre_together`
- [x] **REV43-12** — LIKE sin `ESCAPE` en historial de sync
  (`database/repositories/sync.py:94-98`)
  - [x] 12a. Añadido `ESCAPE '\'`. Hallazgo extra: no basta copiar el patrón
    de `games.py` — `game_dir` es una ruta real de Windows con `\` como
    separador, que choca con el propio carácter de escape; se escapan
    primero las `\` literales del path antes de escapar `_`
  - [x] 12b. Test nuevo: `test_get_save_sync_history_escapes_underscore`

Bugs de web (prioridad media):

- [x] **REV43-13** — `_httpd_instance` en scope equivocado → `/api/shutdown`
  y `/api/update/apply` rotos (`web/server.py:612-615`)
  - [x] 13a. `serve()` asigna `_state._httpd_instance = httpd` (eliminado el
    `global _httpd_instance` local, que nunca se leía desde `_state`)
  - [x] 13b. Smoke test manual: servidor real arrancado en un thread,
    confirmado que antes del fix `_state._httpd_instance` quedaba `None`
    (`AttributeError` al llamar `.shutdown`) y que tras el fix queda
    asignado y `shutdown()` funciona
- [x] **REV43-14** — Health Check siempre falla por import erróneo
  (`web/handlers/esde/maintenance.py:94`)
  - [x] 14a. Import corregido: `_write_health_schedule` vive en
    `web/daemons.py`, no en `web/server.py`
  - [x] 14b. Test nuevo (`tests/test_health_check_job.py`) en vez de smoke
    manual: registra el handler real en un `Router`/`JobManager` reales,
    dispara `/api/health-check` y verifica que el resultado no trae `error`
    y que `health_schedule.json` se escribe. Confirmado que sin el fix
    reproduce el `ImportError` real ("cannot import name
    '_write_health_schedule' from 'rom_manager.web.server'")
- [x] **REV43-15** — Con PIN activo, el setup de Anbernic se rompe
  (`web/server.py:256-263`)
  - [x] 15a. `/s` y `/api/rclone-export-config` añadidas a las rutas exentas
    del gate de sesión/PIN (mismo `if` que ya exime `/static/` y
    `/favicon.ico`)
  - [x] 15b. Confirmado: `_setup_token_ok()` (loopback o `?t=` válido) sigue
    corriendo dentro de ambos handlers sin cambios — no quedan abiertas
  - [x] 15c. 4 tests nuevos (`tests/web/test_anbernic_setup_with_pin.py`),
    con `client_ip` LAN simulado (no loopback) y PIN configurado. Confirmado
    que 3 reproducen el 302 real sin el fix (redirect a `/login` en vez de
    403/200) y 1 confirma que otras rutas (`/api/config`) siguen exigiendo
    sesión
- [x] **REV43-16** — Path traversal por comparación de prefijo de string en
  restore de backup (`web/handlers/games.py:653`)
  - [x] 16a. `str.startswith()` sustituido por `Path.resolve()` +
    `is_relative_to()`
  - [x] 16b. Test nuevo (`tests/web/test_restore_backup_path_traversal.py`)
    con `original_save` en un directorio hermano (`GamesEvil` vs
    `library_root=".../Games"`) — confirmado que sin el fix no solo pasaba
    la validación sino que restauraba de verdad el archivo fuera de la
    biblioteca (`ok: True`, fichero escrito en disco)
- [x] **REV43-17** — `/api/stop-job` no cancela un escaneo ADB en curso
  (`web/handlers/scan.py:401-469`)
  - [x] 17a. `_do_adb_scan` obtiene `job_manager.cancel_event("scan")` (mismo
    job_id que `_do_scan`) y corta el bucle de archivos en el siguiente
    boundary; `cancelled` añadido al `job_result`
  - [x] 17b. Test nuevo (`tests/web/test_adb_scan_cancel.py`): `AdbTransport`
    simulado con 10 archivos, cancelación disparada tras procesar el 3º
    (simulando `/api/stop-job` llegando a mitad del escaneo). Confirmado que
    sin el fix el resultado ni siquiera trae la clave `cancelled` — los 10
    archivos se procesan igual

Inconsistencias y mejoras menores (sin riesgo de datos, por orden del backlog):

- [x] **REV43-18** — `.ups` truncado lanza `IndexError` sin capturar
  (`patch/ups_applier.py:14-16`)
  - [x] Mismo guard que `bps_applier._read_vlq` (`raise PatchError` si
    `pos >= len(data)`)
  - [x] Test nuevo (`tests/test_ups_applier.py`); confirmado que sin el fix
    lanza `IndexError` real, no `PatchError`
- [x] **REV43-19** — Offsets negativos de `SourceCopy`/`TargetCopy` sin
  validar en BPS (`patch/bps_applier.py:87-103`)
  - [x] `raise PatchError` si `src_rel`/`tgt_rel` quedan negativos tras
    aplicar el delta con signo
  - [x] 2 tests nuevos (`tests/test_bps_applier.py`); confirmado sin el fix
    que no había ninguna excepción — el output salía mal en silencio
    (`source[-1]` envuelto al final del array por indexado negativo válido
    en Python)
- [x] **REV43-20** — Conversión N64 sobrescribe destino sin comprobar si ya
  existe (`converters/n64_converter.py:84-85`)
  - [x] `target.exists()` rechaza la conversión, mismo patrón que
    `chd_converter`
  - [x] 2 tests nuevos (`tests/test_n64_converter.py`, no existía archivo de
    test para este converter); confirmado sin el fix que el destino se
    sobrescribía de verdad (`success=True`, contenido previo perdido)
- [x] **REV43-21** — Padding del último chunk se escribe también al `.z64`
  (`converters/n64_converter.py:90-103`)
  - [x] El padding solo se usa para que el swap de bytes/palabras esté bien
    definido; se escriben solo los primeros `orig_len` bytes del resultado,
    descartando el relleno
  - [x] 2 tests nuevos (v64 impar, n64 no múltiplo de 4); confirmado sin el
    fix que el `.z64` salía con 1 byte de más (8 en vez de 7)
- [x] **REV43-22** — `LIKE` sin escapar en `last_played_at`
  (`scanner/rom_scanner.py:145-148`)
  - [x] Escapado `\`/`%`/`_` + `ESCAPE '\'` (mismo ajuste de barras invertidas
    de rutas Windows que REV43-12 — el separador de ruta también hay que
    escaparlo, no solo el nombre)
  - [x] Test nuevo (`tests/test_scanner.py`); confirmado sin el fix que un
    ROM señuelo (`ZeldaXofXTime.gba`) recibía `last_played_at` de un save de
    `Zelda_of_Time.srm`
- [x] **REV43-23** — `cue_validator` no reconoce `FILE` sin comillas
  (`detection/cue_validator.py:6`)
  - [x] Mismo patrón de dos regex (comillas → fallback sin comillas) que
    `chd_converter.parse_bins_from_cue`, sin crear dependencia inversa
    `detection`→`converters`
  - [x] 2 tests nuevos (`tests/test_cue_validator.py`); confirmado sin el
    fix que un `.bin` ausente referenciado sin comillas pasaba con 0 errores
- [x] **REV43-24** — Rollback de `move_disc_set_to_subfolder` traga
  `OSError` en silencio (`renamer/file_renamer.py:264-273`)
  - [x] `_rollback()` devuelve la lista de fallos; incluida en el error con
    el mismo formato "rollback INCOMPLETE — manual fix needed" que
    `rename_rom_with_saves`
  - [x] 1 test nuevo (2º BIN forzado a fallar + rollback del 1º forzado a
    fallar también); confirmado sin el fix que el error no mencionaba nada
    del rollback fallido
- [x] **REV43-25** — Backup degenera en no-op si `.bak` ya existe de un
  reintento previo (`renamer/file_renamer.py:169-171`)
  - [x] Siempre busca un nombre `.bak`/`.bak1`/`.bak2`... libre, nunca
    reemplaza `bak` por sí mismo
  - [x] 1 test nuevo (`.bak` preexistente + save actual con contenido
    distinto); confirmado sin el fix que el save actual se sobrescribía sin
    backup real
- [x] **REV43-26** — `verify_multidisc()` revienta con `IndexError` en un
  grupo solo-sidecar (`utils/multidisc_verifier.py:84`)
  - [x] El chequeo de gaps se salta si `disc_numbers` queda vacío (nada que
    comprobar sin imágenes reales; el chequeo de `missing_file` ya cubre
    archivos ausentes)
  - [x] 1 test nuevo; confirmado sin el fix que un solo set roto abortaba
    la verificación de toda la biblioteca, incluido un segundo grupo sano
    en el mismo directorio
- [x] **REV43-27** — Fallo de red en RA trata la plataforma como "0 juegos
  con logros" (`retroachievements/ra_checker.py:94-97`)
  - [x] Nuevo status `"check_failed"` (distinto de `"no_support"`) para los
    juegos de una plataforma cuyo fetch falló — no entran en
    `no_support_entries` (`web/handlers/sync.py`), que alimenta el
    bulk-discard de `discard_no_support`
  - [x] 2 tests nuevos (`tests/test_ra_checker.py`, no existía archivo de
    test para este módulo); confirmado sin el fix que un `TimeoutError`
    simulado marcaba `no_support` a juegos con soporte RA real —
    elegibles para borrado masivo por un blip de red
- [x] **REV43-28** — `get_ra_hash_lib` bypasea el TTL de 1 semana de la
  caché RA (`services/ra_duplicates_service.py:180-190`)
  - [x] Mismo `_CACHE_TTL_SECONDS` que `ra_client.fetch_hash_library`; caché
    caducada se trata como inexistente
  - [x] 2 tests nuevos; confirmado sin el fix que una caché de 8 días se
    usaba igual que una fresca
  - [x] Hallazgo extra (investigar antes de arreglar): `_build_ra_duplicates`
    (`web/builders/duplicates.py:407-416`) lee el mismo `.rommgr/ra_cache/`
    con idéntico bug — documentado como **REV43-53** en el backlog, no
    corregido en esta rama (alcance distinto al ticket original)
- [x] **REV43-29** — Dedup de `gamelist_writer` colapsa discos distintos del
  mismo set (`scraper/gamelist_writer.py:142-166`)
  - [x] Dedup en dos niveles: `.m3u` presente → colapsa todo el set (igual
    que antes); sin `.m3u` → deduplica por número de disco, no solo por
    título
  - [x] 4 tests nuevos (`tests/test_gamelist_writer.py`, no existía archivo
    de test); confirmado sin el fix que Disc 2/3 desaparecían del gamelist
    cuando ScreenScraper asigna el mismo título a todos los discos sin
    `.m3u` presente

> Resto de hallazgos (REV43-30…53 — RA/scraper/cli/database/web) quedan en
> el backlog para sesiones siguientes, por orden de valor/esfuerzo.

### Aplazado de Día42

- Release `develop` → `main` + `RetroVault-Setup.exe` + tag — se retoma
  cuando REV43-1…17 (o al menos el bloque de sync 1…8) esté cerrado.
- Probar en navegador la UI de conflictos del Inbox (RA-CONFLICT-2).
- D37-8 (prueba en PC limpio sin Python) — sigue pendiente, sin bloquear.

## Trabajo realizado

REV43-1…17 cerrados en su propia rama cada uno (patrón INBOX-FIX-*), todos
con tests nuevos y suite completa en verde antes de commitear:

- Sync (Pilar 3): `feature/cable-ux` (REV43-1…8)
- Integridad de BD: `fix/db-integrity` (REV43-9…12) — FK enforcement activado
  descubrió 4 sitios más con `DELETE FROM games` sin cascada, unificados en
  `cascade_delete_games_by_source_path()`
- Bugs de web: `fix/web-httpd-shutdown` (13), `fix/web-health-check-import`
  (14), `fix/web-pin-anbernic-setup` (15), `fix/web-restore-path-traversal`
  (16), `fix/web-adb-scan-cancel` (17)

## Archivos modificados

- `Tareas/backlog.md` — sección REV43 (52 hallazgos, archivo:línea) + estado
  de cada uno de REV43-1…17.
- `Tareas/diario/Día43.md` — este archivo.
- Código: ver cada commit en las ramas `fix/*` listadas arriba.

## Estado al finalizar

Bloque REV43-1…17 completo. Queda REV43-18…52 (inconsistencias y mejoras
menores) en el backlog para sesiones futuras, sin bloquear la release.

## Siguiente sesión recomendada

1. Las ramas `fix/*` de esta sesión están sin mergear a `develop`/`main` —
   revisar y mergear antes de nada.
2. Con REV43-1…17 cerrado, retomar la release aplazada de Día42
   (`develop` → `main` + `RetroVault-Setup.exe` + tag).
3. REV43-18…52 quedan por orden de valor/esfuerzo cuando se retome el
   backlog de calidad de código.
