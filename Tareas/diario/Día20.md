# Retro Vault — Día 20 (2026-03-24)

## Contexto

El Día 19 completó todos los objetivos del Tier 4 y arrancó el Tier 5 con:
- **S39-3** Tray icon Win32 completo (`tray_icon.py`, `--tray` flag)
- **S39-4** Toggle inicio automático con Windows (registro HKCU, `/api/autostart-toggle`)
- **S40** Auto-configuración consola Android (script Termux + QR + detección User-Agent)
- **BUG-A/B/C/D/E** — todos resueltos

Pendientes arrastrados:
- **S39-1** — PyInstaller packaging (Tier 5, pendiente desde Día 19)
- **Cyberpunk UI** — pendiente por tercera vez

---

## Objetivos del día

### ✅ QoL-IA — Botón "Link Internet Archive" en lista de ROMs faltantes
- Nueva columna "Internet Archive" en la tabla de ROMs faltantes (Colección)
- Botón `🔗 Link IA` por cada entrada: copia la URL `https://archive.org/search?query=...`
- El link copia directo al portapapeles → JDownloader lo detecta automáticamente si tiene el monitor de portapapeles activo
- URL format: `https://archive.org/search?query={título}+{plataforma}+No-Intro`
- Botón verde-teal distinguible del botón "Copiar" (query de texto) existente
- Toast: "Link copiado — pégalo en JDownloader"

### Portabilidad (Tier 5)

- [x] **QoL-IA** — Botón Link Internet Archive (completado, ver arriba)
- [x] **S39-1** — Empaquetado con PyInstaller (`--onedir`):
  - `RetroVault.spec` — spec completo con binaries, datas, hiddenimports
  - `build.ps1` — script PowerShell que limpia, compila y copia config.toml
  - Fix `cli.py`: detecta `sys.frozen` → usa `Path(sys.executable).parent` como project_root
  - Fix `server.py`: detecta `sys.frozen` → usa `sys._MEIPASS / rom_manager/web/static`
  - Incluye `tools/chdman.exe`, `tools/adb.exe`, `tools/AdbWinApi.dll`, `tools/AdbWinUsbApi.dll`
  - Output: `dist/RetroVault/RetroVault.exe serve --tray`

### UI

- [x] **UI-2** — Dashboard de inicio:
  - Stats clave: N juegos · N plataformas · último sync hace X
  - "Juego del día" visible desde home
  - Acciones rápidas: Sync ahora · Scan · Inbox (si hay archivos pendientes)
  - Indicador salud: X ROMs OK · Y problemas detectados
  - Sustituye al "Overview" actual que solo muestra resultados del scanner

- [x] **UI-4** — Modal/página de detalle de juego:
  - Click en una card → modal con portada grande
  - Metadatos completos (región, año, desarrollador, descripción)
  - Estado de saves (cuántos, última sync, backup disponible)
  - Logros RA (cuántos, enlace a retroachievements.org)
  - Notas del usuario
  - Botones: Editar metadata · Ver backups · Abrir carpeta

### Ideas nuevas

- [x] **QoL-11** — Backup automático pre-sync:
  - Antes de cualquier sync (cloud o cable), hacer snapshot versionado local automático
  - Integrar con el sistema de backup (S29) ya existente
  - Config: `[sync] pre_sync_backup = true` (default true)

- [ ] **QoL-12** — Exportar colección:
  - Botón "Exportar biblioteca" → genera `.json` o `.csv` con toda la colección
  - Campos: título, plataforma, SHA1, play_status, playtime, última partida
  - Útil para backups externos o importar en otra instalación de Retro Vault

- [x] **QoL-13** — Indicador de estado en tray icon:
  - El icono cambia de color según el estado del sistema
  - Gris = idle · Verde = sync en curso · Amarillo = conflictos pendientes · Rojo = error
  - Implementar con `set_status()` ya disponible en `TrayIcon`

- [x] **QoL-14** — Modo offline explícito en dashboard:
  - Si rclone/ADB no están disponibles, mostrar claramente qué funciones están deshabilitadas
  - En lugar de dejar que fallen silenciosamente cuando el usuario intenta usarlas
  - Badge "Sin conexión" en los paneles afectados (Cloud Sync, Cable Sync)

- [x] **TEC-6** — Test de integración E2E post-Día 18-19:
  - Ejecutar el agente `integration-tester` para verificar que el pipeline completo funciona
  - Comprobar: scan → match → rename → inbox → sync → report
  - Prioritario antes de añadir más features encima

- [ ] **Cyberpunk UI** ← pendiente por **cuarta vez**

---

## Trabajo realizado

### S39-1 — PyInstaller packaging (✅ completado)
- `RetroVault.spec` — spec PyInstaller con `--onedir`:
  - `datas`: `src/rom_manager/web/static` → `rom_manager/web/static`
  - `binaries`: `tools/chdman.exe`, `tools/adb.exe`, `tools/AdbWinApi.dll`, `tools/AdbWinUsbApi.dll`
  - `hiddenimports`: módulos con late-import (inbox_pipeline, cable_sync_daemon, tray_icon, notifier, etc.)
  - `excludes`: pytest, tkinter, setuptools
- `build.ps1` — PowerShell: detecta Python Conda, limpia dist/, ejecuta PyInstaller, copia config.toml.example
- `src/rom_manager/cli.py` — `main()`: si `sys.frozen` → `project_root = Path(sys.executable).parent`
- `src/rom_manager/web/server.py` — static dir: si `sys.frozen` → `Path(sys._MEIPASS) / "rom_manager/web/static"`

**Para compilar:**
```powershell
.\build.ps1
# → dist\RetroVault\RetroVault.exe serve --tray
```

### QoL-IA — Botón Link Internet Archive (✅ completado al inicio del día)
- `src/rom_manager/web/static/app.js` — `_renderMissingList()`:
  - Nueva columna "Internet Archive" en la tabla de ROMs faltantes
  - Variable `iaUrl` con URL real de archive.org (`encodeURIComponent`)
  - Botón `🔗 Link IA` con estilo verde-teal (`#4ec9b0`, border `#2d4a2d`)
  - `onclick` copia la URL al portapapeles + toast "Link copiado — pégalo en JDownloader"
  - Compatible con el monitor de portapapeles de JDownloader (añade la descarga automáticamente)

---

## Archivos modificados

```
src/rom_manager/web/static/app.js  — nueva columna + botón Link IA en _renderMissingList()
```

---

## Siguiente sesión recomendada

**Día 21 — UI & Calidad**

Prioridad 1:
- **UI-5** — Refactorizar inline styles a clases CSS (alto impacto visual, mantenibilidad)

Prioridad 2:
- **QoL-1** — Quick scan en backend (modo rápido ya en UI, falta implementar en `rom_scanner.py`)
- **QoL-4** — Platform health dashboard (ROMs vs DAT, portadas, logros RA, último sync)

Prioridad 3:
- **QoL-3** — Historial de sync visual (timeline con iconos, agrupado por día)
- **QoL-10** — Save diff viewer (comparar backups, restaurar versión)


Vale, vamos a ver, cositas que hacer mañana:
Es-de no apunta a los emuladores que yo quiero (duckstation para psx, dolphin para gc y wii, flycast para dreamcast, mupen64 para N64...)

creo que la config de ES-de no está muy bien hecha (aún se me ven archivos duplicados, incluso en la colección veo carpetas, cuando me gustaría que solo viera los juegos de dentro)

En la app, al entrar en determinadas pestañas, el menú lateral izquierdo se pierde (cable sync, herramientas, formatos y scraper) 

La pestaña "colección sólo guarda algunos juegos
la pestaña "duplicados" sigue dando error con 101 duplicados, pese a que hay 226 grupos de archivos (no entiendo que no borre todos)
la pestaña organizar saca 483 conflictos que podría resolver añadiendo sufijo, pero no me gusta esa resolución. quiero que resuelva borrando los duplicados que no tengan logros

me gustaría saber si entiendes, de qué pueden ser las roms que la app no identifica (si me faltan catalogs, de qué plataforma son?)
