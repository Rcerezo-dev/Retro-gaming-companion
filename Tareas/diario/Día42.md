# ROM Manager Local — Día 42

## Contexto

Día41 cerró ZIP-ROUTE-FIX-1/2/3 (PRs #105-107) y RA-CONFLICT-1/2 (PRs
#108-109). El plan original de este día era publicar una release con los
~192 commits acumulados en `develop` desde v1.0.0, pero **se aplaza**: antes
hay que cerrar la auditoría funcional (AUD-1…6, `Tareas/Roadmap-Auditoria.md`)
y la nueva auditoría del selector de dispositivo (DEVSEL-FIX-1…4), que ha
destapado dos fallos con riesgo de pérdida de datos. No tiene sentido
publicar con eso abierto.

## Objetivos del día

### Auditoría funcional (AUD, 2026-07-12)

- [x] **AUD-1 — Sync Doctor**: desviación de reloj PC↔consola, saves con mtime
  futuro, saves solo en un lado, último sync por juego. 🟡 en rama
  `feature/aud-1-sync-doctor` (663 tests) — falta validar con consola (V-AUD-1).
- [x] **AUD-2 — Verificación MD5 post-transferencia**: `AdbTransport.pull/push(verify=True)`,
  pull a `.part`, columna `verified` en `save_sync_log`. 🟡 en rama
  `feature/aud-2-transfer-verify` (671 tests) — falta validar con consola (V-AUD-2).
- [ ] **AUD-3 — Papelera unificada con purga** (`_descartados/` + purga >30 días).
- [ ] **AUD-4 — `.md` ambiguos del Inbox por CRC** (los 177 varados).
- [ ] **AUD-5 — Informe de completitud por plataforma (1G1R)**.
- [ ] **AUD-6 — `chdman verify` en health check**.

### Auditoría del selector de dispositivo (DEVSEL-FIX, 2026-07-12)

Hallazgos documentados en `Tareas/backlog.md` con archivo:línea. Cada fix en
su propia rama. Orden por riesgo:

- [x] **DEVSEL-FIX-1 — Acciones de duplicados ignoran el dispositivo**
  (pérdida de datos: "Eliminar todos" en modo consola borra duplicados del PC;
  el delete individual borra el registro de la BD equivocada). 🟡 en rama
  `feature/devsel-fix-1` (664 tests) — pendiente merge.
- [ ] **DEVSEL-FIX-2 — Favoritos/tags/notas/metadatos siempre a la BD del PC**
  (en modo consola escriben en el juego equivocado o en ninguno).
- [ ] **DEVSEL-FIX-3 — "Sistema completo" = solo PC en la práctica**
  (plan/apply/games/stats/exports solo consultan la BD del PC sin `source_root`).
- [ ] **DEVSEL-FIX-4 — Botón "Consola" habilitado por ruta, no por detección**
  (debe deshabilitarse cuando `!deviceConnected`; `dev-both` nunca se deshabilita).

### Aplazado (del plan original de Día42)

- Release `develop` → `main` + `RetroVault-Setup.exe` + tag — **cuando AUD y
  DEVSEL-FIX estén cerrados**.
- Probar en navegador la UI de conflictos del Inbox (RA-CONFLICT-2).
- Restos de Día41: pack `MAME BIOS 0.277.zip`, los 28 "otra versión de
  romset", los 177 `.md` del Inbox (los cubre AUD-4).

## Trabajo realizado

- **AUD-1** implementado en `feature/aud-1-sync-doctor`: `AdbTransport.device_epoch()`,
  builder puro `web/builders/sync_doctor.py`, `GET /api/sync-doctor`, card en
  tab-cable y confirmación en `doCableSync` si hay desviación de reloj. 5 tests.
- **AUD-2** implementado en `feature/aud-2-transfer-verify` (apilada sobre AUD-1):
  verificación MD5 en el chokepoint `AdbTransport.pull/push`, migración de
  `save_sync_log` verificada sobre la BD real, contadores en job y UI. 8 tests.
- **Auditoría DEVSEL**: rastreado el selector completo (frontend `state.js` /
  `main.js:412,428` → backend `_repo_for_path()` en `builders/common.py:141`)
  y cada handler que muta datos. 4 hallazgos documentados en el backlog
  (sección DEVSEL-FIX), sin tocar código (regla "investigar antes de arreglar").

## Archivos modificados

- `Tareas/backlog.md` — nueva sección DEVSEL-FIX-1…4.
- `Tareas/diario/Día42.md` — reenfocado de release a auditorías.
- (En ramas AUD-1/AUD-2: ver sus commits `ff9fbbe` y `ac12e30`.)

## Estado al finalizar

(Se rellena al terminar.)

## Siguiente sesión recomendada

1. **DEVSEL-FIX-1** (pérdida de datos, prioridad absoluta) en su propia rama.
2. **DEVSEL-FIX-2** (rápido: enviar `source_path` + `get_repo_fn()` en 3 handlers).
3. Validar AUD-1/AUD-2 con la consola conectada (V-AUD-1, V-AUD-2) y mergear.
