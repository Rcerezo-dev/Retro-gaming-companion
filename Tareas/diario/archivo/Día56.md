# ROM Manager Local — Día 56

## Contexto

Sesión desde un ordenador distinto al habitual: sin la biblioteca real montada
(ni `F:\Juegos Retro` de esta máquina ni `E:\Carpetas anbernic`/`H:\ROMs` del
otro PC) y sin el dispositivo Anbernic conectado por ADB. Mismo patrón que
Día54/Día55: sesión dedicada a trabajo de código puro, verificable con datos
sintéticos/tests, sin tocar biblioteca ni hardware real.

Estado heredado de Día55: `PR #299` (`LIB-MISPLACED-1`/
`LIBRARY-AUDIT-EXCLUDE-GAP-1`/`REPAIR-TOOL-8`) ya mergeado a `develop`
(`b06d16f`). `PR #300` (`ANBERNIC-PICK-5` + `CHDMAN-TEST-COMPRESS-1`) y
`PR #301` (rama actual `feature/chd-cleanup-1-bare-bins`, con
`CHD-CLEANUP-1` + `DEVPROFILE-8` + `GBA-SAVE-PATH-1c`) siguen abiertos contra
`develop`. Working tree limpio, nada pendiente de commit.

Al comprobar el estado de ambas PRs antes de planificar, las dos aparecen
`mergeable: CONFLICTING` contra `develop` — comprobado con
`git merge-tree` (sin tocar el árbol de trabajo):

- **PR #300**: conflicto solo en `Tareas/backlog.md`.
- **PR #301**: conflicto en `Tareas/backlog.md` y `src/rom_manager/cli.py`.

En ambos casos `develop` solo avanzó por el merge del PR #299, así que son
conflictos de edición cercana (misma zona del backlog, mismo fichero CLI
tocado por dos ramas hermanas), no de diseño — resolubles sin biblioteca ni
hardware.

Triage de `backlog.md` completo (agente `backlog-triage`, con la restricción
explícita de "sin biblioteca ni Anbernic" para esta máquina) encontró 3
candidatos de código puro no abordados en sesiones previas, en orden de
prioridad por pilar (CLAUDE.md: sync > Inbox > primera vez):

1. `CABLE-ROM-FIX-4` (Pilar 3, sync) — `backlog.md:888`
2. `ZIP-ROUTE-6` (Pilar 2, Inbox) — `backlog.md:625`
3. `DUP-CROSSFMT-1` (Pilar 1, organización) — `backlog.md:1511`
4. `DEDUP-RENAME-2` (opcional, si sobra tiempo) — `backlog.md:514`

Descartados por depender de biblioteca real (`E:\`/`H:\`), de ADB/hardware,
o de decisión de diseño pendiente del usuario: `CATALOG-MATCH-REGION-2`,
`GAME-BLOCKLIST-1/2`, `ARCADE-RENAME-BUG-1c/1e`, `HEALTH-CHECK-1a`,
`LIBRARY-SYNC-STALE-1a/1e`, `GAMECUBE-DISC-BUG-1c`, `INBOX-ORPHAN-5`,
`GBA-MISPLACED-1b/1c`, `DEVPROFILE-6`, `DEVPROFILE-8b`/`DEVPROFILE-9`,
`HERR-FIX-4` (necesita reproducir en navegador), `SAGE-3`/`SAGE-4` (pilar
secundario, `SAGE-4` además de esfuerzo grande).

## Objetivos del día

0. **Resolver conflictos de merge de PR #300 y PR #301** contra `develop`
   (`backlog.md` en ambos, `cli.py` también en el #301) — housekeeping previo
   antes de seguir apilando ramas nuevas sobre ellas.
1. **`CABLE-ROM-FIX-4`** — selector de plataformas incluir/excluir en Cable
   Sync (UI + backend), reutilizando `PLATFORM_BY_FOLDER`/
   `_ES_PLATFORM_FOLDERS` ya existentes en `web/handlers/sync_cable.py`, en
   vez del script ad-hoc manual usado en `CABLE-ROM-FIX-3`. Único pendiente
   puro dentro del pilar de mayor prioridad del proyecto.
2. **`ZIP-ROUTE-6`** — `_extract_collection` (`web/zip_router.py:47-72`)
   extrae colecciones sin separar por sub-plataforma (bestset FBNeo con
   `games/<cps1|cps2|cps3|neogeo|fbneo|toaplan_cave_stg>/` cae todo mezclado
   bajo `arcade/`). Reproducible con ZIP sintético en memoria.
3. **`DUP-CROSSFMT-1`** — agrupar duplicados también cruzando formato
   (`.zip` vs `.chd` del mismo disco), hoy solo se agrupa por SHA1 exacto
   dentro del mismo formato. Alcance ya medido en sesión previa (41 casos,
   13,5 GB); lógica testeable con filas sintéticas de `games`/
   `game_metadata`.
4. *(si sobra tiempo)* **`DEDUP-RENAME-2`** — investigación pura: confirmar
   si "Revisar copias" filtra/expone por Anbernic o si el selector de
   dispositivo lo oculta (solo lectura de código + trazado del flujo, regla
   "Investigar antes de arreglar" del CLAUDE.md).

## Trabajo realizado

1. **`CABLE-ROM-FIX-4` implementado** — selector de plataformas a excluir en
   Cable Sync, persistido en el propio endpoint en vez del script ad-hoc
   manual de `CABLE-ROM-FIX-3`.
   - Backend (`web/handlers/sync_cable.py`): nuevo campo
     `exclude_platform_folders` (lista de nombres de carpeta en minúscula,
     p.ej. `["arcade"]`) en `POST /api/cable-sync`. Aplicado en dos puntos:
     `_wanted()` (lado PC/SD — nuevo helper `_platform_folder_of(p)` que
     prueba `pc_root` y `ab_root`, con `ab_root` declarado como `None` antes
     del closure para que no reviente con `NameError` en modo ADB, donde
     nunca se asigna) y el nuevo `_wanted_info(info)` (lado Android/ADB,
     combina `_wanted_name` + carpeta de plataforma del `rel_posix`).
     Cubre las 3 direcciones (`pc_to_anbernic`/`anbernic_to_pc`/`newest`) y
     el espejo (`delete_extra`): una plataforma excluida nunca cuenta como
     "extra" a borrar, en ninguno de los dos lados.
   - Nuevo `GET /api/platform-folders` — expone
     `_STANDARD_PLATFORM_FOLDERS` (ya existente en `handlers/system.py`)
     para poblar el selector, sin duplicar la tabla en el frontend.
   - Frontend: `<select multiple>` "Excluir plataformas" en
     `tab-cable.html` (sección "¿Qué sincronizar?"), poblado una vez por
     sesión de pestaña (`_loadCablePlatformFolders()` en `sync.js`, mismo
     patrón que `_cableModeAutoSelected`). `doCableSync()` lee la selección
     y la manda en ambos cuerpos de petición (ADB y SD/FS).
   - 6 tests nuevos (`tests/test_sync_cable_exclude_platforms.py`): FS mode
     en las 3 direcciones, case-insensitive, comportamiento sin cambios con
     la lista vacía, espejo no borra la plataforma excluida, y el endpoint
     `/api/platform-folders`.
   - Verificado además end-to-end contra el servidor real (`rommgr serve`
     + `curl`, sin biblioteca real montada — datos sintéticos en
     `%TEMP%\cable_smoke`): `pc_to_anbernic` con `exclude_platform_folders:
     ["arcade"]` copió solo `psx/game.chd`, `arcade/sf2.zip` se quedó fuera.
     No se pudo probar visualmente en Chrome (extensión no conectada en
     este ordenador) — se verificó el HTML/JS servidos por curl y `node
     --check` en vez de captura de pantalla.

Verificación: suite completa **1220/1220** (incluye los 6 tests nuevos de
hoy; el resto del incremento sobre los 1204 de cierre de Día55 viene de los
commits de `CHD-CLEANUP-1`/`DEVPROFILE-8`/`GBA-SAVE-PATH-1c`, ya mergeados en
esta rama antes de empezar la sesión de hoy). `ruff check`/`ruff format
--check` limpios en todos los archivos tocados (los 3 scripts sin formatear
que reporta `ruff format --check .` son ajenos, preexistentes).

## Archivos modificados

- `src/rom_manager/web/handlers/sync_cable.py` (`exclude_platform_folders`,
  `_platform_folder_of`, `_wanted_info`, `ab_root` declarado antes del
  closure, endpoint `GET /api/platform-folders`).
- `src/rom_manager/web/static/partials/tab-cable.html` (`<select
  id="cable-exclude-platforms">`).
- `src/rom_manager/web/static/js/tabs/sync.js`
  (`_loadCablePlatformFolders`, `doCableSync` envía
  `exclude_platform_folders`).
- `tests/test_sync_cable_exclude_platforms.py` (nuevo, 6 tests).
- `Tareas/backlog.md` (`CABLE-ROM-FIX-4` marcado `✅`).

## Estado al cierre de `CABLE-ROM-FIX-4`

`CABLE-ROM-FIX-4` completo, probado (unit + smoke HTTP real), documentado,
commit `4b3e1b7` en rama `feature/cable-rom-fix-4-platform-exclude`
(apilada sobre `feature/chd-cleanup-1-bare-bins`, PR #301, aún sin
mergear), push y **PR #302 abierto** contra esa base. Pre-push (ruff +
suite completa 1220/1220) en verde. (Estado intermedio de la sesión —
ver "Estado al finalizar" al final del diario para el cierre real.)

2. **`ZIP-ROUTE-6` implementado** — colecciones arcade con subcarpetas
   internas por sub-sistema (bestset FBNeo: `games/<cps1|cps2|cps3|
   neogeo|fbneo|toaplan_cave_stg>/`) ya no se anidan bajo `arcade/games/
   <subsistema>/`.
   - `docs/arcade-setup.md` confirma que la convención del proyecto es un
     único `arcade\` plano (un mismo core FBNeo cubre todos esos
     subsistemas) — la subcarpeta interna del ZIP nunca es una plataforma
     real, así que aplanar es más simple y correcto que mapear cada
     subsistema a un destino propio. Esto también resuelve de paso el gap
     documentado de `toaplan_cave_stg` (sin carpeta equivalente en la
     convención): al aplanar, deja de hacer falta ningún mapeo.
   - `_extract_collection()` (`web/zip_router.py`) acepta nuevo parámetro
     `flatten: bool` — con `flatten=True` extrae cada miembro por su
     nombre base (`Path(m.filename).name`), ignorando la ruta interna del
     ZIP. `_route_identified()` solo lo activa para el destino arcade
     (`is_arcade = _majority(stems, arcade_names)`); el destino Inbox
     sigue conservando la ruta interna sin cambios — la necesita
     ZIP-ROUTE-FIX-3 para el contexto de carpeta que desambigua `.md`.
   - 2 tests nuevos en `tests/test_zip_router.py`: bestset con 4
     subcarpetas → 4 archivos planos en `arcade/` (sin `arcade/games/`);
     colisión por nombre aplanado no sobreescribe (verifica que el chequeo
     de "ya existe" también mira el nombre aplanado, no la ruta interna
     del ZIP que nunca existiría en un destino plano).
   - Solo verificado con datos sintéticos (el hallazgo original tampoco
     llegó a ejecutar el apply contra el bestset real) — sin biblioteca
     real montada en esta máquina para probar contra el ZIP de verdad.

Verificación: suite completa **1222/1222**. `ruff check`/`ruff format
--check` limpios en los archivos tocados.

## Archivos modificados (`ZIP-ROUTE-6`)

- `src/rom_manager/web/zip_router.py` (`_extract_collection` con
  `flatten`, `_route_identified` lo activa para el destino arcade).
- `tests/test_zip_router.py` (2 tests nuevos).
- `Tareas/backlog.md` (`ZIP-ROUTE-6` marcado `✅`).

`ZIP-ROUTE-6`: commit `712144b` en rama
`feature/zip-route-6-flatten-arcade-subsystems` (apilada sobre
`feature/cable-rom-fix-4-platform-exclude`, PR #302), push y **PR #303
abierto** contra esa base. Pre-push (ruff + suite completa 1222/1222) en
verde.

3. **`DUP-CROSSFMT-1` implementado** — la cola de revisión de duplicados
   ("Revisar copias") ahora también agrupa el mismo disco en dos formatos
   distintos (`.zip` vs `.chd`), no solo por SHA1 exacto dentro del mismo
   formato.
   - Riesgo real a evitar: el propio código ya documenta una regresión
     previa — un normalizador de título fuzzy sin cuidado fusionó 18
     ediciones regionales distintas de Final Fantasy VII en un solo grupo
     "duplicado" (comentario junto al union por `canonical_title` en
     `_review_groups_for_repo`). Diseño deliberadamente más estrecho para
     no repetir ese fallo: nuevo `strip_disc_tag()` (`utils/disc_tag.py`)
     quita solo la etiqueta de disco; nuevo `_normalize_title_cross_format()`
     (`web/builders/duplicates.py`) usa esa función y **conserva** región/
     idioma como tokens — a diferencia del normalizador fuzzy completo de
     `ra_checker`, que sí los borra (esa es la causa raíz de la regresión
     de 2026).
   - Union nuevo en el Union-Find existente (junto a SHA1 y
     canonical_title exacto): mismo `(platform, título fuzzy)` cruzando
     **≥2 extensiones distintas** — mismo formato ya lo cubren los otros
     dos links, unirlos aquí también sería redundante. Razón nueva
     `"crossfmt"`, protegida por el mismo guard `_is_disc_set()` que ya
     usa la razón `"title"` contra sets multi-disco reales.
   - Frontend: badge nuevo `_REASON_LABELS.crossfmt` en
     `review_copies.js` ("Mismo disco, otro formato"); sin más cambios de
     flujo — mismo "Aplicar recomendación"/"Copia intencional" que ya usa
     la razón `"title"` (soft-discard a `_descartados/`, recuperable 30
     días, mismo perfil de riesgo ya aceptado por el proyecto para
     coincidencias fuzzy).
   - 6 tests nuevos: 2 en `tests/test_disc_tag.py` (`strip_disc_tag`
     quita solo la etiqueta de disco, conserva región), 4 en
     `tests/test_builders_duplicates.py` (detecta el caso real .zip/.chd;
     no fusiona regiones distintas — mismo guard que el union exacto; no
     fusiona un set multi-disco real .zip Disc1/.chd Disc2; no marca
     "crossfmt" cuando ambos lados son el mismo formato).
   - Solo verificado con datos sintéticos — sin biblioteca real montada en
     esta máquina para confirmar contra los 41 casos reales (13,5 GB) que
     motivaron la tarea.

Verificación: suite completa **1228/1228**. `ruff check`/`ruff format
--check` limpios.

## Archivos modificados (`DUP-CROSSFMT-1`)

- `src/rom_manager/utils/disc_tag.py` (`strip_disc_tag` nueva).
- `src/rom_manager/web/builders/duplicates.py`
  (`_normalize_title_cross_format`, union cross-format, razón
  `"crossfmt"`).
- `src/rom_manager/web/static/js/tabs/review_copies.js`
  (`_REASON_LABELS.crossfmt`).
- `tests/test_disc_tag.py` (2 tests nuevos).
- `tests/test_builders_duplicates.py` (4 tests nuevos).
- `Tareas/backlog.md` (`DUP-CROSSFMT-1` marcado `✅`).

`DUP-CROSSFMT-1`: commit `fb907b5` en rama
`feature/dup-crossfmt-1-cross-format-detection` (apilada sobre
`feature/zip-route-6-flatten-arcade-subsystems`, PR #303), push y **PR #304
abierto** contra esa base. Pre-push (ruff + suite completa 1228/1228) en
verde.

Con esto, los 3 objetivos del día (`CABLE-ROM-FIX-4`, `ZIP-ROUTE-6`,
`DUP-CROSSFMT-1`) quedan completos, cada uno en su propia rama/PR
(#302→#303→#304), todos apilados y pendientes de mergear.

## Housekeeping — conflictos de PR #300 y #301 resueltos

Objetivo 0 del día (identificado al empezar, pospuesto hasta cerrar los 3
objetivos de código): ambas PRs seguían `mergeable: CONFLICTING` contra
`develop`. Resuelto con `git merge origin/develop` en cada rama (no
`rebase` — así los PR #302/#303/#304, apilados sobre #301, no necesitan
tocarse: sus commits no cambian de hash con un merge).

- **PR #300** (`feature/anbernic-pick-5-other-category`): único conflicto
  real, `Tareas/diario/Día55.md` — un `add/add` porque tanto esta rama
  como `develop` (vía el merge del PR #299) habían añadido el mismo
  archivo nuevo de forma independiente, con contenido distinto (`develop`
  tenía solo la "Ronda 1" de Día55 desde antes de que existiera la "Ronda
  2"; esta rama ya tenía el diario completo con ambas rondas). Resuelto
  quedándose con la versión completa de esta rama, que ya incluye —y
  amplía— todo lo que aportaba la versión de `develop`. `Tareas/backlog.md`
  se auto-mergeó sin intervención (el conflicto que reportaba GitHub ya
  estaba obsoleto para cuando se resolvió). Merge commit `018a581`, push,
  suite completa **1204/1204** tras el merge. PR ahora `MERGEABLE`.
- **PR #301** (`feature/chd-cleanup-1-bare-bins`): mismo conflicto exacto
  en `Día55.md` (misma causa, resuelto igual); `backlog.md` y `cli.py`
  también se auto-mergearon sin intervención. Merge commit `ee4afae`,
  push, suite completa **1214/1214** tras el merge. PR ahora `MERGEABLE`.
- Confirmado con `gh pr view <N> --json mergeable`: los 4 PRs de la cadena
  (#301, #302, #303, #304) quedan `MERGEABLE` de punta a punta — el merge
  en #300/#301 no rompió nada río abajo.

### Toda la cadena mergeada a `develop`

Con los 4 PRs `MERGEABLE`, se mergeó la cadena entera (squash, mismo
patrón que ya usa el repo — todos los commits recientes de `develop` son
de un solo padre): **#300 → #301 → #302 → #303 → #304**, en ese orden.

Cada squash de un eslabón deja "desactualizada" (`not up to date with
base`) la rama del siguiente aunque no haya conflicto real de contenido —
GitHub lo exige por política de la rama protegida. Patrón repetido en
cada paso: `gh pr edit <N> --base develop` (retargetear, las PRs #302-#304
apuntaban a la rama anterior, no a `develop`, al estar apiladas) → `git
merge origin/develop` en la rama → resolver conflicto si lo hay → tests +
push → esperar CI (`gh pr checks <N>`, ~1-2 min) → `gh pr merge <N>
--squash --delete-branch=false` (el repo no borra ramas al mergear,
confirmado con ramas antiguas ya mergeadas que siguen existiendo).

Conflictos encontrados en los reintentos (todos en documentación, ninguno
en código):
- **`Tareas/diario/Día55.md`** (`add/add`, se repitió en 3 de los 4
  merges): cada rama de la cadena tenía su propia copia in-progress del
  diario del día anterior, con contenido divergente según en qué punto de
  la sesión de Día55 se ramificó. Resuelto siempre igual — quedarse con
  la versión más completa (superset) de cada par. Además, al mergear
  `develop` (que traía `Día55.md` en la raíz, nunca archivado en la rama
  de #301) contra `feature/cable-rom-fix-4-platform-exclude` (que ya
  tenía el archivado hecho, commit de apertura de este mismo Día56), Git
  no lo detectó como el mismo archivo movido — lo re-creó en la raíz
  **además** del que ya vivía en `archivo/`, duplicado con contenido
  idéntico. Detectado con `diff`, corregido borrando la copia de la raíz
  (commit `aefd5b5`) antes de seguir con el #303.
- **`Tareas/diario/Día56.md`** (`add/add`, en el último eslabón #304):
  mismo patrón — cada rama tenía su propia versión in-progress de este
  mismo diario (secciones de trabajo realizado añadidas incrementalmente
  a medida que cada tarea se completaba en su propia rama). Resuelto
  quedándose con la versión de HEAD en cada caso (siempre la más
  completa/reciente de las dos).
- **`Tareas/backlog.md`** (conflicto de contenido real, no `add/add`, en
  el último merge): la línea de `DUP-CROSSFMT-1` tenía dos versiones —
  `🔴 pendiente` (la que traía `develop`, de antes de implementarla) y
  `✅ implementado...` (la de esta rama, ya con el resumen final).
  Resuelto quedándose con la versión `✅`.

Verificación final tras el último merge: `git checkout develop` + `git
merge --ff-only origin/develop` (fast-forward limpio, sin nuevos
conflictos) → suite completa **1228/1228** → `Tareas/diario/Día55.md`
confirmado solo en `archivo/`, sin duplicado. `gh pr list --state open`
vacío — cero PRs abiertos, cadena `develop → #300 → #301 → #302 → #303 →
#304` completa y limpia.

Cierre del diario de esta ronda (commit `2cb2eae`, rama
`docs/dia56-housekeeping-cierre`) también pasó por su propio PR (#305,
mismo patrón, checks en verde) — merge limpio, `develop` local
actualizado por fast-forward tras cada uno de los 6 merges de hoy (#300 a
#305).

## `DEDUP-RENAME-2` — investigado

Candidato opcional del triage inicial, retomado tras el housekeeping.
**Conclusión: no es un bug de filtrado** — "Revisar copias" ya incluye
los duplicados de la Anbernic automáticamente, sin ningún selector que
los oculte. Trazado con archivo:línea (documentado completo en
`backlog.md:514`):

- `GET /api/review-queue` (`web/handlers/duplicates.py:161-165`) no
  acepta ningún parámetro de dispositivo/`source_root` — siempre llama a
  `_build_review_queue(repository, repo_android, config)` con los dos
  repos fijos (`repository_android` se crea incondicionalmente en
  `cli.py:467`, nunca es `None`).
- `_build_review_queue`/`_review_groups_for_repo`
  (`web/builders/duplicates.py:602-663`) consulta siempre ambos repos y
  fusiona el resultado en una sola cola (PC y Android nunca se agrupan
  como "el mismo duplicado" entre sí, pero sí aparecen juntos en la
  lista).
- `apiFetch` (`web/static/js/api.js:15-19`) es un `fetch()` desnudo, sin
  inyección de filtros. Confirmado que `tab-plan.html` no tiene ningún
  selector de dispositivo cerca de "Revisar copias" (línea 68).

**Causa probable real de la confusión (UX, no lógica)**: la sección "1.
Plan de renombrado", justo encima en la misma pestaña, sí está filtrada
por dispositivo y muestra un banner explícito "Viendo: PC — ..." /
"Viendo: {consola} — ..." (`organize.js:48-67`). "Revisar copias" no
tiene banner equivalente — el único indicio de que una entrada vive en
la consola es una etiqueta pequeña por fila (`review_copies.js:111-113`,
fácil de pasar por alto). Un usuario viendo "PC" arriba puede asumir
razonablemente que la sección de abajo también está filtrada a PC. Otra
causa posible, no descartable sin biblioteca real: si la Anbernic nunca
se escaneó desde la app web, `library_android.db` está vacío y no habría
nada de consola que mostrar, con razón.

Sin cambio de código en la investigación (regla del proyecto de
investigar antes de arreglar). Follow-up de UX documentado como
`DEDUP-RENAME-2b` — ver implementación abajo.

## `DEDUP-RENAME-2b` — banner de UX implementado

Segundo triage del día (agente `backlog-triage`, tras cerrar el
housekeeping y `DEDUP-RENAME-2`) propuso 3 candidatos nuevos:
`DUP-DISC-RA-1c` (hash RA de disco para Saturn/Dreamcast, esfuerzo
medio-alto), el propio banner de `DEDUP-RENAME-2b` (esfuerzo bajo, cierra
el hilo de hoy), y desambiguar `.md` sueltos por CRC (idea sin ID formal
en `ZIP-ROUTE-FIX-3`). Elegido el banner por ser la continuación más
directa de la investigación recién hecha.

Implementación: nuevo `<div id="review-queue-context-bar">` en
`tab-plan.html` (mismo patrón visual que `plan-context-bar`, oculto por
defecto vía clase `hidden`), justo encima de `review-queue-content`.
`loadReviewQueue()` (`review_copies.js`) lo puebla con "Combinando PC +
{consola}" reutilizando el global `window._devName` ya mantenido por
`state.js` — sin necesidad de un fetch extra a `/api/config`. Mismo
`nbsp;·&nbsp;` y estilo (`color:var(--c-dim)`) que ya usa el banner de
"1. Renombrar" para el texto secundario.

Sin tests nuevos — cambio puramente visual/textual (un `innerHTML` con
texto fijo + una variable global), sin rama lógica que valga la pena
testear con datos sintéticos. Verificado sirviendo el HTML/JS reales por
`curl` contra el servidor levantado en local (`rommgr serve`): el nuevo
`<div>` y la referencia a `review-queue-context-bar` en el JS llegan tal
cual al navegador. No se pudo verificar visualmente en Chrome (extensión
no conectada en este ordenador, mismo límite que el resto de la sesión).
Suite completa **1228/1228** (sin cambios — no hay tests Python que
tocar en un cambio solo de frontend).

## Archivos modificados (`DEDUP-RENAME-2b`)

- `src/rom_manager/web/static/partials/tab-plan.html`
  (`review-queue-context-bar` nuevo).
- `src/rom_manager/web/static/js/tabs/review_copies.js`
  (`loadReviewQueue` puebla el banner).
- `Tareas/backlog.md` (`DEDUP-RENAME-2b` nueva, marcada `✅`).

`DEDUP-RENAME-2b`: commit `54be72e` (+ `58fdfc5` de diario) en rama
`feature/dedup-rename-2b-review-queue-banner` (creada limpia desde
`develop`, ya sin ramas apiladas pendientes), push, **PR #306** — checks
en verde (`Lint`, `Tests` 3.11/3.12, `claude-review`), squash-merge a
`develop`, `git pull --ff-only` local limpio.

## Estado al cierre de la ronda 1

Ronda 1 completa, sin trabajo a medias. **7 PRs mergeados a `develop` hoy**
(#300 a #306), cero PRs abiertos, `develop` local al día por fast-forward
en cada uno de los 7 merges. Suite completa **1228/1228** en el estado
final de `develop`. Ningún cambio de código quedó sin verificar con tests
(salvo `DEDUP-RENAME-2b`, puramente visual — verificado sirviendo el
HTML/JS reales por `curl`) ni sin pasar por CI real (ruff + pytest 3.11/
3.12 + `claude-review` en cada PR).

Resumen de lo cerrado en la ronda 1:

- **Objetivo 0 (housekeeping)** — conflictos de merge de PR #300/#301
  contra `develop` (arrastrados desde Día55) resueltos; 4 conflictos más
  encontrados y resueltos durante la propia cadena de merges de hoy
  (ver sección "Housekeeping" arriba), todos en documentación.
- **`CABLE-ROM-FIX-4`** (Pilar 3) — selector de plataformas a excluir en
  Cable Sync. PR #302.
- **`ZIP-ROUTE-6`** (Pilar 2) — aplanar sub-sistemas arcade al extraer
  colecciones. PR #303.
- **`DUP-CROSSFMT-1`** (Pilar 1) — detección de duplicados que cruzan
  formato de archivo. PR #304.
- **`DEDUP-RENAME-2`** — investigación pura (no bug, hallazgo de UX
  documentado) + **`DEDUP-RENAME-2b`** — banner implementado. PR #306.

Todo verificado solo con datos sintéticos y tests — sesión desde un
ordenador sin biblioteca real ni Anbernic ni extensión de Chrome
disponibles, mismo condicionante en las 4 tareas de código del día.

## Ronda 2 — auditoría de issues + `DUP-DISC-RA-1c`

### Auditoría: alineación `backlog.md` ↔ issues de GitHub

Petición del usuario: comprobar que las tareas del backlog están
alineadas con los issues `epic` de GitHub y salen de ellos. Trazados los
13 issues `epic` (`gh issue list --label epic --state all`) contra los 9
epígrafes `## ` con `→ #NNN` y los ~46 sub-epígrafes `### ` que cuelgan de
ellos (o de `## Transversal — ... (sin epic)`, explícitamente marcada
así). Estructura general correcta — nada huérfano fuera de esa jerarquía.

3 desalineaciones reales encontradas y arregladas:

1. **`#203` (Pilar 2 — Inbox automático) estaba `CLOSED`** pero
   `backlog.md` seguía con 3 tareas pendientes bajo ese epígrafe
   (`ZIP-ROUTE-7` parcial, `INBOX-RA-HASH-GAP`, `INBOX-ORPHAN-5`) — el
   issue se generó automáticamente desde el backlog el 2026-08-14 y nadie
   lo reabrió al añadir esas 3 tareas después. **Reabierto**, con
   comentario explicando el motivo.
2. **`#206` (UX — Auditorías por pestaña, 2/2) estaba `CLOSED`** con 1
   pendiente (`HERR-FIX-4`, bug de layout sin confirmar visualmente).
   **Reabierto**, mismo motivo.
3. **`#275` (LIBRARY-AUDIT) llevaba la etiqueta `enhancement`, no
   `epic`** — salta la convención propia del `CLAUDE.md`. **Añadida la
   etiqueta `epic`** (se queda con las dos).

Sin cambios de código ni en `backlog.md` — solo en GitHub
(`gh issue reopen`/`gh issue edit --add-label`, verificado con
`gh issue view` tras cada cambio).

### `DUP-DISC-RA-1c` implementado

Candidato del segundo triage del día. Investigación directa en la fuente
de rcheevos (`github.com/RetroAchievements/rcheevos`, `curl` a
`src/rhash/hash_disc.c` y `cdreader.c` — no hay `rc_hash_saturn`
dedicado, Saturn usa la misma `rc_hash_sega_cd()` que Sega CD).

Hallazgo que simplificó el trabajo frente a lo esperado en el triage:
Saturn **no** hace boot-exe lookup como PSX — RA solo hashea el header
crudo de 512 bytes al principio de la pista 1 (comentario propio de
rcheevos: "hashing the volume and ROM headers is sufficient"), sin tocar
el sistema de archivos. Dreamcast sí es más parecido a PSX: hashea 256
bytes de IP.BIN (**pista 3**, no la 1 — GD-ROM) + el ejecutable de
arranque (nombre en el offset 96 de IP.BIN), localizado por el mismo
lookup ISO9660 genérico que ya usaba PSX — confirmado en la fuente:
`rc_cd_find_file_sector` es la misma función para todas las consolas,
PSX incluida, nunca fue específica de PSX pese a vivir solo en
`ra_hash_psx.py` hasta hoy.

**Refactor previo sin cambio de comportamiento**: el lector de sectores
CD (`_CdImage`, detección de geometría) y el buscador ISO9660
(`_find_root_file`/`_find_entry`/`_root_dir_location`) se extrajeron de
`ra_hash_psx.py` a un módulo nuevo compartido, `retroachievements/
ra_cd_image.py` — las tres consolas usan literalmente el mismo algoritmo
de lectura de sectores, triplicarlo habría sido puro copy-paste. También
se movieron `_first_cue_bin`/`_extract_chd` (generalizado para aceptar
`.cue` o `.gdi` de salida) y la constante `_MAX_HASH_SIZE`. Los 7 tests
de `ra_hash_psx` siguen en verde tras cada paso del refactor (verificado
antes de seguir, no al final).

Módulo nuevo `retroachievements/ra_hash_saturn_dreamcast.py`:
`compute_saturn_ra_hash()` (`.bin`/`.cue`/`.chd`) y
`compute_dreamcast_ra_hash()` (`.bin`/`.cue`/`.chd`/`.gdi` — parser
propio de `.gdi` para resolver la pista 3 **por número**, no "la última"
ni "la más grande", ya que en GD-ROM cada pista es normalmente su propio
archivo).

7 tests nuevos con imágenes sintéticas (mismo patrón que
`test_ra_hash_psx.py`: sectores raw de 2352 bytes construidos a mano) —
a diferencia de PSX (124/259 hashes reales verificados contra la caché
de RA en `DUP-DISC-RA-1a`), **sin verificar todavía contra caché RA
real** — no hay biblioteca Saturn/Dreamcast en esta máquina. La
corrección del parseo de bytes está probada; la fidelidad al hash real
de RA, no.

**Fuera de esta tarea a propósito**: wirear el hash hasta
`ra_checker.py`/`ra_disc_hash_cache.py` (equivalente a la "parte 1" de
`DUP-DISC-RA-1b` para PSX) — el pedido original decía "mismo algoritmo",
no el wiring, y el propio `DUP-DISC-RA-1b` ya separa esa distinción en
el backlog. Candidato natural para una `DUP-DISC-RA-1d` futura.

Verificación: suite completa **1235/1235**. `ruff check`/`ruff format
--check` limpios en todos los archivos tocados.

### Archivos modificados (`DUP-DISC-RA-1c`)

- `src/rom_manager/retroachievements/ra_cd_image.py` (nuevo — lector de
  sectores CD + buscador ISO9660 compartido).
- `src/rom_manager/retroachievements/ra_hash_saturn_dreamcast.py` (nuevo
  — `compute_saturn_ra_hash`, `compute_dreamcast_ra_hash`).
- `src/rom_manager/retroachievements/ra_hash_psx.py` (refactorizado para
  usar `ra_cd_image.py`, sin cambio de comportamiento — 7 tests
  existentes en verde).
- `tests/test_ra_hash_saturn_dreamcast.py` (nuevo, 7 tests).
- `Tareas/backlog.md` (`DUP-DISC-RA-1c` marcado `✅`).

## Estado al finalizar

Ronda 2 completa. Además de los 7 PRs de la ronda 1, hoy también se
reabrieron 2 issues de GitHub (`#203`, `#206`) y se corrigió la etiqueta
de otro (`#275`) — cambios directos en GitHub, sin PR de código. Queda
`DUP-DISC-RA-1c` implementado y probado (suite 1235/1235) pero **sin
commit todavía** — pendiente antes de dar la ronda 2 por cerrada.

## Siguiente sesión recomendada

1. Commit + push + PR de `DUP-DISC-RA-1c` (rama nueva desde `develop`,
   mismo patrón que el resto del día).
2. Cuando se trabaje desde una máquina con biblioteca real: correr
   "Revisar copias" y confirmar cuántos de los 41 casos manuales
   (13,5 GB, `DUP-CROSSFMT-1`) detecta ahora la razón `"crossfmt"`
   automáticamente — sin esa verificación, el hallazgo original sigue
   siendo la única evidencia de que el patrón es real. También validar
   `ZIP-ROUTE-6` contra el `fbneo_1003_bestset` real si se vuelve a
   descargar, confirmar en vivo si `library_android.db` tiene datos
   reales (relevante para el hallazgo de `DEDUP-RENAME-2`), si el
   banner de `DEDUP-RENAME-2b` se ve bien en el navegador, **y** si
   `compute_saturn_ra_hash`/`compute_dreamcast_ra_hash` coinciden con la
   caché de RA real (mismo tipo de validación que ya se hizo para PSX en
   `DUP-DISC-RA-1a`, pendiente aquí).
3. Candidato del segundo triage que quedó sin empezar: desambiguar los
   177 `.md` sueltos del Inbox por CRC32 en vez de
   contexto de carpeta (idea sin ID formal, mencionada al cierre de
   `ZIP-ROUTE-FIX-3`, `backlog.md:620`).
4. Repo en estado limpio para empezar la próxima sesión sin ramas
   apiladas ni deuda de merge pendiente (una vez commiteado/mergeado
   `DUP-DISC-RA-1c`).

## Instrucciones paso a paso para los 4 puntos anteriores

### 1. Commit + push + PR de `DUP-DISC-RA-1c`

Mismo patrón que el resto de tareas de hoy (rama nueva desde `develop`,
no apilada — la cadena de hoy ya está mergeada entera):

1. `git checkout develop && git pull --ff-only`.
2. `git checkout -b feature/dup-disc-ra-1c-saturn-dreamcast-hash`.
3. `git add src/rom_manager/retroachievements/ra_cd_image.py
   src/rom_manager/retroachievements/ra_hash_saturn_dreamcast.py
   src/rom_manager/retroachievements/ra_hash_psx.py
   tests/test_ra_hash_saturn_dreamcast.py Tareas/backlog.md
   "Tareas/diario/Día56.md"` (revisar `git status` antes de que no se
   cuele nada inesperado).
4. `ruff check . && ruff format --check .` sobre los archivos tocados;
   suite completa (`pytest`) — debe seguir en 1235/1235 o más si se
   añade algo nuevo.
5. Commit con mensaje que resuma lo ya redactado en `backlog.md` para
   `DUP-DISC-RA-1c` (algoritmo Saturn/Dreamcast + refactor compartido
   `ra_cd_image.py`).
6. Push + `gh pr create --base develop` (patrón de PRs simples del día,
   sin apilar — no reutilizar `--base` de otra rama).
7. Esperar CI (`gh pr checks <N>`, ruff + pytest 3.11/3.12 +
   `claude-review`) y mergear con `gh pr merge <N> --squash
   --delete-branch=false` (el repo no borra ramas).
8. `git checkout develop && git merge --ff-only origin/develop` para
   dejar el local al día.

### 2. Validación contra biblioteca real (solo desde una máquina con
   `F:\Juegos Retro` / `E:\Carpetas anbernic` / `H:\ROMs` montada)

Cuatro verificaciones independientes, ninguna requiere tocar código si
el resultado es el esperado — solo si algo falla se abre una tarea
`-fix` nueva:

1. **`DUP-CROSSFMT-1`** — abrir "Revisar copias" en la UI y contar
   cuántas entradas traen razón `crossfmt`. Comparar contra los 41
   casos manuales (13,5 GB) que motivaron la tarea. Si el número queda
   muy por debajo, revisar `_normalize_title_cross_format`
   (`web/builders/duplicates.py`) contra 2-3 casos reales que no
   aparezcan.
2. **`ZIP-ROUTE-6`** — si se vuelve a descargar `fbneo_1003_bestset` de
   archive.org, pasarlo por el junk-scan → "Organizar identificados"
   real (no dry-run) y confirmar que las 4 sub-carpetas
   (`cps1`/`cps2`/`cps3`/`neogeo`/`fbneo`/`toaplan_cave_stg`) caen
   planas en `arcade\`, sin `arcade\games\<subsistema>\`.
3. **`DEDUP-RENAME-2`** — comprobar si `library_android.db` tiene datos
   (la Anbernic se ha escaneado alguna vez desde la app web). Si está
   vacío, ese es el motivo real de por qué "Revisar copias" no mostraba
   nada de consola — no haría falta ningún cambio de código adicional
   al de `DEDUP-RENAME-2b`. Aprovechar para mirar en Chrome que el
   nuevo banner "Combinando PC + {consola}" se ve bien en
   `tab-plan.html`.
4. **`DUP-DISC-RA-1c`** — repetir la validación que ya se hizo para PSX
   en `DUP-DISC-RA-1a` (259 `.bin`/`.cue` reales, 124 coincidieron con
   `ra_hashes_12.json`): correr `compute_saturn_ra_hash`/
   `compute_dreamcast_ra_hash` contra los `.bin`/`.cue`/`.chd`/`.gdi`
   reales de Saturn y Dreamcast de la biblioteca y comparar contra la
   caché de RA (`ra_hashes_<console_id>.json`). Si no coincide ningún
   hash, revisar el offset del header (Saturn, 512 bytes pista 1) o el
   parseo de IP.BIN/`.gdi` (Dreamcast, pista 3) antes de dar el
   algoritmo por bueno.

### 3. `ZIP-ROUTE-FIX-4` (candidato, sin ID formal todavía) — CRC32 para
   los 177 `.md` sueltos del Inbox

Sin biblioteca real no se puede completar (hace falta el índice CRC32
de los DATs de Mega Drive contra los `.md` reales), pero sí se puede
dejar preparado con datos sintéticos:

1. Formalizar el ID en `backlog.md` bajo la sección Pilar 2 (junto a
   `ZIP-ROUTE-FIX-3`, `backlog.md:620`), con la tabla de tareas ya
   existente ahí.
2. Reutilizar `CatalogMatcher.crc_index()` (ya usado por `ZIP-ROUTE-1`
   para ZIPs sueltos) contra los `.md` del Inbox: si el CRC32 del
   archivo coincide con una entrada del DAT de Mega Drive, mover/
   renombrar aunque no haya contexto de carpeta; si no matchea ningún
   DAT, se queda ambiguo tal cual hoy (no hay peor caso — nunca clasifica
   mal algo que hoy queda sin tocar).
3. Test sintético: `.md` con contenido cuyo CRC32 coincide con una
   entrada falsa de un DAT de prueba → se clasifica como Mega Drive sin
   contexto de carpeta; `.md` sin match → sigue en el Inbox sin cambios
   (mismo patrón que los tests existentes de `ZIP-ROUTE-FIX-3`).
4. Verificación final contra los 177 reales solo posible desde una
   máquina con el Inbox real poblado — documentar el conteo antes/
   después en el backlog al cerrar la tarea.

### 4. Repo limpio (consecuencia del punto 1, no una acción aparte)

Una vez mergeado el PR de `DUP-DISC-RA-1c` (punto 1), confirmar:

1. `gh pr list --state open` → vacío.
2. `git branch --show-current` → `develop`, sin ramas locales a medias
   con cambios sin commitear (`git status` limpio).
3. `git log --oneline -1` en `develop` coincide con el último commit
   mergeado del PR de `DUP-DISC-RA-1c`.

Si los 3 se cumplen, no hace falta ninguna acción extra — es solo la
confirmación de que la sesión anterior no dejó nada a medias antes de
empezar la siguiente.

## Ronda 3 (nueva sesión) — punto 1 ejecutado, punto 2 parcialmente validado

### Punto 1: `DUP-DISC-RA-1c` commiteado y mergeado

Ejecutados los pasos documentados arriba sin desviaciones: rama
`feature/dup-disc-ra-1c-saturn-dreamcast-hash` desde `develop` limpio,
commit `7bbf861`, push (pre-push necesitó `conda` además de
`System32` en el `PATH` de Bash — el hook `pytest` lo requiere,
apuntado en memoria), **PR #308**, 4 checks en verde (`Lint`, `Tests`
3.11/3.12, `claude-review`), squash-merge a `develop` (`1d527b4`),
`develop` local al día tras `git fetch` + `merge --ff-only` (el primer
intento de fast-forward no trajo nada nuevo por no haber hecho fetch
antes — corregido en el segundo intento).

### Punto 2: validación con biblioteca real — **la biblioteca SÍ estaba
montada en esta máquina**, corrección sobre lo asumido en la sesión
anterior

El contexto original de Día56 decía "sin `F:\Juegos Retro` de esta
máquina" — comprobado directamente (`ls "F:\Juegos Retro"`) y es falso
para esta sesión: la biblioteca real está montada y `load_config()`
confirma `library_root = F:\Juegos Retro`. Validación ejecutada
llamando a las funciones del backend directamente en Python (sin UI,
sin extensión de Chrome — sigue sin estar conectada) contra
`.rommgr/library_pc.db`/`library_android.db` reales:

1. **`DUP-CROSSFMT-1` — validado, funciona correctamente**:
   `_build_review_queue()` real da **2.684 grupos `crossfmt`, 17,28 GB**
   (Game Boy 1.417, NES 982, Game Gear 185, PlayStation 97, GBA 2, PS2
   1) — muy por encima de los 41 casos/13,5 GB estimados a mano que
   motivaron la tarea. La cifra alta tiene una causa real, no es un
   falso positivo: `F:\Juegos Retro\gb\GB official game ROM complete
   works\A\` es una subcarpeta que duplica TODO el contenido de `gb\`
   un nivel más adentro (5.590 archivos, 3,22 GB, confirmado contando
   filas de `library_pc.db` con esa ruta) — basura real de organización
   (Pilar 1) que la función ahora expone correctamente para limpieza
   desde "Revisar copias". Nota de depuración propia: un primer intento
   de contar los grupos leyó `reasons` desde cada entrada individual en
   vez de desde el grupo (`g['reasons']` es la clave correcta, no
   `entry['reasons']`, que no existe) y dio 0 — error del script de
   verificación, no del código de producción; confirmado repitiendo
   contra `_review_groups_for_repo()` directamente sobre un caso
   conocido antes de corregir el script.
2. **`ZIP-ROUTE-6`** — no hay ningún `fbneo_1003_bestset` (ni ningún
   ZIP de colección FBNeo) descargado en esta máquina; las carpetas
   `cps1/fbneo`, `cps2/fbneo`, `CPS3/fbneo` que aparecen en
   `F:\Juegos Retro` son la organización normal por núcleo, no el
   contenedor a extraer. Sigue sin poderse validar aquí.
3. **`DEDUP-RENAME-2` — confirmado, no es un bug**: `library_android.db`
   tiene **0 filas** (`SELECT COUNT(*) FROM games` sobre el archivo
   real) — la Anbernic nunca se ha escaneado desde la app web en esta
   máquina, exactamente la segunda causa apuntada en la investigación
   original.
4. **`DUP-DISC-RA-1c` — validación real BLOQUEADA por un gap real,
   documentado como `DUP-DISC-RA-1e` en el backlog**: Saturn no tiene
   ningún ROM real en esta biblioteca (`saturn\media\images`/`videos`
   son artefactos del scraper, no juegos). Dreamcast tiene 5 juegos
   reales, **todos en `.cdi`** (Crazy Taxi 2, Dead or Alive 2 Beta,
   Legacy of Kain - Soul Reaver, Marvel Vs Capcom 2, Sonic Adventure —
   los `.A1.bin`/`vmu_save_*.bin` sueltos son saves de VMU, no
   imágenes de disco). `compute_dreamcast_ra_hash()`
   (`retroachievements/ra_hash_saturn_dreamcast.py:145-162`) solo
   reconoce `.gdi`/`.cue`/`.bin`/`.chd` — devuelve `None` para `.cdi`
   sin intentarlo. La corrección del algoritmo sigue sin verificar
   contra la caché RA real; añadir soporte `.cdi` (formato
   DiscJuggler, layout de cabecera/pistas distinto de un `.bin`/`.cue`
   crudo) es requisito previo. Sin implementar en esta sesión (regla
   de investigar antes de arreglar) — candidato `DUP-DISC-RA-1e`
   documentado en el backlog con el archivo:línea exacto.

Sin cambios de código en esta ronda — solo lectura (consultas SQL de
solo lectura contra las BD reales, llamadas a funciones puras del
backend sin efectos secundarios) y actualización de `backlog.md`
(`DUP-CROSSFMT-1` con los resultados reales, `DUP-DISC-RA-1e` nueva).

### Punto 3: sin avanzar — Inbox real vacío en esta máquina

`F:\Juegos Retro\inbox` tiene 0 archivos en total (no solo 0 `.md`) —
tampoco se puede validar aquí con datos reales; sigue pendiente la
preparación con datos sintéticos descrita en las instrucciones de
arriba, sin ejecutar todavía.

## Corrección sobre el punto 3 — `ZIP-ROUTE-FIX-4` ya estaba implementado (`AUD-4`)

Al ir a implementarlo (petición del usuario "el punto 3"), antes de
escribir código se comprobó `web/inbox_pipeline.py` por si ya existía
algo parecido — **sí existe, completo**: `_resolve_ambiguous_md()`
(`inbox_pipeline.py:423-490`), wireado como paso 1.7 del pipeline real
(`inbox_pipeline.py:1070`), exactamente el diseño planeado (CRC32 de
cada `.md` sin contexto de carpeta contra `CatalogMatcher.crc_index()`,
hit de Mega Drive → `inbox/megadrive/`, miss → se deja quieto). 5 tests
en `tests/test_inbox_md_crc.py`, confirmados en verde hoy. Implementado
en **PR #115** (`10d208c`, 2026-07-13) bajo el ID **`AUD-4`** — de un
roadmap distinto (`Tareas/Roadmap-Auditoria.md`), nunca se añadió como
fila a `backlog.md`, así que quedó invisible para el triage y para el
diario de hoy (que lo describió como "candidato sin ID formal
todavía"). Corregido: fila `AUD-4` añadida a `backlog.md` junto a
`ZIP-ROUTE-FIX-3`.

Lo único que de verdad sigue pendiente (ya lo decía el propio PR #115
en su día) es **ejecutar el pipeline sobre la biblioteca real** para
reorganizar los 177 `.md` varados — y tampoco se puede hacer desde
esta máquina, `F:\Juegos Retro\inbox` está vacío aquí (0 archivos); los
177 viven en el Inbox de la otra máquina (`H:\ROMs`/
`E:\Carpetas anbernic`). Sin cambios de código en esta ronda — solo
`backlog.md` (fila `AUD-4` nueva).
