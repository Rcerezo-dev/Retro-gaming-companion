# ROM Manager Local — Día 59

## Contexto

Sesión de continuación del Día58. Al abrir había WIP sin commitear del cierre
de ayer (`DUP-CROSSFMT-4/5/6`, `TRASH-FIX-3/4`, diario Día58 sin trackear) —
commiteado primero en 3 commits limpios antes de empezar tarea nueva.
Triage de hoy propuso: aplicar `resolve-duplicates --apply` sobre PSX
(`DUP-CROSSFMT-4`), resolver los 145 conflictos de tamaño exacto empatado de
`ARCADE-DAT-CONTAMINATION-8`, y si sobraba tiempo extender el `--apply` a
Game Boy/NES/Game Gear.

## Objetivos del día

- Commitear el WIP del Día58.
- `DUP-CROSSFMT-4`: aplicar `resolve-duplicates --apply` filtrado a PSX.
- `ARCADE-DAT-CONTAMINATION-8`: resolver los 145 empates de tamaño exacto.
- Si sobra tiempo: extender el apply a Game Boy/NES/Game Gear.

## Trabajo realizado

### 1. WIP del Día58 commiteado
3 commits: `DUP-CROSSFMT-4/5/6` (fix), `TRASH-FIX-3/4` (feat), docs
(backlog+diario Día58).

### 2. `DUP-CROSSFMT-4` — nada aplicable en PSX, hallazgo nuevo
Filtrando la cola en vivo a `platform == "PlayStation"`: 0 grupos "planos"
aplicables — los 5 grupos de conflicto son `disk`/`collision`, nunca se
auto-resuelven en PSX (`_MULTI_DISC_RISK_PLATFORMS`, protección correcta).
Investigando uno de ellos (`Crash Bandicoot`) se encontró **CATALOG-MATCH-BUG-2**:
49 bootlegs NES sin licencia tienen `platform` mal asignado en `library_pc.db`
(PlayStation, Wii, Sega Saturn...) por un caso sin cubrir del fallback de
título en `catalog/matcher.py:291-297` (cuando `platform_hits` sale vacío tras
filtrar por `ext_platform`, cae a `candidates = hits` sin filtrar). Documentado
en el backlog, sin arreglar (regla "investigar antes de arreglar").

### 3. `ARCADE-DAT-CONTAMINATION-8` → `-12` — 168 conflictos resueltos
Re-medición en vivo: los "145 empates" habían derivado a 168 conflictos
totales. Investigando por qué el criterio "más grande gana" no los resolvía,
se encontró que `_same_content()` (`web/inbox_pipeline.py:23-37`) compara SHA1
del ZIP completo en vez del CRC32 de cada entrada — **90/168 (54%) eran falsos
positivos**, ZIPs re-empaquetados con contenido idéntico. Aplicado sobre la
biblioteca real (backup previo de `library_pc.db`): CRC idéntico/NES → gana lo
ya organizado; tamaño distinto → gana el más grande; empate real no-NES →
gana lo ya organizado por defecto. **160 kept_dest, 8 kept_source, 0 errores.
0 conflictos restantes**, verificado con re-consulta.

### 4. `DUP-CROSSFMT-7` — extendido a Game Boy/NES/Game Gear/Mega Drive
La medición de `DUP-CROSSFMT-1` (Game Boy 1.417, etc.) era contra `F:\Juegos
Retro`, una biblioteca distinta — no comparable con la activa (`E:\Carpetas
anbernic`). En vivo: 2.289 grupos en esas 4 plataformas, **2.286 son
conflictos `disk`/`collision` sin ningún dato de RA** (muestreado 400/2.286,
0 con RA) — `apply_ra_conflicts()` no resolvería nada ahí hoy, y la mezcla
real de parches de traducción/hack/versión arcade en los mismos grupos hace
arriesgado forzar un criterio ciego (necesita investigación propia, no hecha
hoy). Solo 3 grupos "planos" (Game Gear): 2 duplicados reales aplicados
(verificados por CRC interno `.zip` == `.gg`), 1 bloqueado — **hallazgo real**:
`Phantasy Star Adventure` agrupaba el parche de traducción al inglés con el
original japonés (SHA1 distinto, mismo tamaño) porque `normalize_for_match()`
(`detection/filename_normalizer.py:24-49`) borra cualquier anotación entre
corchetes sin distinguir tag de región de tag de parche de traducción.
Documentado, sin tocar el normalizador.

### 5. `DUP-CROSSFMT-8` — Mega Drive, 1.407 grupos categorizados y 772 aplicados
El usuario pidió investigar el bloque grande de Mega Drive. Categorizados con
`sha1` ya guardado en BD (sin re-hashear) + CRC interno de cualquier `.zip`
del grupo (misma técnica que `ARCADE-DAT-CONTAMINATION-12`): **772 grupos
(55%) verificados al 100%** como el mismo contenido real en todas sus
entradas — aplicados con `resolve_duplicate_ra` (backup previo). **773
archivos descartados, 0 errores. 635 grupos restantes**, cuadra exacto.

### 6. `DUP-CROSSFMT-9` — extendido a NES/Game Boy/Game Gear, 168 aplicados
Misma metodología sobre los 879 grupos de conflicto restantes de esas 3
plataformas: **168 verificados y aplicados** (NES 156, Game Boy 1, Game Gear
11) — **263 archivos descartados, 0 errores**. Cobertura mucho menor que
Mega Drive (19% vs 55%) por dos hallazgos: el regex de hack/traducción de la
sesión solo cazaba una fracción de los tags `[T+xxx]`/`[T-xxx]` reales (96/391
grupos NES "variante" llevaban ese tag con código de idioma no cubierto —
un solo *Zelda* tenía 22 "duplicados" que en realidad eran traducciones
legítimas distintas); y un grupo Game Boy mezclaba el juego real con el
arcade "Asterix" de Konami (mismo mecanismo que `CATALOG-MATCH-BUG-2`).

### 7. Fix de raíz: `CATALOG-MATCH-VARIANT-1`/`CATALOG-MATCH-BUG-2`
El usuario pidió implementar el fix propuesto. Escrito en rama
`fix/catalog-match-noncanonical-variants`, PR #313, mergeado tras CI verde:
- Nuevo `is_non_canonical_variant()` en `detection/filename_normalizer.py`.
- `catalog/matcher.py::_match_by_title()`: el guard de `CATALOG-MATCH-BUG-2`
  cubre tanto el caso de un único hit (el que realmente disparaba el caso
  real — `len(hits)==1` nunca comprobaba plataforma) como el de varios.
- `web/builders/duplicates.py`: la unión por título exacto/crossfmt excluye
  archivos marcados como variante; sha1 sigue uniendo copias idénticas.
- `planner/operation_planner.py::_canonical_filename()`: causa real del
  "collision" — una variante ya no calcula el mismo nombre destino que el
  original, así que no compite por la misma ruta en `build_plan()`.
- 12 tests nuevos + 1 corregido (DAT sintético sin nombre reconocible →
  nombre realista). Suite 1269/1272 (3 fallos preexistentes ADB).
- Durante el merge, CI falló primero por 4 archivos ajenos al PR
  (`ruff` sin versión fijada en el workflow) — reformateados en un commit
  aparte. Al sincronizar `develop` local con el remoto se descubrió que todo
  el trabajo de la sesión (puntos 1-6) nunca se había pusheado — verificado
  con `git diff` en ambas direcciones que `origin/develop` era superset
  exacto antes de sincronizar, sin pérdida de nada.

### 8. Re-corrida tras el fix — 87 + 367 duplicados más aplicados
Con el fix ya en `develop`: la cola de las 4 plataformas bajó de 711 a 622
grupos de conflicto y aparecieron **111 grupos "planos" nuevos** — duplicados
reales antes enterrados detrás del falso conflicto de renombrado. **87
verificados y aplicados**, 24 correctamente descartados. Después, resolución
**por entrada** (no por grupo) sobre los 418 conflictos que quedaban en Mega
Drive: dentro de cada grupo, busca sub-clusters de contenido idéntico entre
las entradas NO marcadas como variante, conserva una, descarta el resto,
nunca toca la traducción/hack del mismo grupo. **356/418 grupos con
sub-duplicado, 367 archivos descartados**. Mega Drive terminó en **67
conflictos** (desde 1.407 al empezar el día).

### 9. `CATALOG-MATCH-BUG-2` — medición en `library_android.db`
Mismo query (`detect_platform()` vs `platform` en BD) sobre las 621 filas con
`platform` asignado: **0 mismatches** — biblioteca sin el problema (mucho más
pequeña, 665 filas, menos expuesta a bootlegs).

### 10. `INBOX-STALE-1` — auditoría inicial del Inbox sin procesar
Solo lectura, sin tocar el disco mientras corría el Cable Sync en paralelo.
`E:\Carpetas anbernic\inbox\` tiene **44.611 archivos, 185 GB**, de los que
solo 450 (1%, 1,4 GB) están indexados en la BD. 35 carpetas de primer nivel:
colecciones bulk sin organizar, carpetas ya organizadas por plataforma nunca
movidas, residuo de investigaciones pasadas, y al menos un archivo claramente
ajeno al retro. Sin `organize-source` — pendiente para cuando el disco esté
libre.

### 11. Cable Sync PC→RG556
El usuario pidió llevar los cambios de dedup al dispositivo. El primer
dry-run (`android_path=/storage/emulated/0`, ruta genérica de saves) dio un
resultado absurdo (36.508 archivos/531 GB) — la ruta real de ROMs en la SD es
`/storage/521D-04EA/ROMs` (verificado por ADB). Repetido con la ruta
correcta: el modo espejo (`delete_extra=true`) proponía descartar **974
archivos del dispositivo, varios juegos reales** (`Silent Hill`, `Xenogears`,
`Vagrant Story`...) — causa raíz verificada: el PC tiene `Silent Hill
(USA).chd` (convertido) pero el dispositivo sigue en `.bin`/`.cue`, y la
comparación por ruta exacta no reconoce que es el mismo juego. **No se
ejecutó el espejo.** El usuario eligió "solo copiar, sin borrar" — pero
531 GB no cabían en los 294 GB libres de la SD. Desglosado por carpeta
(`ps2` 199 GB + `psx` 72,9 GB + `gamecube` 63,7 GB ya superaban el espacio
libre ellos solos; `inbox`/`Unknown` no deberían sincronizarse tal cual).
Acotado a las 5 carpetas del dedup de hoy (megadrive/nes/gb/gbc/gamegear,
12,14 GB) y ejecutado de verdad: **14.968 copiados, 0 borrados, 1 error
transitorio** (BIOS shader, ADB se reinició a mitad, se recuperó solo).
Verificado por ADB: 22.456 archivos en esas 5 carpetas en el dispositivo.

## Archivos modificados

- `src/rom_manager/services/ra_duplicates_service.py`,
  `src/rom_manager/web/builders/duplicates.py` (commit WIP Día58,
  `DUP-CROSSFMT-4/5/6`)
- `src/rom_manager/sync/adb_transport.py`, `web/daemons.py`,
  `web/handlers/esde/maintenance.py`, `web/state.py`, `web/static/js/main.js`,
  `web/static/js/tabs/config.js`, `web/static/partials/tab-settings.html`
  (commit WIP Día58, `TRASH-FIX-3/4`)
- `src/rom_manager/detection/filename_normalizer.py` (nuevo
  `is_non_canonical_variant()`), `src/rom_manager/catalog/matcher.py`
  (`CATALOG-MATCH-BUG-2`, un único hit y varios), `src/rom_manager/web/
  builders/duplicates.py` (guard en unión título/crossfmt),
  `src/rom_manager/planner/operation_planner.py` (`_canonical_filename`) —
  PR #313, rama `fix/catalog-match-noncanonical-variants`
- `tests/test_filename_normalizer.py`, `tests/test_catalog_matcher.py`,
  `tests/test_builders_duplicates.py`, `tests/test_operation_planner.py`
  (12 tests nuevos + 1 corregido)
- `Tareas/backlog.md` (muchos commits a lo largo del día: cierre Día58,
  `CATALOG-MATCH-BUG-2`, `ARCADE-DAT-CONTAMINATION-12`, `DUP-CROSSFMT-7/8/9`,
  `INBOX-STALE-1`)
- `library_pc.db` (no versionado): miles de duplicados descartados a lo
  largo del día (168 + 772 + 168 + 87 + 367 ≈ 1.562 archivos entre todas las
  pasadas)
- Backups en `.rommgr/`: `backup_arcade_dat_contamination12_20260909/`,
  `backup_megadrive_dupcrossfmt7_20260909/`,
  `backup_nes_gb_gg_dupcrossfmt9_20260909/`,
  `backup_postfix_plain_groups_20260909/`, `backup_megadrive_subdup_20260909/`
- Dispositivo RG556 (vía ADB, Cable Sync real): 14.968 archivos copiados a
  `/storage/521D-04EA/ROMs/{megadrive,nes,gb,gbc,gamegear}`

## Estado al finalizar

- `ARCADE-DAT-CONTAMINATION-8/-12`: ✅ cerrado, 0 conflictos restantes en
  `Unknown/`.
- `CATALOG-MATCH-BUG-2`: ✅ arreglado y mergeado (PR #313), alcance medido en
  ambas BD (0 mismatches en Android).
- `CATALOG-MATCH-VARIANT-1`: ✅ arreglado y mergeado — causa raíz de gran
  parte del "ruido de colisión" de todo el día, confirmado en datos reales
  (87 + 367 duplicados más aplicables solo después del fix).
- `DUP-CROSSFMT-8` (Mega Drive): 1.407 → 67 conflictos a lo largo del día
  (772 + 367 aplicados en dos pasadas, antes y después del fix).
- `DUP-CROSSFMT-9` (NES/Game Boy/Game Gear): 879 → 622 conflictos, 255
  aplicados (168 + 87). Sin la pasada de resolución por entrada que sí se
  hizo en Mega Drive.
- `INBOX-STALE-1`: tamaño y composición medidos (44.611 archivos, 185 GB),
  sin categorizar a fondo ni tocar nada.
- Cable Sync: 5 plataformas del dedup de hoy llevadas a la RG556 (14.968
  archivos, 0 borrados). Resto de la biblioteca (ps2/psx/gamecube/wii/nds/
  arcade, ~450 GB) sin sincronizar — desajuste de formato `.chd` vs
  `.bin`/`.cue` sin resolver, el modo espejo no es seguro todavía para PSX.

## Siguiente sesión recomendada

- **Prioridad alta — desajuste de formato Cable Sync**: el PC tiene varios
  juegos PSX (mínimo) convertidos a `.chd` que el dispositivo todavía tiene
  en `.bin`/`.cue` — un Cable Sync en modo espejo (`delete_extra=true`)
  descartaría esos juegos reales del dispositivo sin reconocer que es el
  mismo contenido. Medir el alcance real (cuántos juegos, qué plataformas)
  antes de intentar sincronizar ps2/psx/gamecube.
- `DUP-CROSSFMT-9`/NES-Game Boy-Game Gear: aplicar la misma resolución por
  entrada que ya se hizo en Mega Drive (356/418 grupos con sub-duplicado
  real) — no hecha todavía en estas 3 plataformas.
- `INBOX-STALE-1`: `rommgr organize-source "E:\Carpetas anbernic\inbox"`
  (dry-run primero) cuando el disco esté libre — mismo patrón que
  `ARCADE-DAT-CONTAMINATION-8` sobre `Unknown\`. 185 GB, incluye al menos un
  archivo claramente ajeno al retro (`Prince of Persia` PS2/PSP).
- Cable Sync del resto de plataformas (arcade 44 GB, nds 30 GB, wii 18 GB,
  dreamcast 12 GB) una vez resuelto el desajuste `.chd`.
