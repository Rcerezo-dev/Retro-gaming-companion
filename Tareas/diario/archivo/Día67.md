# ROM Manager Local — Día 67

## Contexto

Primera sesión desde "rammu" ejecutando sync real contra Dropbox y la
Anbernic por cable/ADB (todo el Día66 corrió en "Ruben"). Antes de este
diario ya se hizo trabajo real esta mañana, visible en `git log` y en
`Tareas/backlog.md` (working tree, sin commitear aún):

- **3 bugs reales de sync encontrados y arreglados** (commits ya en
  `develop`): `ac9e905` (`_set_key` crashea con rutas Windows al aplicar
  layout de saves), `dc25402` (`record_play_session` autobloquea la BD en
  `sync --apply` real, faltaba pasar `connection=conn`), `d4e46a6`
  (`list_remote()` usaba `--no-modtime-truncate`, flag inexistente en
  rclone — rompía la dirección de descarga del sync, subía siempre sin
  comparar).
- **PR #332 decidido**: mergeado (ya no queda pendiente del Día66).
- **Hallazgos documentados en `backlog.md` pero aún sin commitear**
  (`git status`: `M Tareas/backlog.md`, 122 líneas nuevas):
  - `SYNC-WII-SCOPE-1` (🔴 pendiente) — `SyncSource` no puede acotar solo
    `title/*/*/data/` de Dolphin Wii, subía el NAND emulado entero (96 MB,
    solo 0.29 MB saves reales). Fuente desactivada en `config.toml` hasta
    diseñar el fix.
  - `SYNC-WII-SCOPE-2` (✅ hecho) — limpiado lo ya subido por error a
    Dropbox.
  - `CABLE-SYNC-DUP-PATHS-1` (cerrada, no era bug) — investigación
    completa de cable-sync ADB con la Anbernic real.
  - `CABLE-SYNC-LEGACY-DUPS-1` (🔴 pendiente, baja prioridad) — 8 saves
    duplicados en rutas legado en el dispositivo.
  - `CABLE-SYNC-APPLY-1` (✅ hecho) — 106 saves subidos a la Anbernic real,
    232 MB, 0 errores.
  - `CABLE-SYNC-SAVES-PREFIX-1/2` (🔴 pendiente, sin decidir) — cable-sync
    ADB no reconoce el convenio D2 `saves/` del PC cuando `pc_path` es la
    raíz de la biblioteca (el valor por defecto real de la UI) — riesgo de
    que la UI real marque todos los saves como "nuevos" en vez de ya
    sincronizados.
- Hay un `config.toml.bak` sin trackear en la raíz (backup manual de la
  sesión de pruebas reales, no tocar sin preguntar).

## Objetivos del día

1. Commitear los hallazgos ya documentados en `backlog.md` (están en
   working tree desde esta mañana, sin commit propio todavía).
2. Decidir y, si procede, diseñar `CABLE-SYNC-SAVES-PREFIX-1/2` — mapear
   en el dispositivo real qué cores usan `saves/<core>/` vs plano, antes
   de tocar `sync/android_paths.py`. Prioridad alta: pilar de sync.
3. Diseñar `SYNC-WII-SCOPE-1` (include/exclude en `SyncSource` o entrada
   dinámica por `title/*/*/data/`) y reactivar la fuente Dolphin (Wii).
4. Si queda tiempo: `CABLE-SYNC-LEGACY-DUPS-1` (limpieza manual de 8 saves
   duplicados en la Anbernic).
5. Pendientes de fondo del Día66 si hay hueco: `DUP-DISC-RA-1b` parte 2
   (agrupado por edición vía hash RA), decidir `ANDROID-DUP-2` Fase 3
   (discos, ~382 GB, 3+ horas).

### Candidatas adicionales del backlog (revisadas hoy contra el estado real, por si hay hueco — sin orden de prioridad estricto)

**Pilar 3 — sync (máxima prioridad si aparece algo aquí):**
- `SAVES-FRAGMENT-8` — inventarios real ya hecho (1.286 archivos de save/state
  repartidos en 5 raíces de la Anbernic), la consolidación en sí sigue sin
  implementar.
- `CABLE-ROM-FIX-6` — Cable Sync en espejo bloqueado solo para `psx`: 70
  títulos (127 archivos, 32,75 GB) están como `.chd` en el PC y `.cue`/`.bin`
  en el dispositivo — sin decidir si convertir el dispositivo o enseñar al
  espejo a comparar por contenido cross-formato.
- `LIBRARY-SYNC-STALE-1a` — relanzar Cable Sync `pc_to_anbernic` de `gba/`
  para reflejar los 25 archivos movidos por `GBA-MISPLACED-1a/b/c`; requiere
  ADB autorizado (ya lo tenemos hoy, aprovechar).
- `DEVPROFILE-9` — playtime (`.lrtl`), recientes/favoritos (`.lpl`) y
  credenciales RA no siguen al usuario entre dispositivos; mismo hueco de
  diseño que `DEVPROFILE-8b` (bloqueado, no trivial).

**Organización / biblioteca:**
- `GAME-BLOCKLIST-1/2` — diseñar marca de exclusión permanente por SHA1 +
  acción "eliminar de ambas bibliotecas", y que el scan/Inbox la respeten.
- `PSX-STRUCTURE-2b` — de los ~60 `.bin` sueltos de PSX en la Anbernic, ~24
  no aparecieron como bare-bin en la pasada de `generate-cues`, sin explicar
  por qué; además `D (Europe) (Disc 1).bin` sigue confirmado roto (1024
  bytes) sin decisión tomada.
- `MDFOLDER-FIX-2` — añadir `.smd` a `platforms.toml` (Mega Drive
  interleaved, no detectado hoy por `organize-source`); cambio mecánico de
  una línea, 2 archivos reales afectados, prioridad baja.
- `DOCS-AUDIT-1b` — actualizar 8 comentarios de código que citan la ruta
  vieja de los roadmaps DEVPROFILE movidos el 2026-09-20; mecánico, sin
  riesgo.
- `DOCS-AUDIT-1a` — decidir convención de archivo para informes sueltos en
  `Tareas/` (decisión del usuario, no de código).
- `ARCADE-DAT-CONTAMINATION-10` (acción 4, `H:\ROMs`) — solo si el disco
  `H:` está conectado hoy en esta máquina; si no, sigue igual que hasta
  ahora.
- `ARCADE-RENAME-BUG-1e` — separar `arcade`/`cps1-3`/`neogeo` por core real
  en `emulator_options.json`; depende de que `1b`/`1c` estén cerradas antes.

**Fuera de los 3 pilares (baja prioridad, solo si sobra tiempo):**
- `INBOX-BIOS-ROUTE-1` — auto-enrutado de BIOS/devkit sueltos en el Inbox;
  necesita descargar antes el catálogo `mame_xml` (~300 MB), sesión
  dedicada, no un hueco corto.
- `SAGE-4` — construir el recomendador NLP en sí (SAGE-1/2 solo prepararon
  datos); MVP recomendado por el propio doc de propuestas: Smart Filter con
  scoring + perfil desde favoritos, sin dependencias nuevas.
- `ROADMAP-IDEAS` — propuestas del usuario del 2026-08-13 todavía sin
  diseñar, revisar si alguna sigue vigente.

## Trabajo realizado

1. **Commiteados los hallazgos del working tree de esta mañana** (`5073d5a`):
   `SYNC-WII-SCOPE-1/2` y `CABLE-SYNC-DUP-PATHS-1`/`APPLY-1`/
   `SAVES-PREFIX-1/2` ya estaban en `backlog.md` sin commit propio.
2. **`CABLE-SYNC-SAVES-PREFIX-1` — mapeo real completado** con la Anbernic
   conectada por ADB (`adb shell find`/`stat`). Hallazgo clave: el problema
   es mucho mayor de lo estimado — no son 8 duplicados legado, hay
   solapamiento casi total de nombres de archivo entre `RetroArch/<plataforma>/`
   plano y `RetroArch/saves/<algo>/` en todas las plataformas revisadas
   (mame, gba, psx, snes, nes, cps1, cps2), y ambas copias reciben
   escrituras recientes (no es solo arrastre histórico de una migración).
   El segundo segmento bajo `saves/` unas veces es el nombre de plataforma
   (reconocido por `PLATFORM_BY_FOLDER`) y otras el nombre del core de
   RetroArch (no reconocido) — sin mapeo core→plataforma fiable hoy.
   Detalle completo y tabla de conteos en `backlog.md`.
3. **`CABLE-SYNC-SAVES-PREFIX-2` — diseño propuesto** (sin implementar,
   según regla "Investigar antes de arreglar" de CLAUDE.md): matchear por
   `(plataforma, nombre de archivo)` ignorando todos los segmentos
   intermedios de la ruta Android, en vez de depender de un mapeo
   core→plataforma completo. Alternativa para colisiones de nombre:
   tamaño o SHA1.
4. Actualizado el alcance de `CABLE-SYNC-LEGACY-DUPS-1` (bloqueada por
   `CABLE-SYNC-SAVES-PREFIX-2` — decidir la copia "correcta" puede no ser
   universal).
5. **`SYNC-WII-SCOPE-1` — diseñado e implementado**: campo `include_glob`
   en `SyncSource` (por defecto `"**/*"`, sin cambio de comportamiento para
   el resto de fuentes) usado en `list_local_saves()`/`sync_saves()` en vez
   del `rglob("*")` fijo. `config.toml` reactivado con
   `include_glob = "title/*/*/data/**/*"`. Test añadido en
   `tests/test_save_syncer.py`. Verificado en dry-run real: 10 archivos
   detectados, todos bajo `data/` reales — el resto del NAND (IOS, System
   Menu, apps) queda fuera.

## Archivos modificados

- `Tareas/backlog.md` — hallazgos de esta mañana + mapeo real de
  `CABLE-SYNC-SAVES-PREFIX-1/2`, alcance revisado de
  `CABLE-SYNC-LEGACY-DUPS-1`, `SYNC-WII-SCOPE-1` marcada hecha.
- `src/rom_manager/config.py` — `SyncSource.include_glob`.
- `src/rom_manager/sync/save_syncer.py` — `list_local_saves`/`sync_saves`
  usan `include_glob`.
- `src/rom_manager/cli.py` — hilo `include_glob` en los 2 call sites de
  `sync_saves`.
- `src/rom_manager/web/handlers/sync_cloud.py` — idem para el endpoint web.
- `tests/test_save_syncer.py` — test del filtrado del NAND de Dolphin Wii.
- `config.toml` — fuente Dolphin (Wii) reactivada con `include_glob`.

## Trabajo realizado (continuación)

6. **`CABLE-SYNC-SAVES-PREFIX-2` — implementado**: `_skip_existing_device`/
   `_skip_existing_pc` en `sync_cable.py` ahora tienen un fallback por
   nombre de archivo (`ab_by_name`/`_pc_by_name()`) cuando la ruta exacta
   no coincide — mismo tamaño = ya sincronizado, sin importar el prefijo
   (`saves/`, nombre de core). Sin mapeo core→plataforma, sin tocar
   `android_paths.py`. 2 tests nuevos en
   `tests/test_cable_sync_rom_skip_existing.py`.
   **Validado contra la Anbernic real** (servidor local levantado
   temporalmente para la prueba, dry-run, luego cerrado): con
   `pc_path=library_root` — el caso exacto que `CABLE-SYNC-DUP-PATHS-1`
   documentó ayer como roto (0/528 reconocidos) — ahora **522/528 se
   detectan como ya sincronizados, solo 6 genuinamente nuevos**.
   Documentados sin implementar 2 hallazgos relacionados descubiertos al
   revisar el código: `CABLE-SYNC-DOWNLOAD-DEST-1` (`anbernic_to_pc` no
   canonicaliza el destino de descarga en PC) y `CABLE-SYNC-NEWEST-CANON-1`
   (la dirección `newest` no canonicaliza ningún lado).

## Trabajo realizado (continuación 2)

7. **`CABLE-SYNC-NEWEST-CANON-1` — implementado**: la dirección `newest`
   en modo ADB reconstruía su propio `ab_index` sin el fallback por
   nombre añadido en el punto 6 — reescrito para reusar el `ab_index`/
   `ab_by_name` ya construidos, con match por nombre solo cuando hay un
   único candidato del lado Android (ambiguo → se trata como distinto,
   fail-safe). Test nuevo. **Validado en real** (`direction=newest`,
   `pc_path=library_root`): 405/597 reconocidos como ya sincronizados,
   el resto son casos legítimos (BIOS/defaults, mtimes reales distintos).
   Al investigar aparecieron 2 reimplementaciones más del mismo patrón
   (`sync/cable_engine.py:plan_direction()` y el fallback "RetroArch
   (legacy)" del daemon auto-sync ADB en `cable_sync_daemon.py`) — mismo
   problema de fondo, no comparten código con lo arreglado hoy.
   Documentadas sin implementar como `CABLE-SYNC-NEWEST-CANON-2`.

## Trabajo realizado (continuación 3)

8. **`CABLE-SYNC-NEWEST-CANON-2` — implementado**: en vez de parchear las
   2 reimplementaciones restantes del mismo bug por separado, se extrajo
   la lógica de matching a `reconcile_newest_by_name()` en
   `sync/android_paths.py` — función única y testeada, usada ahora por
   las 3 implementaciones (`sync_cable.py`, `cable_engine.py`,
   `cable_sync_daemon.py`), que quedan como consumidores delgados.
   4 tests unitarios nuevos + 2 en `test_cable_engine.py`. Suite completa
   (1414 tests) sin regresiones nuevas (los 3 fallos son ambientales,
   dispositivo ADB conectado). **Revalidado en real tras el refactor**:
   resultado idéntico al de antes (405/597, 192 candidatos, 0 errores) —
   confirma que el refactor no cambió comportamiento.

## Trabajo realizado (continuación 4)

9. **`CABLE-SYNC-DOWNLOAD-DEST-1` — implementado**: pregunté al usuario el
   criterio de diseño antes de tocar código (cambiaba dónde aterrizan
   descargas para todos los usuarios). Decisión: con carpeta intermedia
   reconocible (`saves/psx/foo.sav`) → ruta canónica sin `saves/`; con
   carpeta ambigua (`saves/mame2003/...`, core sin mapear) → mantener ruta
   cruda, no adivinar. Nueva `canonical_download_rel_posix()` en
   `android_paths.py`, usada en los 3 sitios que escriben destino de
   descarga (`sync_cable.py`, `cable_engine.py`, `cable_sync_daemon.py`) —
   incluyó actualizar 2 bloques `delete_extra`/espejo que comparaban por
   ruta cruda (si no, un archivo recién bajado a su ruta canónica se leía
   como "extra" y se borraba). 8 tests nuevos. Intento de validación en
   real inconcluso — el auto-sync daemon se disparó en paralelo y
   contaminó el log compartido; descartado en vez de arriesgar una lectura
   errónea, la cobertura de tests se consideró suficiente.

## Trabajo realizado (continuación 5)

10. **`CABLE-SYNC-LEGACY-DUPS-1` — mapeo completo + backup, sin borrar**:
    con la Anbernic real, se listaron los 823 archivos de save/state del
    dispositivo y se encontraron **289 nombres duplicados (706 archivos)**
    — mucho mayor que la estimación de "8" con la que se abrió la tarea.
    Antes de proponer borrar nada, se le preguntó al usuario cómo proceder
    (dado el riesgo real de pérdida de partidas) — eligió "informe +
    backup, sin borrar todavía". Clasificación en 4 categorías (basura
    0 bytes, mismo-tamaño-copia-puntual, mismo-tamaño-fechas-separadas,
    tamaño-distinto-conflicto) guardada en
    `.rommgr/legacy_dups_report_2026-09-21.csv`. Los 706 archivos
    respaldados en `.rommgr/legacy_dups_backup_2026-09-21/` (0 errores).
    **Hallazgo que justificó no automatizar nada**: uno de los 7 grupos
    conflictivos tiene una copia de 0 bytes con el mtime más reciente —
    un criterio ciego de "más nueva gana" habría borrado una partida real
    de 512KB a favor de un archivo vacío. No se borró nada del
    dispositivo en esta sesión.

## Trabajo realizado (continuación 6)

11. **Investigado el caso conflictivo `SoulSilver` señalado antes**: no
    era un conflicto real de partidas. `emulator_saves/<package>/` es una
    carpeta interna del proyecto (solo para bajar saves por-emulador vía
    ADB) que se filtró al dispositivo entero — confirmado con **54
    archivos** bajo `/storage/emulated/0/RetroArch/emulator_saves/` en el
    dispositivo real (incluye toda la carpeta de estados de DuckStation),
    todos subidos en un único evento. Causa raíz: el fallback "RetroArch
    (legacy)" del daemon auto-sync (y el endpoint manual con
    `pc_path=library_root`) caminan la biblioteca entera sin excluir
    `emulator_saves/`, a diferencia de `_descartados/` que sí está
    excluido. Documentado como `CABLE-SYNC-EMULATOR-SAVES-LEAK-1`, sin
    implementar — explica varios de los 289 grupos de
    `CABLE-SYNC-LEGACY-DUPS-1`.

## Trabajo realizado (continuación 7)

12. **`CABLE-SYNC-EMULATOR-SAVES-LEAK-1` — implementado**: nueva constante
    `EMULATOR_SAVES_DIR_NAME` en `config.py` (reemplaza el literal ya
    usado en `get_adb_sync_sources()`), excluida del walk en los 3 sitios
    afectados (`sync_cable.py`, `cable_engine.py`, `cable_sync_daemon.py`)
    con el mismo patrón que ya usa `_descartados/`/`TRASH_DIR_NAME`.
    2 tests nuevos. Suite completa (1419 tests) sin regresiones. Corta la
    fuga hacia adelante — no limpia los 54 archivos ya filtrados en el
    dispositivo real (eso sigue en `CABLE-SYNC-LEGACY-DUPS-1`, ya
    respaldados).

## Estado al finalizar

Objetivos 1-3 del día completados. Además: `CABLE-SYNC-NEWEST-CANON-1/2`
y `CABLE-SYNC-DOWNLOAD-DEST-1` implementados y commiteados — todo el
problema de fondo de rutas Android sin canonicalizar en cable-sync queda
cerrado en las 3 implementaciones que existían. `CABLE-SYNC-LEGACY-
DUPS-1` con mapeo completo + backup (706 archivos, sin borrar nada,
pendiente commit de solo `backlog.md` — el backup/CSV son locales,
gitignored). Objetivos 4-5 no abordados por falta de tiempo/contexto.

## Trabajo realizado (continuación 8)

13. **`CABLE-SYNC-LEGACY-DUPS-1` — borrado ejecutado**: con el usuario
    decidido a automatizar el borrado de los grupos de bajo riesgo, se leyó
    el CSV real (no la cifra aproximada de "269" del hallazgo original):
    **159 grupos / 240 archivos** (`junk_zero_bytes`: 14 grupos/29 archivos,
    todos 0 bytes; `safe_same_copy_event`: 145 grupos/211 archivos,
    conservando la ruta canónica más corta sin `saves/`). Borrados vía
    `adb shell rm` (script puntual, no forma parte del código de la app).
    Primer intento falló por quoting (nombres con espacios/paréntesis se
    partían en argumentos sueltos para el shell remoto) — arreglado
    envolviendo cada ruta en comillas simples escapadas antes de mandarla a
    `adb shell`. Verificado post-borrado con `ls` (también con el mismo
    quoting, el primer chequeo dio falsos positivos por glob sin comillas
    en nombres tipo `[E]`): las 145 rutas canónicas siguen intactas, los 240
    borrados ya no están. Backup previo intacto en
    `.rommgr/legacy_dups_backup_2026-09-21/`. Quedan sin tocar (revisión
    manual, como se acordó): 123 `review_time_gap` + 7 `CONFLICT_diff_size`.
14. **`DUP-DISC-RA-1b` parte 2 — implementada**: agrupado por edición en la
    cola de duplicados vía hash de disco RA (nueva pasada de unión en
    `_review_groups_for_repo`, mismo patrón que `crossfmt`/`region` pero
    uniendo por hash de disco en vez de título difuso). Cubre PSX y también
    GameCube/Wii (ya tenían hash cacheado, más alcance del que decía la fila
    original del backlog). Nuevo motivo `"disc_hash"` en la cola + etiqueta
    en `review_copies.js`. 2 tests nuevos cubriendo el caso real de
    `ANDROID-DUP-1` (Crash Bandicoot en 3 formatos, la copia legacy sin
    `canonical_title`).
15. **`DUP-DISC-RA-1b` parte 2 — validada contra `library_pc.db` real**:
    345 PlayStation + 59 GameCube + 118 Wii (biblioteca real de
    `E:\Carpetas anbernic`), 3 min de ejecución (caché de hashes ya cubría
    casi todo), 1829 grupos totales, **1 grupo nuevo vía `disc_hash`**
    (`Resident Evil 3 - Nemesis (USA, Canada).chd` + `resident evil 3.chd`,
    nombre no canónico) — confirma que el hash de disco RA funciona en
    producción sin errores. El escenario que motivó la tarea (volcado legacy
    CloneCD con nombre de serial, `ANDROID-DUP-1`) vive en la RG556 vía ADB,
    medio físico distinto de `E:\Carpetas anbernic` — validar ese caso exacto
    requeriría copiar esos archivos localmente primero, sesión aparte.

## Trabajo realizado (continuación 9)

16. **`ANDROID-DUP-2` Fase 3 — parcial, ejecutada 2026-09-21**: siguiendo el
    roadmap `.claude/roadmaps/23-android-hash-rescan-dedup.md`, se lanzó el
    servidor local (`python -m rom_manager serve`) y se escanearon por ADB
    (hash real, una llamada `POST /api/adb-scan` por plataforma) las 5
    plataformas de disco medianas: `nds` (480 archivos/385 ROMs), `gamecube`
    (19), `dreamcast` (61), `3ds` (37), `psp` (7) — 971 archivos, 0 errores.
    `psx` (215G) y `ps2` (89,5G) quedaron fuera a propósito (coste ~2h15
    juntas, decisión explícita del usuario de empezar solo con las baratas).
    `_build_review_queue` contra `library_android.db`: **7 grupos
    duplicados reales, todos en Dreamcast, 4,79 GB desperdiciados** —
    `Dead or Alive 2 (Europe).cdi` (2 copias idénticas + variante Beta),
    `Legacy of Kain - Soul Reaver` (`(USA).cdi` byte-idéntico a
    `(Europe).cdi`, mismo SHA1 — probable mal etiquetado, no versión
    distinta), `Crazy Taxi 2`, `Sonic Adventure (XBLA)`, `D2 (USA)` —
    coincide exactamente con los 5 juegos Dreamcast que `DUP-DISC-RA-1e` ya
    había confirmado en `.cdi`. nds/gamecube/3ds/psp salieron limpios.
    **Hallazgo colateral, documentado sin implementar**: dos
    `vmu_save_*.bin` (saves de memory card Dreamcast) se clasificaron como
    ROM duplicado en vez de save — `.bin` no está en `save_extensions`
    (correcto, rompería discos reales), `_do_adb_scan`
    (`web/handlers/scan.py:519-527`) los trata como ROM por descarte. Nada
    aplicado (`resolve-duplicates --apply`) — pendiente de decisión del
    usuario, mismo criterio de todo el backlog.

## Trabajo realizado (continuación 10)

17. **Hallazgo colateral de `ANDROID-DUP-2` — arreglado**: nueva rama en
    `_do_adb_scan` (`web/handlers/scan.py`) que reconoce el patrón de
    nombre `vmu_save_*.bin` (convención Flycast/Redream para memory cards
    Dreamcast) y lo clasifica como save, no ROM — sin tocar
    `save_extensions` global (`.bin` también es un disco real). Test nuevo
    `test_adb_scan_classifies_vmu_save_bin_as_save_not_rom`. Editado con el
    servidor ya corriendo (seguro, Python no recarga módulos en caliente —
    el scan de `psx` en curso siguió con el código anterior).
18. **`ANDROID-DUP-2` Fase 3 — completada al 100% (7/7 plataformas)**: `psx`
    (215 GB) — 1746 archivos, 1685 ROMs, 0 errores. `ps2` (89,5 GB) — 27
    archivos, 26 ROMs, 0 errores. `_build_review_queue` contra
    `library_android.db` completo: **149 grupos duplicados en psx/ps2,
    19,6 GB desperdiciados** (`crossfmt`: 94, `sha1`: 67). `disc_hash`
    (parte 2 de `DUP-DISC-RA-1b`) no aportó nada aquí — a diferencia de la
    validación contra `library_pc.db`, las filas de `library_android.db`
    tienen rutas Android que `get_psx_disc_hash` no puede leer desde esta
    máquina Windows, esperado, no un bug. **Hallazgo nuevo y real, sin
    implementar**: en el grupo `Xenogears`, `_is_disc_set()`
    (`web/builders/duplicates.py:56-80`) no protegió un set multi-disco
    real — la edición japonesa (un disco, sin tag `(Disc N)`) quedó unida
    con los 2 discos individuales de la edición USA, porque el guard se
    desactiva en cuanto un solo miembro del clúster no tiene número de
    disco parseable. Con datos PSX reales vía ADB por primera vez este
    patrón aparece en varios juegos — **no aplicar `resolve-duplicates
    --apply` sobre psx/ps2 sin revisión manual** hasta arreglarlo.
19. **`CABLE-SYNC-LEGACY-DUPS-1` — los 130 grupos restantes resueltos por
    contenido real, no por tamaño/fecha**: en vez de decidir a ciegas con
    heurísticas de tamaño/fecha (un save GBA de tamaño fijo no dice nada del
    contenido real), se calculó el SHA1 de cada una de las 321 copias contra
    el backup local ya hecho (sin usar ADB, el dispositivo estaba ocupado
    con `ps2`). **174 eran copias byte-idénticas de verdad** pese a fechas
    distintas — borradas, 0 errores, verificado que las 147 rutas canónicas
    de contenido único siguen intactas. Ningún archivo de contenido
    realmente distinto se tocó (p. ej. `Metroid - Zero Mission [E].srm`
    conserva su save propio de `mGBA` además del de `VBA Next`/`gba`).
    **Hallazgo colateral nuevo, sin implementar**: 2 grupos tenían copias en
    `saves/.stversions/<core>/` — carpeta de versionado de Syncthing
    (herramienta de terceros en el dispositivo), candidata a excluirse del
    escaneo igual que `_descartados/`/`emulator_saves/`. `CABLE-SYNC-
    LEGACY-DUPS-1` queda cerrada.

## Estado real al cierre (tras continuación 10)

Todo el plan original del día (objetivos 1-5) quedó completado, más varias
cosas que no estaban planeadas al empezar: Fase 3 completa de
`ANDROID-DUP-2` (7/7 plataformas, no solo las 5 baratas decididas a media
tarde), y los 289 grupos completos de `CABLE-SYNC-LEGACY-DUPS-1` resueltos
(no solo el primer lote de bajo riesgo). Dos hallazgos nuevos y reales
quedaron documentados con tarea propia sin implementar: `DUP-DISC-SET-1`
(riesgo real al aplicar duplicados en PSX) y `ANDROID-STVERSIONS-1`
(ruido de Syncthing). Sesión larga — varias horas reales con la Anbernic
conectada por ADB sin cortes.

## Pendiente para mañana (Día68)

**Bloqueante antes de tocar duplicados en PSX/PS2:**
1. `DUP-DISC-SET-1` (nuevo hoy, backlog) — `_is_disc_set()` no protege un
   set multi-disco cuando una edición regional sin tag `(Disc N)` se mezcla
   con los discos sueltos de otra edición (confirmado con `Xenogears` real).
   Bloquea aplicar `resolve-duplicates --apply` sobre los **149 grupos
   psx/ps2** encontrados hoy (19,6 GB) hasta que se arregle — es el área de
   mayor riesgo de duplicados, diseñar con cuidado, no parchear a ciegas.

**Pilar 3 — sync (máxima prioridad si se toca algo aquí):**
2. Ejecutar el `--apply` real de `pc_to_anbernic`/`newest`/`anbernic_to_pc`
   con los archivos genuinamente nuevos detectados ayer (todo quedó en
   dry-run) — validar también en real el destino de descarga canonicalizado
   de `CABLE-SYNC-DOWNLOAD-DEST-1` (interferencia del auto-sync daemon lo
   impidió la última vez).
3. Aplicar `SYNC-WII-SCOPE-1` en PC2 (mismo `include_glob`, reactivar la
   fuente allí también).
4. `SAVES-FRAGMENT-8` — inventario ya hecho (1.286 archivos de save/state
   en 5 raíces de la Anbernic), consolidación sin implementar.

**Nuevas tareas de hoy, documentadas sin implementar:**
5. `DUP-DISC-SET-1` (ver punto 1, alta prioridad).
6. `ANDROID-STVERSIONS-1` — medir alcance real de `saves/.stversions/`
   (carpeta de Syncthing) en la Anbernic y excluirla del cable-sync/escaneo
   de duplicados, mismo patrón que `_descartados/`/`emulator_saves/`.

**Decisiones del usuario pendientes (nada aplicado, solo detectado):**
7. Los 7 duplicados reales de Dreamcast encontrados en `ANDROID-DUP-2` Fase
   3 (4,79 GB) — sin aplicar `resolve-duplicates`.
8. Los 149 grupos psx/ps2 (19,6 GB) — bloqueados por `DUP-DISC-SET-1`, no
   decidir nada aquí hasta que se arregle.
9. El grupo `disc_hash` de `Resident Evil 3 - Nemesis` (`E:\Carpetas
   anbernic`, validación de `DUP-DISC-RA-1b`) — sin aplicar.
10. Los 123 `review_time_gap`/7 `CONFLICT_diff_size` de contenido
    genuinamente distinto en `CABLE-SYNC-LEGACY-DUPS-1` que quedaron
    intactos a propósito (p. ej. `Metroid - Zero Mission` con save propio
    en `mGBA`) — decidir si alguna vez hace falta reconciliarlos.
