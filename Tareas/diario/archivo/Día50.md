# ROM Manager Local — Día 50

## Contexto

Continuación de `feature/devprofile-2-retroarch-cfg-writer`. Al llegar a la
sesión, `DEVPROFILE-2a/2b/2c` ya estaban implementados y en verde
(`retroarch_cfg_writer.py`, `_handle_retroarch_check` refactorizado) pero sin
commitear, y quedaba abierta `DEVPROFILE-2d`: cómo disparar
`apply_savefile_layout()` desde la UI — pregunta explícita para el usuario en
`Tareas/Roadmap-DEVPROFILE-1-4.md` §3.

## Objetivos del día

1. Resolver `DEVPROFILE-2d` (disparador del layout de saves en Settings)
2. Verificar la suite completa y, si es posible, la UI en navegador
3. Commitear el trabajo de `DEVPROFILE-2` completo
4. Documentar los próximos pasos de la rama (DEVPROFILE-3/4/5/6)

## Trabajo realizado

1. **DEVPROFILE-2d resuelto** — confirmado con el usuario: botón manual, no
   automático (coincide con la recomendación ya escrita en el roadmap).
   - `default_savefile_layout(library_root)` nueva en
     `retroarch_cfg_writer.py`: fija `library_root/saves` +
     `library_root/states` como destino por defecto — mismo convenio que ya
     usa el sync a la nube D2 (`sync.saves_remote`/`states_remote`, ver
     `server.py::_implicit_tray`) — sin pedir rutas por UI.
   - `_handle_apply_retroarch_savefile_layout()` nuevo en
     `web/handlers/system.py`: reusa la misma resolución de `retroarch.cfg`
     que `_handle_retroarch_check` (junto al exe configurado en Settings, no
     `_detect_retroarch_install()`).
   - Ruta `POST /api/retroarch-apply-savefile-layout` registrada en
     `web/handlers/esde/system.py`.
   - Botón "💾 Aplicar layout de saves" en el panel RetroArch de Settings
     (`tab-settings.html`), función `applyRetroArchSavefileLayout()` en
     `js/tabs/esde.js` (patrón calcado de `doMigrateSavesStructure`/
     `loadRetroArchCheck` ya existentes), exportada en `main.js`. Refresca el
     diagnóstico (`loadRetroArchCheck()`) tras aplicar.
   - Test nuevo `test_default_savefile_layout_matches_d2_sync_convention`.
2. **Verificación** — suite completa: 1107 tests en verde (antes 1094 con 3
   fallos preexistentes de ADB, ahora ninguno visible). Servidor real
   levantado (`rommgr serve`) y probado con `curl`: caso sin RetroArch
   configurado devuelve el error esperado; el HTML sirve el botón nuevo;
   `node --check` sin errores en `main.js`/`esde.js`. **No se pudo probar el
   clic real en navegador** — la extensión Claude-in-Chrome no está
   conectada esta sesión.
3. **Commit** — `e8a6d11 feat(devprofile): escribir
   savefile_directory/savestate_directory en retroarch.cfg (DEVPROFILE-2)`,
   10 archivos, cierra `DEVPROFILE-2` completo (2a-2d).
4. **Backlog + roadmap actualizados** — `Tareas/backlog.md` (fila
   `DEVPROFILE-2` → ✅) y `Tareas/Roadmap-DEVPROFILE-1-4.md` §3 (§2d resuelto
   con el detalle de implementación).

## Archivos modificados

- Backend: `src/rom_manager/services/retroarch_cfg_writer.py` (nuevo,
  `apply_savefile_layout`, `default_savefile_layout`),
  `src/rom_manager/web/handlers/system.py`
  (`_handle_apply_retroarch_savefile_layout`, refactor de
  `_handle_retroarch_check`), `src/rom_manager/web/handlers/esde/system.py`
  (ruta nueva).
- Frontend: `src/rom_manager/web/static/js/tabs/esde.js`
  (`applyRetroArchSavefileLayout`), `src/rom_manager/web/static/js/main.js`
  (export), `src/rom_manager/web/static/js/api.js` (entrada
  `retroarchApplySavefileLayout`), `src/rom_manager/web/static/partials/tab-settings.html`
  (botón + contenedor de resultado).
- Tests: `tests/test_retroarch_cfg_writer.py` (+2 tests).
- `Tareas/backlog.md`, `Tareas/Roadmap-DEVPROFILE-1-4.md`,
  `Tareas/diario/Día50.md` (este archivo).

## Estado al finalizar

`DEVPROFILE-2` (1..4) completo y commiteado en
`feature/devprofile-2-retroarch-cfg-writer`. Suite en verde (1107).
**PR #270 abierta a `develop`** (incluye también DEVPROFILE-1b/1c/1d, ya en
la misma rama desde antes de esta sesión) — pendiente de checks +
CodeRabbit.

`DEVPROFILE-1` (catálogo único) y `DEVPROFILE-2` (retroarch.cfg, solo PC)
están ✅ en el backlog. Quedan `DEVPROFILE-3` (tokenizador de rutas),
`DEVPROFILE-4` (manifiesto Tier A + backup), `DEVPROFILE-5/6` (botones de
restauración PC/Android) y `DEVPROFILE-7/8/9` (deriva, restauración de BD/
caché, datos de usuario) — todos ⬜, ver `Tareas/backlog.md` sección
`DEVPROFILE`.

## Trabajo realizado (continuación — PR #270 abierta)

5. **DEVPROFILE-3 resuelto** — `services/path_tokenizer.py`
   (`tokenize()`/`resolve()`, puras, sin I/O). Raíz más específica gana
   cuando una está anidada dentro de otra; ruta fuera de las tres raíces se
   devuelve tal cual (rutas de standalones). 6 tests.
6. **DEVPROFILE-4a/4b — solo backend, confirmado con el usuario** (la
   pantalla de Settings queda para otra sesión):
   `services/device_profile.py::detect_tier_a_sources()` — investigación
   previa mostró que `config/` (con `.opt` **y** `remaps/`, que vive dentro)
   y `cheats/` ya los mueve `build_cloud_sync_sources()` sin tocar nada; lo
   que faltaba de verdad eran 3 carpetas hermanas bajo el directorio de
   RetroArch: `autoconfig/`, `shaders/`, `system/` (BIOS), cada una solo
   devuelta si existe en disco. `retroarch-core-options.cfg` (archivo
   suelto) queda fuera a propósito — `SyncSource` sincroniza carpetas, no
   archivos — marcado `ponytail:` en el código.
   `export_profile_sources()`/`import_profile_sources()` tokenizan
   `SyncSource.local_dir` solo al export/import entre dispositivos. 5 tests.
7. **Rama apilada, no sobre `develop`** — al crear la rama nueva se detectó
   que dependía de `Tareas/Roadmap-DEVPROFILE-1-4.md` y del backlog
   actualizado, que solo existen en `feature/devprofile-2-retroarch-cfg-writer`
   (PR #270 aún sin mergear) — no en `develop`. Rebase de la rama nueva sobre
   esa (`git reset --hard feature/devprofile-2-retroarch-cfg-writer`, sin
   commits propios que perder) en vez de sobre `develop`.
8. **PR #270** (DEVPROFILE-1/2) y **PR #271** (DEVPROFILE-3/4 backend,
   apilada sobre la #270) abiertas. 1118 tests en verde.

## Siguiente sesión recomendada

1. ✅ **PR #270** mergeada a `develop` (checks Lint+Tests en verde).
2. ✅ **PR #271** retargeteada a `develop`, rebasada y mergeada (Lint+Tests
   en verde; `claude-review` falló por un error interno del bot —
   "Reached maximum number of turns" — no es check obligatorio en la
   protección de rama y no llegó a producir ningún hallazgo).
3. **Pantalla de Settings "Perfil del dispositivo"** (DEVPROFILE-4a UI,
   pendiente): consumir `detect_tier_a_sources()`, tabla con checkboxes para
   confirmar/descartar cada fuente detectada + campo de remoto editable
   (sugerido `<remote>:RetroSync/<carpeta>`), guardar como `sync_sources`
   reales en `config.toml`. Sin esto, DEVPROFILE-4 no es usable desde la UI.
4. **DEVPROFILE-5/6** (botones de restauración PC/Android) dependen de que
   la pantalla de perfil de 4 exista — no empezarlos antes.
5. Pendientes menores sin fecha: `DEVPROFILE-7` (detección de deriva del
   `retroarch.cfg`, "sale gratis" una vez existe 4), `DEVPROFILE-8`
   (restaurar BD/caché/DATs) y `DEVPROFILE-9` (recientes/favoritos/playtime/
   credenciales RA).

## Trabajo realizado (continuación — DEVPROFILE-4a UI, rama nueva)

9. **Pantalla "Perfil del dispositivo" en Settings** — cierra el pendiente
   de PR #271. `GET /api/device-profile-detect`
   (`_handle_device_profile_detect` en `web/handlers/system.py`, ruta en
   `web/handlers/esde/system.py`) localiza RetroArch igual que
   `_handle_retroarch_check`, deriva `remote_base` de
   `saves_remote`/`states_remote` y excluye del listado de candidatos las
   carpetas Tier A que ya están en `config.sync.sync_sources` (no se
   vuelve a preguntar por lo ya confirmado). El guardado reutiliza
   `POST /api/config` — solo hizo falta añadir `"sync.sources"` al
   `allowed` set de `_save_config()`; `write_config_toml()` ya soportaba
   array-of-tables genérico y el reload ya recargaba `config.sync` entero,
   cero código nuevo en esa capa. UI: panel nuevo en `tab-settings.html`
   bajo el de RetroArch + `loadDeviceProfileDetect()`/
   `saveDeviceProfileSources()` en `js/tabs/esde.js` (checkboxes + remoto
   editable por fila), exportadas en `main.js`. 2 tests nuevos en
   `tests/test_device_profile.py`; suite completa 1120 en verde.
10. **Incidente durante la verificación manual — corregido en la misma
    sesión**: al probar `POST /api/config` con `curl` para comprobar el
    guardado de `sync.sources`, se envió un array parcial (solo 2 de las 6
    fuentes reales) contra el `config.toml` **real** del proyecto en vez de
    uno de prueba — `write_config_toml()` reemplaza la lista completa, así
    que borró temporalmente las entradas de PCSX2/PPSSPP/MelonDS/Dolphin y
    sobrescribió `launchers.retroarch` con una ruta de prueba. Detectado
    inmediatamente al revisar `config.toml` tras la prueba. Restaurado vía
    la misma API con los 6 `sync.sources` originales (capturados en la
    llamada anterior a `/api/device-profile-detect`, no reconstruidos de
    memoria) y `launchers.retroarch` reapuntado a
    `E:/Emuladores/Retroarch/retroarch.exe` — confirmado por
    `savestate_directory = "E:\Carpetas anbernic\states"` en su
    `retroarch.cfg`, que coincide exactamente con `library_root`.
    **`config.toml` no está versionado (fuera de git)**, así que este tipo
    de prueba manual contra él no tiene red de seguridad — a partir de
    ahora, probar endpoints que escriben `config.toml` contra un
    `project_root` de prueba (`tmp_path`), nunca contra el real.
    **Usuario: verifica que `E:/Emuladores/Retroarch/retroarch.exe` es la
    ruta correcta** (Settings → Lanzador RetroArch) — es una inferencia con
    evidencia fuerte, no un valor recuperado con certeza.

## Trabajo realizado (continuación — DEVPROFILE-7, rama nueva sobre `develop`)

Nota: `DEVPROFILE-4a` (pantalla "Perfil del dispositivo") se completó en
paralelo en `feature/devprofile-4a-settings-ui` (PR #272, ya mergeada a
`develop`) — ver la sección anterior para el detalle, esta rama partió de
`develop` antes de que esa PR mergease.

11. **DEVPROFILE-7 — detección de deriva del layout de saves** (confirmada
   por el usuario mientras la PR #272 estaba en checks). No depende de
   DEVPROFILE-4a/PR #272 — solo de DEVPROFILE-2 (ya en `develop`), así que
   va en rama propia sobre `develop`. `_handle_retroarch_check()`
   (`web/handlers/system.py`) compara ahora `savefile_directory`/
   `savestate_directory` (leídos vía `read_savefile_layout()`, nuevo en
   `retroarch_cfg_writer.py`) contra `default_savefile_layout(library_root)`
   — el mismo convenio D2 que ya usa el botón "Aplicar layout de saves"
   (DEVPROFILE-2d). Sin endpoint ni UI nuevos: añade `savefile_drift` +
   un issue accionable al resultado que ya existía, y el panel de Settings
   ya renderiza `issues[]` como avisos — "sale gratis" tal como preveía el
   backlog. **Hallazgo real en este PC** (verificado con `curl` contra
   `GET /api/retroarch-check`, solo lectura): `savefile_directory` apunta a
   `E:\ROMs\saves` (ruta obsoleta, no coincide con `library_root`
   `E:\Carpetas anbernic`) mientras que `savestate_directory` sí está
   correcto (`E:\Carpetas anbernic\states`) — es decir, el botón de
   DEVPROFILE-2d nunca se había pulsado del todo, o se aplicó a medias.
   **Acción recomendada**: pulsar "Aplicar layout de saves" en Settings →
   Lanzador RetroArch. 2 tests nuevos
   (`tests/test_retroarch_check_drift.py`); suite completa 1122 en verde.

## Trabajo realizado (continuación — DEVPROFILE-5a/5b-5f, rama `feature/devprofile-5a-export-manifest`)

12. **DEVPROFILE-5a** — `save_profile_manifest()` (`services/device_profile.py`)
    sube `config.sync.sync_sources` tokenizado a `<remote_base>/device-profile.json`
    reusando `RcloneTransport.upload()` (enrutado a `fallback_remote`, sin
    método nuevo). Botón "Guardar copia del perfil en la nube" en el panel
    de Settings (`POST /api/device-profile-save-manifest`). 6 tests nuevos.
13. **DEVPROFILE-5b-5f — comando `rommgr restore`** (`cli.py`, nuevo
    subparser): descarga `device-profile.json` (`RcloneTransport.download`,
    mismo patrón de fallback-remote que 5a) → pide `library_root`/ruta de
    RetroArch (reusa `_ask`/`_ask_yn`/`_detect_tool` de `wizard.py`, mismo
    aviso de sobreescritura de `config.toml` si ya existe) →
    `import_profile_sources()` (ya existía, sin caller en producción hasta
    ahora) resuelve los tokens `{ROMS}/{SAVES}/{SYSTEM}` contra las rutas de
    este PC → `write_config_toml()` escribe `library.library_root`,
    `launchers.retroarch` y `[[sync.sources]]`. Luego `mkdir(parents=True)`
    por cada fuente + `sync_saves()` (dry-run por defecto, `--apply` para
    bajar de verdad — mismo patrón que `rommgr sync`). Si hay RetroArch
    configurado y ES-DE está instalado, regenera `custom_systems/es_systems.xml`
    (`generate_es_systems_xml()`). Termina con `check_bios()` sobre
    `library_root`/`library_root/bios`/`retroarch/system` y lista las BIOS
    requeridas que faltan. Test nuevo `tests/test_cli_restore.py` — manifiesto
    + config.toml de punta a punta con `RcloneTransport._run` fake (nunca
    contra rclone/Dropbox real, mismo criterio de `feedback_config_toml_manual_testing`).
    Suite completa: 1127 en verde. Lint (`ruff check`) limpio; `ruff format`
    solo reformateó el archivo nuevo (5 archivos preexistentes sin tocar
    siguen con drift de formato propio, no de esta sesión).
14. **DEVPROFILE-5 completo** (5a-5f) en el backlog. Cierra la pieza central
    de "restaurar en un PC nuevo desde el perfil en la nube" — pendiente
    solo de validarse en un PC realmente vacío (no probado contra hardware/
    nube real todavía, deliberado).

## Siguiente sesión recomendada (actualizado)

1. Validar `rommgr restore` en un PC/directorio realmente nuevo (o al menos
   contra un remoto Dropbox de prueba) antes de confiar en él para un caso
   real.
2. ✅ PRs #272, #273 y #274 (DEVPROFILE-4a, DEVPROFILE-7, DEVPROFILE-5a/5b-5f)
   rebasadas y mergeadas a `develop` (2026-09-04).
3. **DEVPROFILE-6** (botón Android de restauración) — diseñar después de
   validar 5, reusando lo aprendido.
4. Pendientes menores sin fecha: `DEVPROFILE-8` (restaurar BD/caché/DATs),
   `DEVPROFILE-9` (recientes/favoritos/playtime/credenciales RA).
