# Retro Vault — Día 24 (2026-03-31)

## Contexto

Sesión de continuación. Pendientes arrastrados de Día 23 + tareas pendientes del
`Tareas/Roadmap-Arquitectura-Frontend.md`.

---

## Flecos de Día 23 (prioritarios)

- [x] **B-test** — Verificar que el ganador RA queda renombrado y el perdedor en `_descartados/`
  (`handlers/duplicates.py::_apply_ra_conflicts`)
- [x] **D2** — Handler rclone: enrutar archivos a `saves_remote` o `states_remote` según extensión
  (`sync/rclone_transport.py`) — ya implementado (upload/download con routing por extensión + diagnose_routing)

---

## Migración Frontend — FASE 2 (continuación)

Estado actual: 13 de 17 ítems migrados. `app.js` bajó de 7.799 → ~3.947 líneas.

### 2a — `js/state.js` — Centralizar variables globales

Las ~40 variables globales de `app.js` todavía están sueltas. Este módulo las agrupa.

- [x] Crear `static/js/state.js` con objeto `AppState` y las variables actuales como propiedades
- [x] Exponer `window.AppState` desde `main.js`
- [x] Verificar que los tabs ya migrados siguen funcionando (no requiere migrarlos todos a la vez)

### 2b — `js/tabs/esde.js` — Tab ES-DE + Tools + RA Check + Doctor

- [x] Completado — `esde.js` existe con todas las funciones migradas

### 2c — `js/tabs/games.js` — Lista de juegos + game panel + TV mode

- [x] Completado — `games.js` existe

### 2d — `js/tabs/overview.js` — Overview + wizard + heatmap + charts

- [x] Completado — `overview.js` existe

### 2e — Eliminar `app.js` legacy

- [x] Completado — `app.js` eliminado; `main.js` es el único entry point

---

## Migración Frontend — FASE 3 (opcional, post-migración)

Solo tiene sentido después de eliminar `app.js`.

### 3a — `js/state.js` reactivo

- [ ] Implementar `createStore(initial)` con `get`, `set`, `subscribe`
- [ ] Migrar las secciones con más bugs de estado a usar el store
- [ ] Validar que no hay regresiones

---

## Migración Frontend — FASE 4 (opcional)

### 4a — SSE en lugar de polling de jobs

- [ ] Añadir endpoint `GET /api/job-events` que emita `text/event-stream`
- [ ] Reemplazar `setInterval` de `jobs.js` por `new EventSource('/api/job-events')`
- [ ] Mantener el polling como fallback si SSE no está disponible
- [ ] Validar que los toasts y el estado de jobs siguen funcionando

---

## Bugs críticos (de Día 23, aplazados)

### B2 / 0A — Batch run completo en Tools

- [ ] Añadir checkboxes para cada tool al batch run (Scan, Match, Health Check, Orphan, ZIP, CHD, Scraper, RA Check)
- [ ] Respetar orden lógico: esperar resultado de cada paso antes del siguiente
- [ ] Aplicar el selector de contexto (PC / Android) donde corresponda

### B3 / 0B — Comparador de bibliotecas PC vs Android

- [ ] `GET /api/library-diff` — comparar qué juegos hay en PC pero no en Android y viceversa
- [ ] Pantalla "Comparador de bibliotecas" con lista de diferencias
- [ ] `POST /api/sync-roms` — copiar ROMs faltantes en cada lado (ADB o filesystem)
- [ ] Verificación post-sync: re-comparar y confirmar que ambas listas son iguales
- [ ] Política de conflictos: si el mismo juego tiene SHA1 distinto en cada lado → mostrar al usuario

---

## Roadmap App Universal (pendientes de Día 23)

### Fase 1 — Primer arranque sin fricción

- [ ] Auto-detección de RetroArch en rutas comunes (C:\RetroArch-Win64, Steam, RetroBat, Program Files)
- [ ] Auto-detección de cores — leer `cores/` de RetroArch; avisar si falta alguno relevante
- [ ] Generador de `es_systems.cfg` a partir de los cores detectados
- [ ] Auto-detección de dispositivo Android via ADB al conectar USB
- [ ] Selector de carpeta con botón "Examinar" (Settings: library_root, android_root, etc.)

### Fase 2 — DATs sin esfuerzo

- [ ] Descarga guiada de DATs — botón "Descargar catálogos" con explicación contextual
- [ ] UI clara sobre modo de matching — visible cuándo es "con DAT" vs "por nombre de archivo"

### Fase 3 — Sync sin configuración

- [ ] WiFi sync directo PC ↔ consola via SFTP (prerequisito: Termux + sshd — guía en `Tareas/Guia-Termux-Anbernic.md`)
- [ ] Sync automático al conectar — detectar dispositivo via ADB y preguntar "¿Sincronizar ahora?"
- [ ] Estado de sync siempre visible en el header ("Último sync hace 2h · 3 archivos")

### Fase 4 — UX para no-técnicos

- [ ] Errores en lenguaje humano — ningún stack trace en la UI
- [ ] Ayuda contextual — tooltips e iconos `?` en cada sección
- [ ] UI responsive — funciona desde el navegador de la consola Android
- [ ] Notificaciones Windows toast al terminar sync o detectar juegos en inbox
- [ ] Nombres sin jerga — revisar UI: "DATs" → "Base de datos de juegos", "SHA1 match" → "Identificación automática"

### Fase 5 — Autenticación

- [ ] PIN de acceso cuando `host = 0.0.0.0`

### Fase 6 — Distribución

- [ ] S39-1 — Ejecutable PyInstaller (`RetroVault.exe` con Python embebido)
- [ ] Instalador Windows con Inno Setup — acceso directo, "Programas y características", DATs mínimos incluidos
- [ ] Auto-update — comprobar versión en GitHub Releases al arrancar
- [ ] Nombre definitivo — decidir entre Retro Vault y Retro Companion; README orientado al usuario final

---

## Propuestas de valor (de Día 23)

| # | Feature | Estado |
|---|---------|--------|
| P1 | Inbox file watcher (polling 30s → auto-pipeline → toast) | [ ] |
| P3 | Panel de espacio por plataforma (`GET /api/disk-usage`) | [ ] |
| P4 | Auto-resolución conflictos save (más reciente + backup + log) | [ ] |
| P5 | Completitud de colección (cruzar con DATs, % por plataforma) | [ ] |
| P6 | Verificación integridad CHD (`chdman verify` sobre todos los CHDs) | [ ] |
| P7 | Alertas de sync (badge si llevan +X días sin sync) | [ ] |
| P8 | Soporte multi-dispositivo (múltiples perfiles de consola) | [ ] |

---

## Orden de trabajo sugerido

```
Flecos Día 23 (B-test, D2) — rápidos
        ↓
FASE 2 migración JS:
  state.js → esde.js → games.js → overview.js → borrar app.js
        ↓
B2/0A — Batch run completo
        ↓
Fase 1 — Selector "Examinar" (bajo esfuerzo, alta QoL)
        ↓
B3/0B — Comparador bibliotecas PC/Android
        ↓
Fase 3 — WiFi sync + estado visible
        ↓
Propuestas P1, P3, P5
        ↓
FASE 3+4 (opcionales) + resto de roadmap
```
