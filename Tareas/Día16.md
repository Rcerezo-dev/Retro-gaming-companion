# Retro Vault — Día 16 (2026-03-21)

## Contexto

Este es el primer archivo de diario del proyecto. Cubre dos sesiones:
- **Sesión A (2026-03-20):** S34d parcial + BUG-5 + BUG-6 — continuación desde el Día 15 (2026-03-18)
- **Sesión B (2026-03-21):** B7-1..B7-9 + B6-7 + depuración ES en PC + organización en subcarpetas

El historial completo de S28–S34c está archivado en `Tareas/diario/Roadmap-S28-S34d-completado.md`.

---

## Objetivos del día

**Sesión A (2026-03-20)**
- [x] S34d-0/34d-1: fix parse_bins_from_cue + nueva pestaña Formatos
- [x] BUG-5: observabilidad CHD + protección arcade + verificación flows ES-DE/PSX
- [x] BUG-6: bugs en uso real (emuladores, scraper, inbox, RetroArch)

**Sesión B (2026-03-21)**
- [x] B7-1..B7-9: resolver bugs de UI detectados en uso real
- [x] B6-7: mejorar campos de media en gamelist.xml + documentar temas ES-DE
- [x] Depurar EmulationStation: exit code 1 al lanzar juegos
- [x] Arreglar "no se han encontrado archivos de juego" en ES
- [x] Limpiar pipeline de exportación de gamelists para ES-DE
- [x] Corregir duplicados que ES-DE mostraba por extensiones `.bin`/`.img`
- [x] Organizar juegos multi-fichero en subcarpetas propias

---

## Trabajo realizado

---

### SESIÓN A — 2026-03-20

### A1. S34d-0 — Fix `parse_bins_from_cue` sin comillas

`parse_bins_from_cue` no soportaba la forma sin comillas (`FILE nombre.bin BINARY`).
Arreglaba Street Fighter Collection y otros sets no estándar.

### A2. S34d-1 — Nueva pestaña "Formatos de archivo"

Reorganización de la UI: los convertidores y herramientas de formato se mueven de Tools
a una nueva pestaña dedicada **Formatos**. Tools queda con: Library Doctor, Estructura,
Saves huérfanos, Health Check, RA, Informe, Pegasus, Limpieza.

Formatos incluye: CHD, ZIP, N64, M3U, LPL, Análisis de carpeta, Multidisc.

### A3. BUG-5 — Observabilidad y robustez CHD + Arcade

| # | Fix |
|---|-----|
| B5-1 | CHD: checkbox "Solo errores" sobre la lista; se activa automáticamente si hay fallos; contador `bin_count` por juego |
| B5-2 | CHD: tras exit code 0, verifica que el `.chd` exista y tenga `st_size > 0`; si no, borra el fichero vacío y retorna `success=False` |
| B5-3 | Arcade matching: `mame_loader.py` parsea MAME XML + FBNeo DAT; `matcher.py` usa `load_arcade_dir`; endpoint `/api/import-arcade-catalog` |
| B5-4 | ES-DE PSX: `parse_bins_from_cue` corregido (B2-1), `set_detector.py` fija `.cue`/`.chd` duplicados (B6-2) |
| B5-5 | Pestaña Formatos: IDs HTML estables, JS sin cambios estructurales |

### A4. BUG-6 — Bugs detectados en uso real (2026-03-20)

| # | Fix |
|---|-----|
| B6-1 | **ES-DE: emuladores no lanzan** — `GET /api/retroarch-check`: verifica exe, cfg, cores dir, saves dirs. Panel "Diagnosticar" en Settings con tabla de checks + badges de cores por plataforma |
| B6-2 | **PSX duplicados/triplicados** — `set_detector.py`: `.cue` con `.chd` hermano → `"disc_auxiliary"`. CHD converter actualiza BD inmediatamente tras conversión. Fix `.iso` standalone para PS2/DC |
| B6-3 | **Scraper: metadatos no persisten** — cambiado de `repository.batch()` (rollback total en error) a `repository.connect()` + `conn.commit()` tras cada juego. Ahora cada ROM queda persistido en cuanto se procesa |
| B6-4 | **Scraper: reintentar automáticamente** — retry por juego con backoff 5s→15s→30s (3 intentos). PermissionError fatal. Errores de red no detienen el job. UI muestra "⚠ N errores de red" en tiempo real |
| B6-5 | **Inbox: pipeline no mueve ROMs** — ZIPs procesados se mueven a `inbox/_processed/` (excluida del scan por prefijo `_`). Si `delete_source=True`, se borran directamente |
| B6-6 | **RetroArch verificación completa** — cubierto por el mismo panel de B6-1. Verifica discrepancias entre la ruta que usa RetroArch y la que apunta ES-DE |

---

### SESIÓN B — 2026-03-21

### 1. B7-1 — "Resolver con RA" siempre pedía ejecutar comprobación

La lógica de detección de caché no reconocía que los datos de RetroAchievements ya estaban
disponibles aunque se hubiera ejecutado la comprobación. Fix en la detección de estado de caché
en `server.py`.

### 2. B7-2 — "Eliminar todos los duplicados" se bloqueaba a mitad

Los registros ya eliminados quedaban en la lista en memoria; al reintentar se fallaba sobre rutas
inexistentes. Fix: la lista se filtra tras cada eliminación exitosa.

### 3. B7-3 — Cable Sync: assets e imágenes en la sincronización

Añadidos dos checkboxes en Cable Sync:
- **Assets/imágenes** — copia las portadas scrapeadas a la tarjeta / ADB
- **Gamelists** — copia `gamelist.xml` a la consola Android

Implementación: `wantAssets` y `wantGamelists` construidos en el array `what` del frontend,
filtro `_wanted()` en el daemon reconoce ambas categorías.

### 4. B7-4 — Inbox: ordenar resultados por plataforma

Los resultados del análisis de carpeta (Inbox) ahora se ordenan por plataforma antes de renderizarlos,
con los archivos `unknown` al final. Centinela `'\xff'` como clave de sort para plataforma nula.

```javascript
const sortedFiles = [...(d.files || [])].sort((a, b) => {
    const pa = a.platform_guess || '\xff';
    const pb = b.platform_guess || '\xff';
    if (pa !== pb) return pa < pb ? -1 : 1;
    return a.name.localeCompare(b.name);
});
```

### 5. B7-5 — ZIP extractor: proteger ROMs de MAME/arcade

Los `.zip` de MAME son el formato nativo del ROM set — descomprimirlos los rompe.
Añadido `_ARCADE_FOLDER_NAMES` en `zip_extractor.py`; si el ZIP está dentro de una carpeta con
nombre arcade (`mame`, `arcade`, `fbneo`, `fba`, `neogeo`…), se omite con mensaje claro.

### 6. B7-6 — Conversor N64: indicador "sin herramienta externa"

El conversor N64 → `.z64` no requiere herramienta externa (Python puro). Se añadió un badge
permanente verde en el header del panel:

```html
&#x2713; Sin herramienta externa
```

### 7. B7-7 — Library Doctor: botones de acción directa

Las sugerencias del Library Doctor ahora tienen botones ejecutables desde la interfaz:
- **ROMs mal ubicados** → botón "Mover" → `POST /api/doctor-move-rom`
- **Carpetas vacías** → botón "Eliminar" → `POST /api/doctor-delete-dir`

Implementación: `window._doctorIssues[idx]` indexado por posición; filas con `id="doctor-row-N"`.
Botón se oculta tras la acción y la fila muestra confirmación inline.

### 8. B7-8 — RetroAchievements: tabla, icono copiar e informe HTML

- **Tabla más ancha:** `max-width:220px` → `min-width:180px` en columna de nombre de fichero
- **Icono copiar estándar:** botón de copiar query reemplazado por SVG de dos rectángulos
  (estándar UI) en todos los `onclick="_copyText(...)"` de la tabla RA
- **Informe HTML:** sección RetroAchievements añadida al export HTML del informe de biblioteca

### 9. B7-9 — Logs: solo backend, sin UI para usuario

El log viewer es solo para diagnóstico del desarrollador. Se añadió el endpoint `GET /api/logs`
(devuelve las últimas N líneas de `rommgr.log` y `cable_sync_ops.log`) pero se decidió
**no exponer UI** al usuario final. Los logs se pueden consultar desde el navegador directamente.

### 10. B6-7 — Campos de media adicionales en gamelist.xml

El scraper de ScreenScraper ahora descarga tres tipos de imagen por juego:

| Tag XML | Carpeta en disco | Uso en ES-DE |
|---------|------------------|--------------|
| `<image>` | `media/images/` | Portada (box art) |
| `<thumbnail>` + `<marquee>` | `media/wheels/` | Logo/wheel lateral |
| `<screenshot>` | `media/screenshots/` | Captura en detalle |

Cambios:
- `database/schema.py`: migración añade columnas `screenshot_path` y `wheel_path`
- `database/repository.py`: `upsert_metadata()` y `get_metadata_for_platform()` actualizados
- `scraper/gamelist_writer.py`: emite `<thumbnail>`, `<marquee>` y `<screenshot>`
- `server.py`: scraper descarga a `media/wheels/` y `media/screenshots/`; ambos paths pasados a `upsert_metadata()`
- `docs/esde-themes.md`: guía nueva con 4 temas recomendados (Art Book Next ⭐, Slate, Rbsimple-DE, Modern)

### 11. ES — Fix de lanzamiento (exit code 1)

- **Diagnóstico:** el port Aloshi 2015 (32-bit) usa `cmd.exe /c` para lanzar emuladores.
  En Windows 11 esto causa exit code 1 al inicializar D3D11/audio en apps 64-bit.
- **Fix:** 7 ficheros `.bat` wrapper que hacen `cd /d` al directorio del emulador antes de lanzarlo.
  `es_systems.cfg` actualizado para usar estos wrappers en todos los `<command>`.

Wrappers creados:
- `E:\Emuladores\Retroarch\run_retroarch.bat`
- `E:\Emuladores\Mupen64\run_mupen64.bat`
- `E:\Emuladores\Duckstation\run_duckstation.bat`
- `E:\Emuladores\PCSX2\run_pcsx2.bat`
- `E:\Emuladores\Flycast\run_flycast.bat`
- `E:\Emuladores\Dolphin (GC, Wii)\run_dolphin.bat`
- `E:\Emuladores\MAME\run_mame.bat`

### 12. ES — "no se han encontrado archivos de juego"

- **Diagnóstico:** los 19 ficheros `~/.emulationstation/gamelists/*/gamelist.xml` tenían `<path>`
  apuntando a `C:\Users\rammu\.emulationstation\gamelists\gba\Game.gba` en lugar de
  `E:\Carpetas anbernic\gba\Game.gba`. ES los usaba como fuente de verdad.
- **Fix:** eliminados los 19 gamelist.xml rotos. ES escanea directamente las carpetas de
  `es_systems.cfg` (que ya estaban bien configuradas).

### 13. Gamelist export — limpieza para ES-DE

El pipeline escribía copia secundaria de cada gamelist a `~/.emulationstation/gamelists/`,
lo que recreaba exactamente los ficheros rotos.

| Fichero | Cambio |
|---------|--------|
| `server.py` | Eliminada detección `_es_root` y escritura secundaria en scraper auto-export |
| `server.py` | Eliminada escritura secundaria en `_handle_export_gamelists` |
| `app.js` | `useEsdeGamelistDir()` limpia el campo (= `library_root`) en vez de rellenar con ruta incorrecta |
| `app.js` | `_autoFillEsdeGamelistDir()` ya no toca el campo de exportación |
| `frontend.py` | Eliminado botón "ES-DE ↗" |
| `frontend.py` | Eliminado botón "Copiar a ES-DE" del panel de detalle |
| `frontend.py` | Descripción del panel de exportación actualizada para ES-DE |

### 14. ES-DE — duplicados por extensiones

ES-DE mostraba cada track `.bin` como un juego separado.

| Sistema | Antes | Después |
|---------|-------|---------|
| PSX | `.cue .chd .pbp .m3u .img .bin` | `.cue .chd .pbp .m3u` |
| PS2 | `.iso .chd .bin .img .cso .zso .m3u` | `.iso .chd .cso .zso .m3u` |
| Saturn | `.cue .chd .iso .m3u .bin .mdf .img` | `.cue .chd .iso .m3u` |
| Dreamcast | `.cdi .gdi .chd .m3u .bin .iso .elf` | `.cdi .gdi .chd .m3u .iso .elf` |

### 15. Organización en subcarpetas para ROMs multi-fichero

Los juegos de plataformas de disco se reorganizan en subcarpetas al ejecutar "Aplicar renombres":

```
psx/Crash Bandicoot.cue           →  psx/Crash Bandicoot (USA)/Crash Bandicoot (USA).cue
psx/Crash Bandicoot (Track 1).bin    psx/Crash Bandicoot (USA)/Crash Bandicoot (Track 1).bin
```

| Fichero | Cambio |
|---------|--------|
| `operation_planner.py` | `_DISC_SUBFOLDER_PLATFORMS = {"psx","saturn","ps2","dreamcast","gamecube","wii"}`. Target = `platform_dir/stem/filename`. `already_correct` usa `source == target` |
| `file_renamer.py` | `move_disc_set_to_subfolder()`: mueve CUE o GDI + tracks + saves. Rollback atómico |
| `chd_converter.py` | `parse_tracks_from_gdi()`: parsea formato GDI de Dreamcast |
| `server.py` | `_handle_apply`: dispatch `.cue`/`.gdi` → `move_disc_set_to_subfolder`; `mkdir(parents=True)` para el resto |

| Plataforma | Formato | Tratamiento |
|---|---|---|
| PSX / Saturn | `.cue` + N×`.bin` | Mueve CUE + tracks |
| Dreamcast | `.gdi` + N×`.bin`/`.raw` | Parsea GDI, mueve todo |
| PS2 / GC / Wii | `.iso`/`.chd`/`.rvz` (único) | Solo mkdir + move |

---

## Archivos modificados

```
src/rom_manager/
  web/server.py                   — B6-1..6, B7-1,2,7,9; sin escritura secundaria gamelists; dispatch CUE/GDI en apply
  web/frontend.py                 — S34d-1 nueva pestaña Formatos; B7-6,9; botones ES-DE eliminados
  web/static/app.js               — B7-3,4,7,8; useEsdeGamelistDir y _autoFillEsdeGamelistDir corregidos
  web/response_builders.py        — B6-1: retroarch-check response builder
  planner/operation_planner.py    — _DISC_SUBFOLDER_PLATFORMS; target con subfolder; source==target
  renamer/file_renamer.py         — move_disc_set_to_subfolder() con rollback atómico
  converters/chd_converter.py     — B5-1,2: observabilidad + validación post-conversión; parse_tracks_from_gdi()
  converters/zip_extractor.py     — B7-5: _ARCADE_FOLDER_NAMES
  converters/n64_converter.py     — (nuevo, S34) byte-swap .v64/.n64 → .z64 en Python puro
  detection/platform_detector.py  — S34: detección ampliada 180+ plataformas vía platforms.toml
  detection/platforms.toml        — (nuevo, S34) configuración externa de plataformas
  detection/bios_checker.py       — (nuevo, S34) 21 BIOS con MD5, sección en Settings
  database/schema.py              — B6-7: screenshot_path + wheel_path columns
  database/repository.py          — B6-7: upsert_metadata + get_metadata_for_platform
  scraper/gamelist_writer.py      — B6-7: <thumbnail>, <marquee>, <screenshot>
  catalog/mame_loader.py          — (nuevo, B5-3/S34b) parser MAME XML + FBNeo DAT
  utils/lpl_generator.py          — (nuevo, S32) generador playlists RetroArch .lpl
  utils/health_checker.py         — actualizaciones varias
  config.py                       — S28/34: retroarch_path, launcher_cores, backup_saves_*

docs/
  esde-themes.md                  — nuevo: guía de temas ES-DE recomendados (B6-7)

C:\Users\rammu\.emulationstation\
  es_systems.cfg                  — wrappers .bat en <command>; .bin/.img/.mdf eliminados de extensiones
  gamelists\*/gamelist.xml        — eliminados (19 ficheros con rutas incorrectas)

E:\Emuladores\*\run_*.bat         — 7 wrappers nuevos (retroarch, mupen64, duckstation, pcsx2, flycast, dolphin, mame)
```

---

## Estado al finalizar

- B7-1 a B7-9 completados ✅
- B6-7 completado ✅
- EmulationStation arranca y encuentra todos los juegos ✅
- ES-DE no mostrará duplicados por tracks `.bin` ✅
- Pipeline de export nunca escribe a `~/.emulationstation/gamelists/` ✅
- ROMs multi-fichero (PSX/Saturn/Dreamcast) se organizan en subcarpetas al aplicar ✅
- `34d-3` marcado como completado ✅

**Pendiente de validar en hardware:**
- Wrappers `.bat`: confirmar que los emuladores arrancan desde ES
- ES-DE: confirmar detección de juegos tras cambios de extensiones
- Aplicar renombres con juegos PSX reales y verificar subcarpetas + tracks

---

## Siguiente sesión recomendada

- **S35** — Modo claro + grid de plataformas con logos (siguiente en roadmap Tier 2)
- **B1-4** — Resolución inteligente de duplicados conservando la copia con logros RA
- Validación en hardware de todo lo de hoy
---

### S34d — Formatos de archivo (pendientes)

| # | Qué | Detalle |
|---|-----|---------|
| 34d-2 | **Conversión CSO/ZSO → ISO (PSP)** | Sección para descomprimir `.cso`/`.zso` a `.iso` usando maxcso. Mostrar aviso si no se encuentra el binario. |
| 34d-3 ✅ | **Organizar en subcarpetas — multi-fichero** | `move_disc_set_to_subfolder()` en `file_renamer.py` mueve CUE+BINs (o GDI+tracks) y saves al subdirectorio. `parse_tracks_from_gdi()` en `chd_converter.py`. Plataformas: PSX, Saturn, PS2, Dreamcast, GameCube, Wii. Rollback atómico. `_DISC_SUBFOLDER_PLATFORMS` en `operation_planner.py`. |

---

## Tier 2 — Poder y comodidad

### S35 — Modo claro + Grid de plataformas con logos

| # | Qué | Detalle |
|---|-----|---------|
| 35-1 | **Modo claro** | Refactor CSS con variables CSS (`--bg`, `--fg`, `--accent`…). Toggle en Settings. Oscuro como default. |
| 35-2 | **Grid de plataformas con logos** | En Overview: tarjetas por plataforma con logo SVG inline. ~20 logos SVG embebidos (NES, SNES, GBA, PSX, PS2, N64, GB, GBC, GG, MD, SMS, NDS…). Sin dependencias de internet. |
| 35-3 | **QR de acceso** | Generar QR de la URL local en Settings para escanear con la consola/móvil. Implementación pure-Python (mini QR Level 1). |
| 35-4 | **Animaciones de panel** | Microanimaciones: carga de portada (skeleton), hover en cards, transición de estado de completado. |
| 35-5 | **Vista "Colección" estilo vitrina** | Ruta `/collection` — grid de carátulas a tamaño grande por plataforma, como una estantería virtual. Click abre el panel de detalle. |

---

## Tier 3 — Experiencia visual avanzada

### S36 — Modo presentación (TV mode) + Mapa de calor + Juego del día

| # | Qué | Detalle |
|---|-----|---------|
| 36-1 | **TV mode** | Ruta `/tv` — pantalla completa, grid de carátulas grandes, navegación por teclado/flechas. Sin ratón. |
| 36-2 | **Mapa de calor de actividad** | Cuadrícula estilo GitHub (últimos 365 días). Calculable desde `last_played_at` sin cambios en backend. |
| 36-3 | **Análisis de tiempo por plataforma** | Gráfico de barras mensual: cuántos juegos distintos tocaste en GBA este mes. Agrupa `last_played_at` por (plataforma, mes). |
| 36-4 | **"Juego del día"** | Tarjeta en Overview con sugerencia aleatoria de un juego no tocado en >6 meses. Botón "ignorar". |
| 36-5 | **Tracker de tiempo de juego (RetroArch log)** | Parsear logs de sesión de RetroArch para extraer tiempo jugado por juego. Mostrar en panel de detalle y mapa de calor. |

---

## Tier 4 — Automatización y robustez

### S37 — Health check + Delta sync + Notificaciones

| # | Qué | Detalle |
|---|-----|---------|
| 37-1 | **Health check programado** | Health check semanal en background. Notificar si algún ROM tiene hash diferente al registrado (corrupción silenciosa). |
| 37-2 | **Delta sync** | Solo transferir bytes que cambiaron en el save (bsdiff). Relevante para saves de PSX/N64 grandes. |
| 37-3 | **Notificaciones de escritorio** | Al terminar sync, detectar inbox o encontrar ROMs corruptos: toast nativo de Windows via `PowerShell` (sin dependencias). |
| 37-4 | **Reintentos inteligentes en sync** | Si rclone falla por timeout, reintento con backoff exponencial. Actualmente falla silenciosamente. |

---

## Tier 5 — Experimental / Largo plazo

### S38 — Multi-perfil + Modo headless

| # | Qué | Detalle |
|---|-----|---------|
| 38-1 | **Multi-perfil de saves** | Perfiles separados (usuario 1, usuario 2…) con su propia carpeta de sync y `last_played_at`. Útil para familias. |
| 38-2 | **Modo headless completo** | Todo desde CLI: `rommgr sync`, `rommgr inbox`, `rommgr health`. Sin levantar el servidor web. Útil para Task Scheduler. |
| 38-3 | **API REST documentada** | Generar `openapi.json` desde los endpoints existentes. Permite integraciones externas (Home Assistant, scripts personales). |

---

### S39 — MAME avanzado + Agente Claude

| # | Qué | Detalle |
|---|-----|---------|
| 39-1 | **Soporte MAME avanzado** | Lo que queda tras S34c: ROMs MAME split sets vs merged sets, CHDs de MAME (`.chd` con nombre de carpeta = set name), detección de samples y artwork directories. |
| 39-2 | **Agente Claude integrado** | Chat en la UI que responde preguntas sobre la biblioteca usando la BD y la API de Claude. "¿Qué juegos de PSX no he completado?", "¿Cuánto pesa mi colección de GBA?". |

---

### S40 — Distribución Windows — **ÚLTIMA SESIÓN, SIEMPRE**

> No tocar hasta que todo lo anterior funcione end-to-end en hardware real.

| # | Qué | Detalle |
|---|-----|---------|
| 40-1 | **Ejecutable PyInstaller** | `pyinstaller --onefile --noconsole` → `RetroVault.exe`. Bundle incluye `tools/chdman.exe`, `tools/adb.exe`. Abre navegador automáticamente. |
| 40-2 | **Icono** | `.ico` de 256×256. Diseño: logo vault + pixel art. |
| 40-3 | **Instalador NSIS o InnoSetup** | Instala en `%LOCALAPPDATA%\RetroVault\`. Acceso directo escritorio + menú inicio. Arranque con Windows. Desinstalador limpio. |
| 40-4 | **Icono de bandeja (tray)** | Minimizar a tray en vez de cerrar. Menú: "Abrir", "Sync ahora", "Salir". `pystray` + `Pillow`. |
| 40-5 | **Auto-update** | Comprobar GitHub Releases en arranque. Toast con link si hay versión nueva. Sin actualización automática (opt-in). |
| 40-6 | **GitHub Releases** | Pipeline: tag → PyInstaller → upload `.exe` firmado + `CHANGELOG.md` auto-generado. |

---

## Resumen visual

```
Tier 1 — Bugs y pendientes activos
  BUG-1   Duplicados: resolución inteligente                          [B1-4]  (B1-1 ✅)
  BUG-2   ✅ Resuelto
  BUG-3   ✅ Resuelto
  BUG-5   Observabilidad CHD + verificación arcade + ES-DE PSX        [B5-1..B5-5]
  BUG-6   ✅ Bugs uso real (2026-03-20)                               [B6-1..7 ✅]
  BUG-7   ✅ Bugs uso real (2026-03-21)                               [B7-1..B7-9]
  S34d    Formatos: CSO/ZSO + subcarpetas PSX                         [34d-2, 34d-3]

Tier 2 — Poder y comodidad
  S35     Modo claro + logos plataforma + QR + vitrina

Tier 3 — Visual avanzado
  S36     TV mode + mapa de calor + análisis tiempo + juego del día

Tier 4 — Automatización
  S37     Health check + delta sync + notificaciones escritorio
  S38     Multi-perfil + modo headless + API REST

Tier 5 — Experimental
  S39     MAME avanzado + agente Claude integrado

  S40  🏁 DISTRIBUCIÓN — PyInstaller + NSIS + tray + auto-update
```

---

## Referencia: Formatos de archivo por plataforma (ES-DE / RetroArch)

> Todos los sistemas aceptan además `.zip` y `.7z`.

### Consolas Nintendo

| Sistema | Extensiones |
|---------|-------------|
| NES / Famicom | `.nes` `.unf` `.unif` `.nsf` |
| Famicom Disk System | `.fds` |
| SNES / Super Famicom | `.smc` `.sfc` `.fig` `.swc` `.bs` `.st` |
| Nintendo 64 | `.z64` `.v64` `.n64` `.ndd` `.u1` |
| GameCube | `.iso` `.gcm` `.rvz` `.wia` `.wbfs` `.ciso` `.gcz` `.m3u` |
| Wii | `.iso` `.rvz` `.wia` `.wbfs` `.ciso` `.gcz` `.wad` `.m3u` |
| Game Boy | `.gb` `.gbc` `.sgb` |
| Game Boy Color | `.gbc` `.gb` |
| Game Boy Advance | `.gba` `.agb` `.gbz` |
| Nintendo DS | `.nds` `.dsi` `.ids` `.srl` `.app` |
| Nintendo 3DS | `.3ds` `.3dsx` `.cia` `.csu` `.cci` `.cxi` `.app` |
| Virtual Boy | `.vb` `.vboy` `.bin` |
| Pokémon Mini | `.min` |

### Consolas Sony

| Sistema | Extensiones |
|---------|-------------|
| PlayStation (PSX) | `.bin` `.cue` `.iso` `.img` `.pbp` `.mdf` `.toc` `.cbn` `.m3u` `.chd` |
| PlayStation 2 | `.iso` `.bin` `.img` `.mdf` `.gz` `.cso` `.zso` `.chd` `.m3u` |
| PlayStation 3 | `.pkg` `.ps3` `.ps3dir` |
| PSP | `.iso` `.cso` `.pbp` `.elf` `.prx` `.ppdmp` `.chd` |
| PS Vita | `.vpk` `.psvita` |

### Consolas Sega

| Sistema | Extensiones |
|---------|-------------|
| Master System / Mark III | `.sms` `.bin` `.sg` |
| Game Gear | `.gg` `.bin` |
| Mega Drive / Genesis | `.md` `.bin` `.smd` `.gen` `.68k` `.sgd` `.chd` |
| Mega-CD / Sega CD | `.bin` `.cue` `.iso` `.chd` `.m3u` |
| 32X | `.32x` `.bin` `.smd` |
| Saturn | `.bin` `.cue` `.iso` `.mdf` `.img` `.chd` `.m3u` |
| Dreamcast | `.bin` `.cue` `.iso` `.gdi` `.elf` `.cdi` `.m3u` `.chd` |
| SG-1000 | `.sg` `.bin` `.sc` `.sf7` |

### Consolas Atari

| Sistema | Extensiones |
|---------|-------------|
| Atari 2600 | `.a26` `.bin` `.rom` `.cart` |
| Atari 5200 | `.a52` `.bin` `.car` |
| Atari 7800 | `.a78` `.bin` |
| Atari Lynx | `.lnx` `.lyx` `.o` |
| Atari Jaguar | `.j64` `.jag` `.rom` `.abs` `.cof` `.bin` |
| Atari Jaguar CD | `.bin` `.cue` `.chd` |
| Atari ST / STE / TT / Falcon | `.st` `.msa` `.stx` `.dim` `.ipf` `.m3u` |
| Atari XL/XE | `.xex` `.atr` `.xfd` `.atx` `.cdm` `.cas` `.car` `.bin` `.a8s` |

### Neo Geo / SNK

| Sistema | Extensiones |
|---------|-------------|
| Neo Geo | `.neo` `.zip` `.bin` |
| Neo Geo CD | `.bin` `.cue` `.chd` `.m3u` |
| Neo Geo Pocket / Color | `.ngp` `.ngc` `.ngpc` `.npc` |

### NEC

| Sistema | Extensiones |
|---------|-------------|
| PC Engine / TurboGrafx-16 | `.pce` `.tg16` |
| PC Engine CD | `.bin` `.cue` `.chd` `.m3u` |
| PC-FX | `.fx` `.img` `.iso` `.cue` `.chd` `.m3u` |
| SuperGrafx | `.pce` `.sgx` |

### Computadoras personales

| Sistema | Extensiones |
|---------|-------------|
| DOS (DOSBox) | `.bat` `.com` `.exe` `.conf` |
| ScummVM | `.scummvm` |
| Amiga | `.adf` `.adz` `.dms` `.fdi` `.ipf` `.hdf` `.hdz` `.lha` `.slave` `.info` `.cue` `.ccd` `.nrg` `.mds` `.iso` `.m3u` |
| Commodore 64 | `.d64` `.d71` `.d80` `.d81` `.d82` `.g64` `.g41` `.x64` `.t64` `.tap` `.prg` `.p00` `.crt` `.bin` `.nib` `.nbz` |
| MSX / MSX2 | `.rom` `.ri` `.mx1` `.mx2` `.col` `.dsk` `.cas` `.sg` `.sc` `.m3u` |
| ZX Spectrum | `.tzx` `.tap` `.z80` `.rzx` `.scl` `.trd` `.dsk` |
| Amstrad CPC | `.dsk` `.sna` `.kcr` `.voc` `.cpr` `.m3u` |
| Sharp X68000 | `.dim` `.img` `.d88` `.88d` `.hdm` `.dup` `.2hd` `.xdf` `.hdf` `.cmd` `.m3u` |

### Handhelds y otros

| Sistema | Extensiones |
|---------|-------------|
| WonderSwan / Color | `.ws` `.wsc` `.bin` `.pc2` |
| Game & Watch | `.mgw` |
| Vectrex | `.bin` `.gam` `.vec` |
| ColecoVision | `.bin` `.col` `.rom` |
| Intellivision | `.int` `.bin` `.rom` |
| 3DO | `.iso` `.bin` `.cue` `.chd` |

### Arcade

| Sistema | Extensiones |
|---------|-------------|
| MAME (genérico) | `.zip` `.7z` `.chd` |
| FBNeo (FinalBurn Neo) | `.zip` `.7z` `.chd` |
| CPS-1 / CPS-2 / CPS-3 | `.zip` `.7z` |


---

### BUG-7 — Bugs detectados en uso real (2026-03-21)

| # | Qué | Detalle |
|---|-----|---------|
| B7-1 ✅ | **Organizar: "Resolver con RA" siempre pide ejecutar comprobación** | Al pulsar "Resolver con RA" en la pestaña Organizar (duplicados), siempre aparece el aviso "ejecuta comprobación de RA primero" aunque ya se haya hecho. La lógica de detección de caché no reconoce que los datos ya están disponibles. |
| B7-2 ✅ | **Organizar: "Eliminar todos los duplicados" se bloquea a mitad** | Llegado un punto, el botón "Eliminar todos los duplicados" no elimina nada más. Posible causa: los registros ya eliminados quedan en la lista en memoria y se reintenta sobre rutas que ya no existen. |
| B7-3 ✅ | **Cable Sync: añadir assets y gamelists a la sincronización** | Añadir dos checkboxes en Cable Sync: (1) **Assets/imágenes** (archivos scrapeados — imágenes de portadas) para copiarlos a la tarjeta microSD o por ADB; (2) **gamelist.xml** para que EmulationStation en la Anbernic tenga los metadatos. La ruta destino en la Anbernic sería `/storage/emulated/0/RetroArch/roms/<plataforma>/gamelist.xml`. |
| B7-4 ✅ | **Inbox: ordenar resultados del análisis por plataforma** | Al analizar carpeta en Inbox, los juegos deberían aparecer agrupados por plataforma (ej. todos los GBA juntos, todos los SNES juntos), con los archivos `unknown` al final de la lista. |
| B7-5 ✅ | **ZIP extractor: proteger ROMs de MAME/arcade de ser descomprimidas** | Los archivos `.zip` de MAME son el formato nativo del ROM set — descomprimirlos los rompe. El extractor de ZIPs en la pestaña Formatos debe detectar si un ZIP está en una carpeta de plataforma arcade (`arcade`, `fbneo`, `mame`) y omitirlo con un mensaje claro. |
| B7-6 ✅ | **Conversor N64: verificar que la herramienta n64conv está disponible** | El conversor N64 → .z64 puede no tener disponible la herramienta necesaria. Añadir comprobación de existencia del ejecutable al cargar la pestaña, con mensaje claro si falta (igual que el check de chdman). |
| B7-7 ✅ | **Library Doctor: botones de acción directa** | Las sugerencias del Library Doctor (ej. "mover ROM a carpeta correcta") solo muestran texto. Cada acción sugerida debería tener un botón que la ejecute directamente desde la interfaz sin tener que hacerlo manualmente. |
| B7-8 ✅ | **RetroAchievements: tabla estrecha + botón copiar + columna en informe HTML** | (1) La tabla de compatibilidad de logros es demasiado estrecha — ampliar o hacer scrollable horizontalmente. (2) El botón de copiar la query de búsqueda debe usar el icono estándar de "copiar" (dos rectángulos con esquinas redondeadas), no texto. (3) La columna de logros RA debe aparecer también en el informe HTML de salud de biblioteca. |
| B7-9 ✅ | **Logs: exportar archivos de log de todos los módulos** | Crear una sección en Settings o en la pestaña de herramientas que permita descargar/ver los logs de todos los módulos (scanner, scraper, inbox, cable sync, CHD converter, sync rclone) para facilitar el diagnóstico de problemas. |


---

### BUG-9 — Mejoras detectadas en uso real (2026-03-21) → añadidas al Roadmap

| # | Qué | Detalle |
|---|-----|---------|
| B9-1 | **Library Doctor: botón "resolver todos los conflictos"** | Las sugerencias del Library Doctor muestran la acción correcta (ej. "mover a megadrive/") pero no la ejecutan. Añadir botón "Resolver todos" que aplique todas las acciones sugeridas de una vez. |
| B9-2 | **Compatibilidad RA: tabla demasiado estrecha** | La tabla sigue siendo demasiado estrecha — hay que hacer scroll lateral para ver todas las columnas. Hacer `overflow-x: auto` con `min-width` por columna. |
| B9-3 | **Botón "copiar" en informe RA no copia nada** | El botón no funciona. Valorar alternativa: botón "Descargar versión X" donde X es la versión del ROM que sí tiene logros según los hashes de RA. |
| B9-4 | **Saves huérfanos: mover todos de golpe** | La sección da sugerencias individuales pero no permite archivarlos en batch. Añadir botón "Mover todos a `_huerfanos/`". |
| B9-5 | **Guía de carpetas de saves en Anbernic** | Documentar la estructura correcta de saves en Anbernic (rutas RetroArch Android por plataforma). Crear `docs/guias/` para este tipo de documentación. |

---

### Tarea completada — Portabilidad a otro PC (2026-03-21) ✅

- `config.toml` ya estaba ignorado por git — contiene contraseñas reales (screenscraper, RA API key, dev_pass). **Nunca subirlo.**
- `Tareas/` eliminado de `.gitignore` → roadmap, diarios y notas ahora en GitHub.
- Creado `config.toml.example` con placeholders para setup limpio en PC nuevo.
- Commit y push realizados (commit `c6b61e3`).